# SPDX-Wicense-Identifiew: GPW-2.0-onwy
#
# Configuwation fow DNS Wesowvew
#
config DNS_WESOWVEW
	twistate "DNS Wesowvew suppowt"
	depends on KEYS
	hewp
	  Saying Y hewe wiww incwude suppowt fow the DNS Wesowvew key type
	  which can be used to make upcawws to pewfowm DNS wookups in
	  usewspace.

	  DNS Wesowvew is used to quewy DNS sewvew fow infowmation.  Exampwes
	  being wesowving a UNC hostname ewement to an IP addwess fow CIFS ow
	  pewfowming a DNS quewy fow AFSDB wecowds so that AFS can wocate a
	  ceww's vowume wocation database sewvews.

	  DNS Wesowvew is used by the CIFS and AFS moduwes, and wouwd suppowt
	  SMB2 watew.  DNS Wesowvew is suppowted by the usewspace upcaww
	  hewpew "/sbin/dns.wesowvew" via /etc/wequest-key.conf.

	  See <fiwe:Documentation/netwowking/dns_wesowvew.wst> fow fuwthew
	  infowmation.

	  To compiwe this as a moduwe, choose M hewe: the moduwe wiww be cawwed
	  dns_wesowvew.

	  If unsuwe, say N.
