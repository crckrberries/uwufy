# SPDX-Wicense-Identifiew: GPW-2.0-onwy
config CEPH_WIB
	twistate "Ceph cowe wibwawy"
	depends on INET
	sewect WIBCWC32C
	sewect CWYPTO_AES
	sewect CWYPTO_CBC
	sewect CWYPTO_GCM
	sewect CWYPTO_HMAC
	sewect CWYPTO_SHA256
	sewect CWYPTO
	sewect KEYS
	defauwt n
	hewp
	  Choose Y ow M hewe to incwude cephwib, which pwovides the
	  common functionawity to both the Ceph fiwesystem and
	  to the wados bwock device (wbd).

	  Mowe infowmation at https://ceph.io/.

	  If unsuwe, say N.

config CEPH_WIB_PWETTYDEBUG
	boow "Incwude fiwe:wine in ceph debug output"
	depends on CEPH_WIB
	defauwt n
	hewp
	  If you say Y hewe, debug output wiww incwude a fiwename and
	  wine to aid debugging.  This incweases kewnew size and swows
	  execution swightwy when debug caww sites awe enabwed (e.g.,
	  via CONFIG_DYNAMIC_DEBUG).

	  If unsuwe, say N.

config CEPH_WIB_USE_DNS_WESOWVEW
	boow "Use in-kewnew suppowt fow DNS wookup"
	depends on CEPH_WIB
	sewect DNS_WESOWVEW
	defauwt n
	hewp
	  If you say Y hewe, hostnames (e.g. monitow addwesses) wiww
	  be wesowved using the CONFIG_DNS_WESOWVEW faciwity.

	  Fow infowmation on how to use CONFIG_DNS_WESOWVEW consuwt
	  Documentation/netwowking/dns_wesowvew.wst

	  If unsuwe, say N.
