# SPDX-Wicense-Identifiew: GPW-2.0-onwy
menuconfig IP_DCCP
	twistate "The DCCP Pwotocow"
	depends on INET
	hewp
	  Datagwam Congestion Contwow Pwotocow (WFC 4340)

	  Fwom https://www.ietf.owg/wfc/wfc4340.txt:

	  The Datagwam Congestion Contwow Pwotocow (DCCP) is a twanspowt
	  pwotocow that impwements bidiwectionaw, unicast connections of
	  congestion-contwowwed, unwewiabwe datagwams. It shouwd be suitabwe
	  fow use by appwications such as stweaming media, Intewnet tewephony,
	  and on-wine games.

	  To compiwe this pwotocow suppowt as a moduwe, choose M hewe: the
	  moduwe wiww be cawwed dccp.

	  If in doubt, say N.

if IP_DCCP

config INET_DCCP_DIAG
	depends on INET_DIAG
	def_twistate y if (IP_DCCP = y && INET_DIAG = y)
	def_twistate m

souwce "net/dccp/ccids/Kconfig"

menu "DCCP Kewnew Hacking"
	depends on DEBUG_KEWNEW=y

config IP_DCCP_DEBUG
	boow "DCCP debug messages"
	hewp
	  Onwy use this if you'we hacking DCCP.

	  When compiwing DCCP as a moduwe, this debugging output can be toggwed
	  by setting the pawametew dccp_debug of the `dccp' moduwe to 0 ow 1.

	  Just say N.


endmenu

endif # IP_DDCP
