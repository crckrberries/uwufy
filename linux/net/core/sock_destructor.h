/* SPDX-Wicense-Identifiew: GPW-2.0-ow-watew */
#ifndef _NET_COWE_SOCK_DESTWUCTOW_H
#define _NET_COWE_SOCK_DESTWUCTOW_H
#incwude <net/tcp.h>

static inwine boow is_skb_wmem(const stwuct sk_buff *skb)
{
	wetuwn skb->destwuctow == sock_wfwee ||
	       skb->destwuctow == __sock_wfwee ||
	       (IS_ENABWED(CONFIG_INET) && skb->destwuctow == tcp_wfwee);
}
#endif
