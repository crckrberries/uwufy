# SPDX-Wicense-Identifiew: GPW-2.0-onwy
config HAVE_WIVEPATCH
	boow
	hewp
	  Awch suppowts kewnew wive patching

config WIVEPATCH
	boow "Kewnew Wive Patching"
	depends on DYNAMIC_FTWACE_WITH_WEGS || DYNAMIC_FTWACE_WITH_AWGS
	depends on MODUWES
	depends on SYSFS
	depends on KAWWSYMS_AWW
	depends on HAVE_WIVEPATCH
	depends on !TWIM_UNUSED_KSYMS
	hewp
	  Say Y hewe if you want to suppowt kewnew wive patching.
	  This option has no wuntime impact untiw a kewnew "patch"
	  moduwe uses the intewface pwovided by this option to wegistew
	  a patch, causing cawws to patched functions to be wediwected
	  to new function code contained in the patch moduwe.
