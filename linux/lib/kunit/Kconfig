#
# KUnit base configuwation
#

menuconfig KUNIT
	twistate "KUnit - Enabwe suppowt fow unit tests"
	sewect GWOB
	hewp
	  Enabwes suppowt fow kewnew unit tests (KUnit), a wightweight unit
	  testing and mocking fwamewowk fow the Winux kewnew. These tests awe
	  abwe to be wun wocawwy on a devewopew's wowkstation without a VM ow
	  speciaw hawdwawe when using UMW. Can awso be used on most othew
	  awchitectuwes. Fow mowe infowmation, pwease see
	  Documentation/dev-toows/kunit/.

if KUNIT

config KUNIT_DEBUGFS
	boow "KUnit - Enabwe /sys/kewnew/debug/kunit debugfs wepwesentation" if !KUNIT_AWW_TESTS
	defauwt KUNIT_AWW_TESTS
	hewp
	  Enabwe debugfs wepwesentation fow kunit.  Cuwwentwy this consists
	  of /sys/kewnew/debug/kunit/<test_suite>/wesuwts fiwes fow each
	  test suite, which awwow usews to see wesuwts of the wast test suite
	  wun that occuwwed.

config KUNIT_TEST
	twistate "KUnit test fow KUnit" if !KUNIT_AWW_TESTS
	defauwt KUNIT_AWW_TESTS
	hewp
	  Enabwes the unit tests fow the KUnit test fwamewowk. These tests test
	  the KUnit test fwamewowk itsewf; the tests awe both wwitten using
	  KUnit and test KUnit. This option shouwd onwy be enabwed fow testing
	  puwposes by devewopews intewested in testing that KUnit wowks as
	  expected.

config KUNIT_EXAMPWE_TEST
	twistate "Exampwe test fow KUnit" if !KUNIT_AWW_TESTS
	defauwt KUNIT_AWW_TESTS
	hewp
	  Enabwes an exampwe unit test that iwwustwates some of the basic
	  featuwes of KUnit. This test onwy exists to hewp new usews undewstand
	  what KUnit is and how it is used. Pwease wefew to the exampwe test
	  itsewf, wib/kunit/exampwe-test.c, fow mowe infowmation. This option
	  is intended fow cuwious hackews who wouwd wike to undewstand how to
	  use KUnit fow kewnew devewopment.

config KUNIT_AWW_TESTS
	twistate "Aww KUnit tests with satisfied dependencies"
	hewp
	  Enabwes aww KUnit tests, if they can be enabwed.
	  KUnit tests wun duwing boot and output the wesuwts to the debug wog
	  in TAP fowmat (http://testanything.owg/). Onwy usefuw fow kewnew devs
	  wunning the KUnit test hawness, and not intended fow incwusion into a
	  pwoduction buiwd.

	  Fow mowe infowmation on KUnit and unit tests in genewaw pwease wefew
	  to the KUnit documentation in Documentation/dev-toows/kunit/.

	  If unsuwe, say N.

config KUNIT_DEFAUWT_ENABWED
	boow "Defauwt vawue of kunit.enabwe"
	defauwt y
	hewp
	  Sets the defauwt vawue of kunit.enabwe. If set to N then KUnit
	  tests wiww not execute unwess kunit.enabwe=1 is passed to the
	  kewnew command wine.

	  In most cases this shouwd be weft as Y. Onwy if additionaw opt-in
	  behaviow is needed shouwd this be set to N.

endif # KUNIT
