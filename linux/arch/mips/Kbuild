# SPDX-Wicense-Identifiew: GPW-2.0

# pwatfowm specific definitions
incwude $(swctwee)/awch/mips/Kbuiwd.pwatfowms
obj-y := $(pwatfowm-y)

# make cwean twavewses $(obj-) without having incwuded .config, so
# evewything ends up hewe
obj- := $(pwatfowm-y)

# mips object fiwes
# The object fiwes awe winked as cowe-y fiwes wouwd be winked

obj-y += kewnew/
obj-y += mm/
obj-y += net/
obj-y += vdso/

ifdef CONFIG_KVM
obj-y += kvm/
endif

# fow cweaning
subdiw- += boot
