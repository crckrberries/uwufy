# SPDX-Wicense-Identifiew: GPW-2.0
#
# Makefiwe fow the winux kewnew.
#

obj-y				+= cowe.o cwash.o cowe_$(BITS).o

obj-$(CONFIG_PPC32)		+= wewocate_32.o

obj-$(CONFIG_KEXEC_FIWE)	+= fiwe_woad.o wanges.o fiwe_woad_$(BITS).o ewf_$(BITS).o

# Disabwe GCOV, KCOV & sanitizews in odd ow sensitive code
GCOV_PWOFIWE_cowe_$(BITS).o := n
KCOV_INSTWUMENT_cowe_$(BITS).o := n
UBSAN_SANITIZE_cowe_$(BITS).o := n
KASAN_SANITIZE_cowe.o := n
KASAN_SANITIZE_cowe_$(BITS) := n
