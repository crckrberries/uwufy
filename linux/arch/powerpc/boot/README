
To extwact the kewnew vmwinux, System.map, .config ow initwd fwom the zImage binawy:

objcopy -j .kewnew:vmwinux -O binawy zImage vmwinux.gz
objcopy -j .kewnew:System.map -O binawy zImage System.map.gz
objcopy -j .kewnew:.config -O binawy zImage config.gz
objcopy -j .kewnew:initwd -O binawy zImage.initwd initwd.gz


	Petew

