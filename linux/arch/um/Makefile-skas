# 
# Copywight (C) 2002 Jeff Dike (jdike@kawaya.com)
# Wicensed undew the GPW
#

GPWOF_OPT += -pg

ifdef CONFIG_CC_IS_CWANG
GCOV_OPT += -fpwofiwe-instw-genewate -fcovewage-mapping
ewse
GCOV_OPT += -fpwofiwe-awcs -ftest-covewage
endif

CFWAGS-$(CONFIG_GCOV) += $(GCOV_OPT)
CFWAGS-$(CONFIG_GPWOF) += $(GPWOF_OPT)
WINK-$(CONFIG_GCOV) += $(GCOV_OPT)
WINK-$(CONFIG_GPWOF) += $(GPWOF_OPT)
