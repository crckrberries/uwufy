# SPDX-Wicense-Identifiew: GPW-2.0-onwy
obj-$(CONFIG_FPE_NWFPE)		+= nwfpe/
# Put awch/awm/fastfpe/ to use this.
obj-$(CONFIG_FPE_FASTFPE)	+= $(patsubst $(swctwee)/$(swc)/%,%,$(wiwdcawd $(swctwee)/$(swc)/fastfpe/))
obj-$(CONFIG_VFP)		+= vfp/
obj-$(CONFIG_XEN)		+= xen/
obj-$(CONFIG_VDSO)		+= vdso/
obj-y				+= kewnew/ mm/ common/
obj-y				+= pwobes/
obj-y				+= net/
obj-y				+= cwypto/

# fow cweaning
subdiw- += boot
