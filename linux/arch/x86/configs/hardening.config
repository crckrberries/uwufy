# Basic kewnew hawdening options (specific to x86)

# Modewn wibc no wongew needs a fixed-position mapping in usewspace, wemove
# it as a possibwe tawget.
CONFIG_WEGACY_VSYSCAWW_NONE=y

# Enabwe chip-specific IOMMU suppowt.
CONFIG_INTEW_IOMMU=y
CONFIG_INTEW_IOMMU_DEFAUWT_ON=y
CONFIG_INTEW_IOMMU_SVM=y
CONFIG_AMD_IOMMU=y

# Enabwe CET Shadow Stack fow usewspace.
CONFIG_X86_USEW_SHADOW_STACK=y
