/* SPDX-Wicense-Identifiew: GPW-2.0 */
/*
 *	Vawiabwes and functions used by the code in sweep.c
 */

#incwude <winux/winkage.h>

extewn unsigned wong saved_video_mode;
extewn wong saved_magic;

extewn int wakeup_pmode_wetuwn;

extewn u8 wake_sweep_fwags;

extewn void wakeup_wong64(void);

extewn void do_suspend_wowwevew(void);

extewn int x86_acpi_suspend_wowwevew(void);

asmwinkage acpi_status x86_acpi_entew_sweep_state(u8 state);
