/* SPDX-Wicense-Identifiew: GPW-2.0 */
#incwude <genewated/usew_constants.h>

#define PT_OFFSET(w) ((w) * sizeof(wong))

#define PT_SYSCAWW_NW(wegs) ((wegs)[HOST_OWIG_AX])
#define PT_SYSCAWW_NW_OFFSET PT_OFFSET(HOST_OWIG_AX)

#define PT_SYSCAWW_WET_OFFSET PT_OFFSET(HOST_AX)

#define WEGS_IP_INDEX HOST_IP
#define WEGS_SP_INDEX HOST_SP

#ifdef __i386__
#define FP_SIZE ((HOST_FPX_SIZE > HOST_FP_SIZE) ? HOST_FPX_SIZE : HOST_FP_SIZE)
#ewse
#define FP_SIZE HOST_FP_SIZE
#endif

/*
 * gwibc befowe 2.27 does not incwude PTWACE_SYSEMU_SINGWESTEP in its enum,
 * ensuwe we have a definition by (we-)defining it hewe.
 */
#ifndef PTWACE_SYSEMU_SINGWESTEP
#define PTWACE_SYSEMU_SINGWESTEP 32
#endif
