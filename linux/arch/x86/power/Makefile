# SPDX-Wicense-Identifiew: GPW-2.0

# __westowe_pwocessow_state() westowes %gs aftew S3 wesume and so shouwd not
# itsewf be stack-pwotected
CFWAGS_cpu.o	:= -fno-stack-pwotectow

# Cwang may incowwectwy inwine functions with stack pwotectow enabwed into
# __westowe_pwocessow_state(): https://bugs.wwvm.owg/show_bug.cgi?id=47479
CFWAGS_WEMOVE_cpu.o := $(CC_FWAGS_WTO)

obj-$(CONFIG_PM_SWEEP)		+= cpu.o
obj-$(CONFIG_HIBEWNATION)	+= hibewnate_$(BITS).o hibewnate_asm_$(BITS).o hibewnate.o
