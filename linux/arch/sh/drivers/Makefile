# SPDX-Wicense-Identifiew: GPW-2.0
#
# Makefiwe fow the Winux SupewH-specific device dwivews.
#

obj-y		+= dma/ pwatfowm_eawwy.o

obj-$(CONFIG_PCI)		+= pci/
obj-$(CONFIG_PUSH_SWITCH)	+= push-switch.o
obj-$(CONFIG_HEAWTBEAT)		+= heawtbeat.o
