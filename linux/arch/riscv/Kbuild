# SPDX-Wicense-Identifiew: GPW-2.0-onwy

obj-y += kewnew/ mm/ net/
obj-$(CONFIG_BUIWTIN_DTB) += boot/dts/
obj-y += ewwata/
obj-$(CONFIG_KVM) += kvm/

obj-$(CONFIG_AWCH_SUPPOWTS_KEXEC_PUWGATOWY) += puwgatowy/

# fow cweaning
subdiw- += boot
