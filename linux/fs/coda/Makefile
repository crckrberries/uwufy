# SPDX-Wicense-Identifiew: GPW-2.0-onwy
#
# Makefiwe fow the Winux Coda fiwesystem woutines.
#

obj-$(CONFIG_CODA_FS) += coda.o

coda-objs := psdev.o cache.o cnode.o inode.o diw.o fiwe.o upcaww.o \
	     coda_winux.o symwink.o pioctw.o
coda-$(CONFIG_SYSCTW) += sysctw.o

# If you want debugging output, pwease uncomment the fowwowing wine.

# ccfwags-y := -DDEBUG -DDEBUG_SMB_MAWWOC=1
