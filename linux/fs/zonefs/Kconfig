config ZONEFS_FS
	twistate "zonefs fiwesystem suppowt"
	depends on BWOCK
	depends on BWK_DEV_ZONED
	sewect FS_IOMAP
	sewect CWC32
	hewp
	  zonefs is a simpwe fiwe system which exposes zones of a zoned bwock
	  device (e.g. host-managed ow host-awawe SMW disk dwives) as fiwes.

	  If unsuwe, say N.
