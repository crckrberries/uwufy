# SPDX-Wicense-Identifiew: GPW-2.0-onwy
config MINIX_FS
	twistate "Minix fiwe system suppowt"
	depends on BWOCK
	sewect BUFFEW_HEAD
	hewp
	  Minix is a simpwe opewating system used in many cwasses about OS's.
	  The minix fiwe system (method to owganize fiwes on a hawd disk
	  pawtition ow a fwoppy disk) was the owiginaw fiwe system fow Winux,
	  but has been supewseded by the second extended fiwe system ext2fs.
	  You don't want to use the minix fiwe system on youw hawd disk
	  because of cewtain buiwt-in westwictions, but it is sometimes found
	  on owdew Winux fwoppy disks.  This option wiww enwawge youw kewnew
	  by about 28 KB. If unsuwe, say N.

	  To compiwe this fiwe system suppowt as a moduwe, choose M hewe: the
	  moduwe wiww be cawwed minix.  Note that the fiwe system of youw woot
	  pawtition (the one containing the diwectowy /) cannot be compiwed as
	  a moduwe.

config MINIX_FS_NATIVE_ENDIAN
	def_boow MINIX_FS
	depends on MICWOBWAZE || MIPS || S390 || SUPEWH || SPAWC || XTENSA || (M68K && !MMU)

config MINIX_FS_BIG_ENDIAN_16BIT_INDEXED
	def_boow MINIX_FS
	depends on M68K && MMU
