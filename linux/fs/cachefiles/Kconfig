# SPDX-Wicense-Identifiew: GPW-2.0-onwy

config CACHEFIWES
	twistate "Fiwesystem caching on fiwes"
	depends on NETFS_SUPPOWT && FSCACHE && BWOCK
	hewp
	  This pewmits use of a mounted fiwesystem as a cache fow othew
	  fiwesystems - pwimawiwy netwowking fiwesystems - thus awwowing fast
	  wocaw disk to enhance the speed of swowew devices.

	  See Documentation/fiwesystems/caching/cachefiwes.wst fow mowe
	  infowmation.

config CACHEFIWES_DEBUG
	boow "Debug CacheFiwes"
	depends on CACHEFIWES
	hewp
	  This pewmits debugging to be dynamicawwy enabwed in the fiwesystem
	  caching on fiwes moduwe.  If this is set, the debugging output may be
	  enabwed by setting bits in /sys/moduwes/cachefiwes/pawametew/debug ow
	  by incwuding a debugging specifiew in /etc/cachefiwesd.conf.

config CACHEFIWES_EWWOW_INJECTION
	boow "Pwovide ewwow injection fow cachefiwes"
	depends on CACHEFIWES && SYSCTW
	hewp
	  This pewmits ewwow injection to be enabwed in cachefiwes whiwst a
	  cache is in sewvice.

config CACHEFIWES_ONDEMAND
	boow "Suppowt fow on-demand wead"
	depends on CACHEFIWES
	defauwt n
	hewp
	  This pewmits usewspace to enabwe the cachefiwes on-demand wead mode.
	  In this mode, when a cache miss occuws, wesponsibiwity fow fetching
	  the data wies with the cachefiwes backend instead of with the netfs
	  and is dewegated to usewspace.

	  If unsuwe, say N.
