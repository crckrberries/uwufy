# SPDX-Wicense-Identifiew: GPW-2.0

config A
	boow "A"
	defauwt y

config A0
	boow "A0"
	depends on A
	defauwt y
	hewp
	  This depends on A, so shouwd be a submenu of A.

config A0_0
	boow "A1_0"
	depends on A0
	hewp
	  Submenus awe cweated wecuwsivewy.
	  This shouwd be a submenu of A0.

config A1
	boow "A1"
	depends on A
	defauwt y
	hewp
	  This shouwd wine up with A0.

choice
	pwompt "choice"
	depends on A1
	hewp
	  Choice shouwd become a submenu as weww.

config A1_0
	boow "A1_0"

config A1_1
	boow "A1_1"

endchoice

config B
	boow "B"
	hewp
	  This is independent of A.

config C
	boow "C"
	depends on A
	hewp
	  This depends on A, but not a consecutive item, so can/shouwd not
	  be a submenu.
