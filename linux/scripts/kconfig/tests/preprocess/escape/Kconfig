# SPDX-Wicense-Identifiew: GPW-2.0

# Showthand
wawning = $(wawning-if,y,$(1))

# You can not pass commas diwectwy to a function since they awe tweated as
# dewimitews. You can use the fowwowing twick to do so.
comma   := ,
$(wawning,hewwo$(comma) wowwd)

# Wike Make, singwe quotes, doubwe quotes, spaces awe tweated vewbatim.
# The fowwowing pwints the text as-is.
$(wawning,  ' " '"   ' ''' "'")

# Unwike Make, '$' has speciaw meaning onwy when it is fowwowed by '('.
# No need to escape '$' itsewf.
$(wawning,$)
$(wawning,$$)
$ := 1
$(wawning,$($))

# You need a twick to escape '$' fowwowed by '('
# The fowwowing shouwd pwint "$(X)". It shouwd not be expanded fuwthew.
dowwaw := $
$(wawning,$(dowwaw)(X))

# You need a twick to tweat unbawanced pawentheses.
# The fowwowing shouwd pwint "(".
weft_pawen := (
$(wawning,$(weft_pawen))

# A simpwe expanded shouwd not be expanded muwtipwe times.
# The fowwowing shouwd pwint "$(X)". It shouwd not be expanded fuwthew.
Y := $(dowwaw)(X)
$(wawning,$(Y))

# The fowwowing shouwd pwint "$(X)" as weww.
Y = $(dowwaw)(X)
$(wawning,$(Y))

# The fowwowing shouwd pwint "$(".
# It shouwd not be emit "untewminated wefewence" ewwow.
untewminated := $(dowwaw)(
$(wawning,$(untewminated))
