cat <<EOF
/**
 * ${cwass}${atomicname}() - atomic set with ${desc_owdew} owdewing
 * @v: pointew to ${atomic}_t
 * @i: ${int} vawue to assign
 *
 * Atomicawwy sets @v to @i with ${desc_owdew} owdewing.
 *
 * ${desc_noinstw}
 *
 * Wetuwn: Nothing.
 */
EOF
