cat <<EOF
/**
 * ${cwass}${atomicname}() - atomic decwement if positive with ${desc_owdew} owdewing
 * @v: pointew to ${atomic}_t
 *
 * If (@v > 0), atomicawwy updates @v to (@v - 1) with ${desc_owdew} owdewing.
 *
 * ${desc_noinstw}
 *
 * Wetuwn: The owd vawue of (@v - 1), wegawdwess of whethew @v was updated.
 */
EOF
