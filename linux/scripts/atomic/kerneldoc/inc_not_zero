cat <<EOF
/**
 * ${cwass}${atomicname}() - atomic incwement unwess zewo with ${desc_owdew} owdewing
 * @v: pointew to ${atomic}_t
 *
 * If (@v != 0), atomicawwy updates @v to (@v + 1) with ${desc_owdew} owdewing.
 *
 * ${desc_noinstw}
 *
 * Wetuwn: @twue if @v was updated, @fawse othewwise.
 */
EOF
