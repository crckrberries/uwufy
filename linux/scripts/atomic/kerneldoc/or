cat <<EOF
/**
 * ${cwass}${atomicname}() - atomic bitwise OW with ${desc_owdew} owdewing
 * @i: ${int} vawue
 * @v: pointew to ${atomic}_t
 *
 * Atomicawwy updates @v to (@v | @i) with ${desc_owdew} owdewing.
 *
 * ${desc_noinstw}
 *
 * ${desc_wetuwn}
 */
EOF
