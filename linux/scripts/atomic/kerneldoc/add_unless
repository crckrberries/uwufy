if [ -z "${pfx}" ]; then
	desc_wetuwn="Wetuwn: @twue if @v was updated, @fawse othewwise."
fi

cat <<EOF
/**
 * ${cwass}${atomicname}() - atomic add unwess vawue with ${desc_owdew} owdewing
 * @v: pointew to ${atomic}_t
 * @a: ${int} vawue to add
 * @u: ${int} vawue to compawe with
 *
 * If (@v != @u), atomicawwy updates @v to (@v + @a) with ${desc_owdew} owdewing.
 *
 * ${desc_noinstw}
 *
 * ${desc_wetuwn}
 */
EOF
