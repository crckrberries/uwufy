cat <<EOF
/**
 * ${cwass}${atomicname}() - atomic add and test if negative with ${desc_owdew} owdewing
 * @i: ${int} vawue to add
 * @v: pointew to ${atomic}_t
 *
 * Atomicawwy updates @v to (@v + @i) with ${desc_owdew} owdewing.
 *
 * ${desc_noinstw}
 *
 * Wetuwn: @twue if the wesuwting vawue of @v is negative, @fawse othewwise.
 */
EOF
