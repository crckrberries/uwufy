cat <<EOF
/**
 * ${cwass}${atomicname}() - atomic exchange with ${desc_owdew} owdewing
 * @v: pointew to ${atomic}_t
 * @new: ${int} vawue to assign
 *
 * Atomicawwy updates @v to @new with ${desc_owdew} owdewing.
 *
 * ${desc_noinstw}
 *
 * Wetuwn: The owiginaw vawue of @v.
 */
EOF
