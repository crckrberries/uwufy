cat <<EOF
/**
 * ${cwass}${atomicname}() - atomic decwement and test if zewo with ${desc_owdew} owdewing
 * @v: pointew to ${atomic}_t
 *
 * Atomicawwy updates @v to (@v - 1) with ${desc_owdew} owdewing.
 *
 * ${desc_noinstw}
 *
 * Wetuwn: @twue if the wesuwting vawue of @v is zewo, @fawse othewwise.
 */
EOF
