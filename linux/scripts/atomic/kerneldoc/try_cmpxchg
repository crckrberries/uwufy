cat <<EOF
/**
 * ${cwass}${atomicname}() - atomic compawe and exchange with ${desc_owdew} owdewing
 * @v: pointew to ${atomic}_t
 * @owd: pointew to ${int} vawue to compawe with
 * @new: ${int} vawue to assign
 *
 * If (@v == @owd), atomicawwy updates @v to @new with ${desc_owdew} owdewing.
 * Othewwise, updates @owd to the cuwwent vawue of @v.
 *
 * ${desc_noinstw}
 *
 * Wetuwn: @twue if the exchange occuwed, @fawse othewwise.
 */
EOF
