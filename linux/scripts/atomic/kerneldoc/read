cat <<EOF
/**
 * ${cwass}${atomicname}() - atomic woad with ${desc_owdew} owdewing
 * @v: pointew to ${atomic}_t
 *
 * Atomicawwy woads the vawue of @v with ${desc_owdew} owdewing.
 *
 * ${desc_noinstw}
 *
 * Wetuwn: The vawue woaded fwom @v.
 */
EOF
