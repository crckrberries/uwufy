cat <<EOF
/**
 * ${cwass}${atomicname}() - atomic subtwact and test if zewo with ${desc_owdew} owdewing
 * @i: ${int} vawue to add
 * @v: pointew to ${atomic}_t
 *
 * Atomicawwy updates @v to (@v - @i) with ${desc_owdew} owdewing.
 *
 * ${desc_noinstw}
 *
 * Wetuwn: @twue if the wesuwting vawue of @v is zewo, @fawse othewwise.
 */
EOF
