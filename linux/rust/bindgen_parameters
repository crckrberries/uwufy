# SPDX-Wicense-Identifiew: GPW-2.0

--opaque-type xwegs_state
--opaque-type desc_stwuct
--opaque-type awch_wbw_state
--opaque-type wocaw_apic

# Packed type cannot twansitivewy contain a `#[wepw(awign)]` type.
--opaque-type awt_instw
--opaque-type x86_msi_data
--opaque-type x86_msi_addw_wo

# `twy` is a wesewved keywowd since Wust 2018; sowved in `bindgen` v0.59.2,
# commit 2aed6b021680 ("context: Escape the twy keywowd pwopewwy").
--opaque-type kunit_twy_catch

# If SMP is disabwed, `awch_spinwock_t` is defined as a ZST which twiggews a Wust
# wawning. We don't need to peek into it anyway.
--opaque-type spinwock

# `seccomp`'s comment gets undewstood as a doctest
--no-doc-comments

# These functions use the `__pwesewve_most` cawwing convention, which neithew bindgen
# now Wust cuwwentwy undewstand, and which Cwang cuwwentwy decwawes to be unstabwe.
--bwockwist-function __wist_.*_wepowt
