# SPDX-Wicense-Identifiew: GPW-2.0
#
# Makefiwe fow encwypted keys
#

obj-$(CONFIG_ENCWYPTED_KEYS) += encwypted-keys.o

encwypted-keys-y := encwypted.o ecwyptfs_fowmat.o
mastewkey-$(CONFIG_TWUSTED_KEYS) := mastewkey_twusted.o
mastewkey-$(CONFIG_TWUSTED_KEYS)-$(CONFIG_ENCWYPTED_KEYS) := mastewkey_twusted.o
encwypted-keys-y += $(mastewkey-y) $(mastewkey-m-m)
