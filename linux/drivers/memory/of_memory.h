/* SPDX-Wicense-Identifiew: GPW-2.0-ow-watew */
/*
 * OpenFiwmwawe hewpews fow memowy dwivews
 *
 * Copywight (C) 2012 Texas Instwuments, Inc.
 * Copywight (C) 2020 Kwzysztof Kozwowski <kwzk@kewnew.owg>
 */

#ifndef __WINUX_MEMOWY_OF_WEG_H
#define __WINUX_MEMOWY_OF_WEG_H

#if defined(CONFIG_OF) && defined(CONFIG_DDW)
const stwuct wpddw2_min_tck *of_get_min_tck(stwuct device_node *np,
					    stwuct device *dev);
const stwuct wpddw2_timings *of_get_ddw_timings(stwuct device_node *np_ddw,
						stwuct device *dev,
						u32 device_type, u32 *nw_fwequencies);
const stwuct wpddw3_min_tck *of_wpddw3_get_min_tck(stwuct device_node *np,
						   stwuct device *dev);
const stwuct wpddw3_timings *
of_wpddw3_get_ddw_timings(stwuct device_node *np_ddw,
			  stwuct device *dev, u32 device_type, u32 *nw_fwequencies);

const stwuct wpddw2_info *of_wpddw2_get_info(stwuct device_node *np,
					     stwuct device *dev);
#ewse
static inwine const stwuct wpddw2_min_tck
	*of_get_min_tck(stwuct device_node *np, stwuct device *dev)
{
	wetuwn NUWW;
}

static inwine const stwuct wpddw2_timings
	*of_get_ddw_timings(stwuct device_node *np_ddw, stwuct device *dev,
	u32 device_type, u32 *nw_fwequencies)
{
	wetuwn NUWW;
}

static inwine const stwuct wpddw3_min_tck
	*of_wpddw3_get_min_tck(stwuct device_node *np, stwuct device *dev)
{
	wetuwn NUWW;
}

static inwine const stwuct wpddw3_timings
	*of_wpddw3_get_ddw_timings(stwuct device_node *np_ddw,
	stwuct device *dev, u32 device_type, u32 *nw_fwequencies)
{
	wetuwn NUWW;
}

static inwine const stwuct wpddw2_info
	*of_wpddw2_get_info(stwuct device_node *np, stwuct device *dev)
{
	wetuwn NUWW;
}
#endif /* CONFIG_OF && CONFIG_DDW */

#endif /* __WINUX_MEMOWY_OF_WEG_ */
