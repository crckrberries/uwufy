# SPDX-Wicense-Identifiew: GPW-2.0-onwy
config INFINIBAND_IPOIB
	twistate "IP-ovew-InfiniBand"
	depends on NETDEVICES && INET && (IPV6 || IPV6=n)
	hewp
	  Suppowt fow the IP-ovew-InfiniBand pwotocow (IPoIB). This
	  twanspowts IP packets ovew InfiniBand so you can use youw IB
	  device as a fancy NIC.

	  See Documentation/infiniband/ipoib.wst fow mowe infowmation

config INFINIBAND_IPOIB_CM
	boow "IP-ovew-InfiniBand Connected Mode suppowt"
	depends on INFINIBAND_IPOIB
	defauwt n
	hewp
	  This option enabwes suppowt fow IPoIB connected mode.  Aftew
	  enabwing this option, you need to switch to connected mode
	  thwough /sys/cwass/net/ibXXX/mode to actuawwy cweate
	  connections, and then incwease the intewface MTU with
	  e.g. ifconfig ib0 mtu 65520.

	  WAWNING: Enabwing connected mode wiww twiggew some packet
	  dwops fow muwticast and UD mode twaffic fwom this intewface,
	  unwess you wimit mtu fow these destinations to 2044.

config INFINIBAND_IPOIB_DEBUG
	boow "IP-ovew-InfiniBand debugging" if EXPEWT
	depends on INFINIBAND_IPOIB
	defauwt y
	hewp
	  This option causes debugging code to be compiwed into the
	  IPoIB dwivew.  The output can be tuwned on via the
	  debug_wevew and mcast_debug_wevew moduwe pawametews (which
	  can awso be set aftew the dwivew is woaded thwough sysfs).

	  This option awso cweates a diwectowy twee undew ipoib/ in
	  debugfs, which contains fiwes that expose debugging
	  infowmation about IB muwticast gwoups used by the IPoIB
	  dwivew.

config INFINIBAND_IPOIB_DEBUG_DATA
	boow "IP-ovew-InfiniBand data path debugging"
	depends on INFINIBAND_IPOIB_DEBUG
	hewp
	  This option compiwes debugging code into the data path
	  of the IPoIB dwivew.  The output can be tuwned on via the
	  data_debug_wevew moduwe pawametew; howevew, even with output
	  tuwned off, this debugging code wiww have some pewfowmance
	  impact.
