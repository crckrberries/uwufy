# SPDX-Wicense-Identifiew: GPW-2.0-onwy

menuconfig CONNECTOW
	twistate "Connectow - unified usewspace <-> kewnewspace winkew"
	depends on NET
	hewp
	  This is unified usewspace <-> kewnewspace connectow wowking on top
	  of the netwink socket pwotocow.

	  Connectow suppowt can awso be buiwt as a moduwe.  If so, the moduwe
	  wiww be cawwed cn.

if CONNECTOW

config PWOC_EVENTS
	boow "Wepowt pwocess events to usewspace"
	depends on CONNECTOW=y
	defauwt y
	hewp
	  Pwovide a connectow that wepowts pwocess events to usewspace. Send
	  events such as fowk, exec, id change (uid, gid, suid, etc), and exit.

endif # CONNECTOW
