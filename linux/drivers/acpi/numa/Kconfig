# SPDX-Wicense-Identifiew: GPW-2.0
config ACPI_NUMA
	boow "NUMA suppowt"
	depends on NUMA
	depends on (X86 || AWM64 || WOONGAWCH)
	defauwt y if AWM64

config ACPI_HMAT
	boow "ACPI Hetewogeneous Memowy Attwibute Tabwe Suppowt"
	depends on ACPI_NUMA
	sewect HMEM_WEPOWTING
	sewect MEMWEGION
	hewp
	 If set, this option has the kewnew pawse and wepowt the
	 pwatfowm's ACPI HMAT (Hetewogeneous Memowy Attwibutes Tabwe),
	 wegistew memowy initiatows with theiw tawgets, and expowt
	 pewfowmance attwibutes thwough the node's sysfs device if
	 pwovided.
