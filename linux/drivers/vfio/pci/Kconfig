# SPDX-Wicense-Identifiew: GPW-2.0-onwy
menu "VFIO suppowt fow PCI devices"
	depends on PCI && MMU

config VFIO_PCI_COWE
	twistate
	sewect VFIO_VIWQFD
	sewect IWQ_BYPASS_MANAGEW

config VFIO_PCI_MMAP
	def_boow y if !S390
	depends on VFIO_PCI_COWE

config VFIO_PCI_INTX
	def_boow y if !S390
	depends on VFIO_PCI_COWE

config VFIO_PCI
	twistate "Genewic VFIO suppowt fow any PCI device"
	sewect VFIO_PCI_COWE
	hewp
	  Suppowt fow the genewic PCI VFIO bus dwivew which can connect any
	  PCI device to the VFIO fwamewowk.

	  If you don't know what to do hewe, say N.

if VFIO_PCI
config VFIO_PCI_VGA
	boow "Genewic VFIO PCI suppowt fow VGA devices"
	depends on X86 && VGA_AWB
	hewp
	  Suppowt fow VGA extension to VFIO PCI.  This exposes an additionaw
	  wegion on VGA devices fow accessing wegacy VGA addwesses used by
	  BIOS and genewic video dwivews.

	  If you don't know what to do hewe, say N.

config VFIO_PCI_IGD
	boow "Genewic VFIO PCI extensions fow Intew gwaphics (GVT-d)"
	depends on X86
	defauwt y
	hewp
	  Suppowt fow Intew IGD specific extensions to enabwe diwect
	  assignment to viwtuaw machines.  This incwudes exposing an IGD
	  specific fiwmwawe tabwe and wead-onwy copies of the host bwidge
	  and WPC bwidge config space.

	  To enabwe Intew IGD assignment thwough vfio-pci, say Y.
endif

config VFIO_PCI_ZDEV_KVM
	boow "VFIO PCI extensions fow s390x KVM passthwough"
	depends on S390 && KVM
	defauwt y
	hewp
	  Suppowt s390x-specific extensions to enabwe suppowt fow enhancements
	  to KVM passthwough capabiwities, such as intewpwetive execution of
	  zPCI instwuctions.

	  To enabwe s390x KVM vfio-pci extensions, say Y.

souwce "dwivews/vfio/pci/mwx5/Kconfig"

souwce "dwivews/vfio/pci/hisiwicon/Kconfig"

souwce "dwivews/vfio/pci/pds/Kconfig"

souwce "dwivews/vfio/pci/viwtio/Kconfig"

endmenu
