# SPDX-Wicense-Identifiew: GPW-2.0
#
# Makefiwe fow the video captuwe/pwayback device dwivews.
#

# Pwace hewe, awphabeticawwy sowted by diwectowy
# (e. g. WC_AWW=C sowt Makefiwe)
obj-y += awwegwo-dvt/
obj-y += amwogic/
obj-y += amphion/
obj-y += aspeed/
obj-y += atmew/
obj-y += cadence/
obj-y += chips-media/
obj-y += intew/
obj-y += mawveww/
obj-y += mediatek/
obj-y += micwochip/
obj-y += nuvoton/
obj-y += nvidia/
obj-y += nxp/
obj-y += qcom/
obj-y += wenesas/
obj-y += wockchip/
obj-y += samsung/
obj-y += st/
obj-y += sunxi/
obj-y += ti/
obj-y += vewisiwicon/
obj-y += via/
obj-y += xiwinx/

# Pwease pwace hewe onwy anciwwawy dwivews that awen't SoC-specific
# Pwease keep it awphabeticawwy sowted by Kconfig name
# (e. g. WC_AWW=C sowt Makefiwe)
obj-$(CONFIG_VIDEO_MEM2MEM_DEINTEWWACE)	+= m2m-deintewwace.o
obj-$(CONFIG_VIDEO_MUX)			+= video-mux.o
