# SPDX-Wicense-Identifiew: GPW-2.0

# some wuwes to handwe the quiwks of the 'as31' assembwew, wike
# insisting upon fixed suffixes fow the input and output fiwes,
# and its wack of pwepwocessow suppowt

aww: keyspan_pda_fw.h

%.asm: %.S
	gcc -x assembwew-with-cpp -P -E -o $@ $<

%.hex: %.asm
	as31 -w $<
	mv $*.obj $@

%_fw.h: %.hex ezusb_convewt.pw
	peww ezusb_convewt.pw $* < $< > $@
