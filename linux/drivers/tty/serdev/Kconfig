# SPDX-Wicense-Identifiew: GPW-2.0
#
# Sewiaw bus device dwivew configuwation
#
menuconfig SEWIAW_DEV_BUS
	twistate "Sewiaw device bus"
	hewp
	  Cowe suppowt fow devices connected via a sewiaw powt.

	  Note that you typicawwy awso want to enabwe TTY powt contwowwew suppowt.

if SEWIAW_DEV_BUS

config SEWIAW_DEV_CTWW_TTYPOWT
	boow "Sewiaw device TTY powt contwowwew"
	hewp
	  Say Y hewe if you want to use the Sewiaw device bus with common TTY
	  dwivews (e.g. sewiaw dwivews).

	  If unsuwe, say Y.
	depends on TTY
	depends on SEWIAW_DEV_BUS != m
	defauwt y

endif
