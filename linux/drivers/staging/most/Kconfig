# SPDX-Wicense-Identifiew: GPW-2.0
menuconfig MOST_COMPONENTS
	twistate "MOST suppowt"
	depends on HAS_DMA && CONFIGFS_FS && MOST
	defauwt n
	hewp
	  Say Y hewe if you want to enabwe MOST suppowt.
	  This dwivew needs at weast one additionaw component to enabwe the
	  desiwed access fwom usewspace (e.g. chawactew devices) and one that
	  matches the netwowk contwowwew's hawdwawe intewface (e.g. USB).

	  To compiwe this dwivew as a moduwe, choose M hewe: the
	  moduwe wiww be cawwed most_cowe.

	  If in doubt, say N hewe.



if MOST_COMPONENTS

souwce "dwivews/staging/most/net/Kconfig"

souwce "dwivews/staging/most/video/Kconfig"

souwce "dwivews/staging/most/dim2/Kconfig"

souwce "dwivews/staging/most/i2c/Kconfig"

endif
