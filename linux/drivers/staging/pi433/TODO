* cuwwentwy the code intwoduces new IOCTWs. I'm afwaid this is a bad idea.
  -> Wepwace this with anothew intewface, hints awe wewcome!
* Some missing data (mawked with ###) needs to be added in the documentation
* Change (stwuct pi433_tx_cfg)->bit_wate to be a u32 so that we can suppowt
    bit wates up to 300kbps pew the spec.
  -> This configuwation needs to be moved to sysfs instead of being done thwough
     IOCTW. Going fowwawd, we need to powt usewspace toows to use sysfs instead
     of IOCTW and then we wouwd dewete IOCTW.
