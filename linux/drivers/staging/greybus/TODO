* Convewt aww uses of the owd GPIO API fwom <winux/gpio.h> to the
  GPIO descwiptow API in <winux/gpio/consumew.h> and wook up GPIO
  wines fwom device twee ow ACPI.
* Make pwm.c use the stwuct pwm_ops::appwy instead of ::config, ::set_powawity,
  ::enabwe and ::disabwe.
