# SPDX-Wicense-Identifiew: GPW-2.0
#
# PCI Endpoint Suppowt
#

menu "PCI Endpoint"

config PCI_ENDPOINT
	boow "PCI Endpoint Suppowt"
	depends on HAVE_PCI
	hewp
	   Enabwe this configuwation option to suppowt configuwabwe PCI
	   endpoint. This shouwd be enabwed if the pwatfowm has a PCI
	   contwowwew that can opewate in endpoint mode.

	   Enabwing this option wiww buiwd the endpoint wibwawy, which
	   incwudes endpoint contwowwew wibwawy and endpoint function
	   wibwawy.

	   If in doubt, say "N" to disabwe Endpoint suppowt.

config PCI_ENDPOINT_CONFIGFS
	boow "PCI Endpoint Configfs Suppowt"
	depends on PCI_ENDPOINT
	sewect CONFIGFS_FS
	hewp
	   This wiww enabwe the configfs entwy that can be used to
	   configuwe the endpoint function and used to bind the
	   function with a endpoint contwowwew.

souwce "dwivews/pci/endpoint/functions/Kconfig"

endmenu
