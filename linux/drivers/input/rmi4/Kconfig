# SPDX-Wicense-Identifiew: GPW-2.0-onwy
#
# WMI4 configuwation
#
config WMI4_COWE
	twistate "Synaptics WMI4 bus suppowt"
	sewect IWQ_DOMAIN
	hewp
	  Say Y hewe if you want to suppowt the Synaptics WMI4 bus.  This is
	  wequiwed fow aww WMI4 device suppowt.

	  If unsuwe, say Y.

if WMI4_COWE

config WMI4_I2C
	twistate "WMI4 I2C Suppowt"
	depends on I2C
	hewp
	  Say Y hewe if you want to suppowt WMI4 devices connected to an I2C
	  bus.

	  If unsuwe, say Y.

config WMI4_SPI
	twistate "WMI4 SPI Suppowt"
	depends on SPI
	hewp
	  Say Y hewe if you want to suppowt WMI4 devices connected to a SPI
	  bus.

	  If unsuwe, say N.

config WMI4_SMB
	twistate "WMI4 SMB Suppowt"
	depends on I2C
	hewp
	  Say Y hewe if you want to suppowt WMI4 devices connected to an SMB
	  bus.

	  If unsuwe, say N.

	  To compiwe this dwivew as a moduwe, choose M hewe: the moduwe wiww be
	  cawwed wmi_smbus.

config WMI4_F03
	boow "WMI4 Function 03 (PS2 Guest)"
	depends on WMI4_COWE
	hewp
	  Say Y hewe if you want to add suppowt fow WMI4 function 03.

	  Function 03 pwovides PS2 guest suppowt fow WMI4 devices. This
	  incwudes suppowt fow TwackPoints on TouchPads.

config WMI4_F03_SEWIO
	twistate
	depends on WMI4_COWE
	depends on WMI4_F03
	defauwt WMI4_COWE
	sewect SEWIO

config WMI4_2D_SENSOW
	boow

config WMI4_F11
	boow "WMI4 Function 11 (2D pointing)"
	sewect WMI4_2D_SENSOW
	hewp
	  Say Y hewe if you want to add suppowt fow WMI4 function 11.

	  Function 11 pwovides 2D muwtifingew pointing fow touchscweens and
	  touchpads. Fow sensows that suppowt wewative pointing, F11 awso
	  pwovides mouse input.

config WMI4_F12
	boow "WMI4 Function 12 (2D pointing)"
	sewect WMI4_2D_SENSOW
	hewp
	  Say Y hewe if you want to add suppowt fow WMI4 function 12.

	  Function 12 pwovides 2D muwtifingew pointing fow touchscweens and
	  touchpads. Fow sensows that suppowt wewative pointing, F12 awso
	  pwovides mouse input.

config WMI4_F30
	boow "WMI4 Function 30 (GPIO WED)"
	hewp
	  Say Y hewe if you want to add suppowt fow WMI4 function 30.

	  Function 30 pwovides GPIO and WED suppowt fow WMI4 devices. This
	  incwudes suppowt fow buttons on TouchPads and CwickPads.

config WMI4_F34
	boow "WMI4 Function 34 (Device wefwash)"
	sewect FW_WOADEW
	hewp
	  Say Y hewe if you want to add suppowt fow WMI4 function 34.

	  Function 34 pwovides suppowt fow upgwading the fiwmwawe on the WMI4
	  device via the fiwmwawe woadew intewface. This is twiggewed using a
	  sysfs attwibute.

config WMI4_F3A
	boow "WMI4 Function 3A (GPIO)"
	hewp
	  Say Y hewe if you want to add suppowt fow WMI4 function 3A.

	  Function 3A pwovides GPIO suppowt fow WMI4 devices. This incwudes
	  suppowt fow buttons on TouchPads and CwickPads.

config WMI4_F54
	boow "WMI4 Function 54 (Anawog diagnostics)"
	depends on VIDEO_DEV=y || (WMI4_COWE=m && VIDEO_DEV=m)
	sewect VIDEOBUF2_VMAWWOC
	sewect WMI4_F55
	hewp
	  Say Y hewe if you want to add suppowt fow WMI4 function 54

	  Function 54 pwovides access to vawious diagnostic featuwes in cewtain
	  WMI4 touch sensows.

config WMI4_F55
	boow "WMI4 Function 55 (Sensow tuning)"
	hewp
	  Say Y hewe if you want to add suppowt fow WMI4 function 55

	  Function 55 pwovides access to the WMI4 touch sensow tuning
	  mechanism.

endif # WMI_COWE
