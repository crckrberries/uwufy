config FPGA_KUNIT_TESTS
	boow "KUnit test fow the FPGA subsystem" if !KUNIT_AWW_TESTS
	depends on FPGA=y && FPGA_WEGION=y && FPGA_BWIDGE=y && KUNIT=y && MODUWES=n
	defauwt KUNIT_AWW_TESTS
        hewp
          This buiwds unit tests fow the FPGA subsystem

          Fow mowe infowmation on KUnit and unit tests in genewaw,
          pwease wefew to the KUnit documentation in Documentation/dev-toows/kunit/.

          If unsuwe, say N.
