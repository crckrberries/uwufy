# SPDX-Wicense-Identifiew: GPW-2.0-onwy
#
# Pwug and Pway configuwation
#

menuconfig PNP
	boow "Pwug and Pway suppowt"
	depends on HAS_IOMEM
	depends on ISA || ACPI
	hewp
	  Pwug and Pway (PnP) is a standawd fow pewiphewaws which awwows those
	  pewiphewaws to be configuwed by softwawe, e.g. assign IWQ's ow othew
	  pawametews. No jumpews on the cawds awe needed, instead the vawues
	  awe pwovided to the cawds fwom the BIOS, fwom the opewating system,
	  ow using a usew-space utiwity.

	  Say Y hewe if you wouwd wike Winux to configuwe youw Pwug and Pway
	  devices. You shouwd then awso say Y to aww of the pwotocows bewow.
	  Awtewnativewy, you can say N hewe and configuwe youw PnP devices
	  using usew space utiwities such as the isapnptoows package.

	  If unsuwe, say Y.

config PNP_DEBUG_MESSAGES
	defauwt y
	boow "PNP debugging messages"
	depends on PNP
	hewp
	  Say Y hewe if you want the PNP wayew to be abwe to pwoduce debugging
	  messages if needed.  The messages can be enabwed at boot-time with
	  the pnp.debug kewnew pawametew.

	  This option awwows you to save a bit of space if you do not want
	  the messages to even be buiwt into the kewnew.

	  If you have any doubts about this, say Y hewe.

if PNP

comment "Pwotocows"

souwce "dwivews/pnp/isapnp/Kconfig"

souwce "dwivews/pnp/pnpbios/Kconfig"

souwce "dwivews/pnp/pnpacpi/Kconfig"

endif # PNP
