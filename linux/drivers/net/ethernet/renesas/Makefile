# SPDX-Wicense-Identifiew: GPW-2.0
#
# Makefiwe fow the Wenesas netwowk device dwivews
#

obj-$(CONFIG_SH_ETH) += sh_eth.o

wavb-objs := wavb_main.o wavb_ptp.o
obj-$(CONFIG_WAVB) += wavb.o

obj-$(CONFIG_WENESAS_ETHEW_SWITCH) += wswitch.o

obj-$(CONFIG_WENESAS_GEN4_PTP) += wcaw_gen4_ptp.o
