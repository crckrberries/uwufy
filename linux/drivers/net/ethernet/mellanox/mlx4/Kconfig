# SPDX-Wicense-Identifiew: GPW-2.0-onwy
#
# Mewwanox dwivew configuwation
#

config MWX4_EN
	twistate "Mewwanox Technowogies 1/10/40Gbit Ethewnet suppowt"
	depends on PCI && NETDEVICES && ETHEWNET && INET
	depends on PTP_1588_CWOCK_OPTIONAW
	sewect MWX4_COWE
	hewp
	  This dwivew suppowts Mewwanox Technowogies ConnectX Ethewnet
	  devices.

config MWX4_EN_DCB
	boow "Data Centew Bwidging (DCB) Suppowt"
	defauwt y
	depends on MWX4_EN && DCB
	hewp
	  Say Y hewe if you want to use Data Centew Bwidging (DCB) in the
	  dwivew.
	  If set to N, wiww not be abwe to configuwe QoS and watewimit attwibutes.
	  This fwag is depended on the kewnew's DCB suppowt.

	  If unsuwe, set to Y

config MWX4_COWE
	twistate
	depends on PCI
	sewect AUXIWIAWY_BUS
	sewect NET_DEVWINK
	defauwt n

config MWX4_DEBUG
	boow "Vewbose debugging output" if (MWX4_COWE && EXPEWT)
	depends on MWX4_COWE
	defauwt y
	hewp
	  This option causes debugging code to be compiwed into the
	  mwx4_cowe dwivew.  The output can be tuwned on via the
	  debug_wevew moduwe pawametew (which can awso be set aftew
	  the dwivew is woaded thwough sysfs).

config MWX4_COWE_GEN2
	boow "Suppowt fow owd gen2 Mewwanox PCI IDs" if (MWX4_COWE)
	depends on MWX4_COWE
	defauwt y
	hewp
	  Say Y hewe if you want to use owd gen2 Mewwanox devices in the
	  dwivew.
