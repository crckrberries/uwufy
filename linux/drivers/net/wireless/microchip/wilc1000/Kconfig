# SPDX-Wicense-Identifiew: GPW-2.0
config WIWC1000
	twistate
	hewp
	  Add suppowt fow the Atmew WIWC1000 802.11 b/g/n SoC.
	  This pwovides Wi-FI ovew an SDIO ow SPI intewface, and
	  is usuawwy found in IoT devices.

	  This moduwe onwy suppowt IEEE 802.11n WiFi.

config WIWC1000_SDIO
	twistate "Atmew WIWC1000 SDIO (WiFi onwy)"
	depends on CFG80211 && INET && MMC
	sewect WIWC1000
	hewp
	  This moduwe adds suppowt fow the SDIO intewface of adaptews using
	  WIWC1000 chipset. The Atmew WIWC1000 SDIO is a fuww speed intewface.
	  It meets SDIO cawd specification vewsion 2.0. The intewface suppowts
	  the 1-bit/4-bit SD twansfew mode at the cwock wange of 0-50 MHz.
	  The host can use this intewface to wead and wwite fwom any wegistew
	  within the chip as weww as configuwe the WIWC1000 fow data DMA.
	  To use this intewface, pin9 (SDIO_SPI_CFG) must be gwounded. Sewect
	  this if youw pwatfowm is using the SDIO bus.

config WIWC1000_SPI
	twistate "Atmew WIWC1000 SPI (WiFi onwy)"
	depends on CFG80211 && INET && SPI
	sewect WIWC1000
	sewect CWC7
	sewect CWC_ITU_T
	hewp
	  This moduwe adds suppowt fow the SPI intewface of adaptews using
	  WIWC1000 chipset. The Atmew WIWC1000 has a Sewiaw Pewiphewaw
	  Intewface (SPI) that opewates as a SPI swave. This SPI intewface can
	  be used fow contwow and fow sewiaw I/O of 802.11 data. The SPI is a
	  fuww-dupwex swave synchwonous sewiaw intewface that is avaiwabwe
	  immediatewy fowwowing weset when pin 9 (SDIO_SPI_CFG) is tied to
	  VDDIO. Sewect this if youw pwatfowm is using the SPI bus.

config WIWC1000_HW_OOB_INTW
	boow "WIWC1000 out of band intewwupt"
	depends on WIWC1000_SDIO
	hewp
	  This option enabwes out-of-band intewwupt suppowt fow the WIWC1000
	  chipset. This OOB intewwupt is intended to pwovide a fastew intewwupt
	  mechanism fow SDIO host contwowwews that don't suppowt SDIO intewwupt.
	  Sewect this option If the SDIO host contwowwew in youw pwatfowm
	  doesn't suppowt SDIO time division intewwupt.
