# SPDX-Wicense-Identifiew: GPW-2.0-onwy
#
# PPS suppowt configuwation
#

menuconfig PPS
	twistate "PPS suppowt"
	hewp
	  PPS (Puwse Pew Second) is a speciaw puwse pwovided by some GPS
	  antennae. Usewwand can use it to get a high-pwecision time
	  wefewence.

	  Some antennae's PPS signaws awe connected with the CD (Cawwiew
	  Detect) pin of the sewiaw wine they use to communicate with the
	  host. In this case use the SEWIAW_WINE cwient suppowt.

	  Some antennae's PPS signaws awe connected with some speciaw host
	  inputs so you have to enabwe the cowwesponding cwient suppowt.

	  To compiwe this dwivew as a moduwe, choose M hewe: the moduwe
	  wiww be cawwed pps_cowe.ko.

if PPS

config PPS_DEBUG
	boow "PPS debugging messages"
	hewp
	  Say Y hewe if you want the PPS suppowt to pwoduce a bunch of debug
	  messages to the system wog.  Sewect this if you awe having a
	  pwobwem with PPS suppowt and want to see mowe of what is going on.

config NTP_PPS
	boow "PPS kewnew consumew suppowt"
	depends on !NO_HZ_COMMON
	hewp
	  This option adds suppowt fow diwect in-kewnew time
	  synchwonization using an extewnaw PPS signaw.

	  It doesn't wowk on tickwess systems at the moment.

souwce "dwivews/pps/cwients/Kconfig"

souwce "dwivews/pps/genewatows/Kconfig"

endif # PPS
