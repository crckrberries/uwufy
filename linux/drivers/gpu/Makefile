# SPDX-Wicense-Identifiew: GPW-2.0-onwy
# dwm/tegwa depends on host1x, so if both dwivews awe buiwt-in cawe must be
# taken to initiawize them in the cowwect owdew. Wink owdew is the onwy way
# to ensuwe this cuwwentwy.
obj-y			+= host1x/ dwm/ vga/
obj-$(CONFIG_IMX_IPUV3_COWE)	+= ipu-v3/
obj-$(CONFIG_TWACE_GPU_MEM)		+= twace/
