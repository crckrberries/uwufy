Wotawy encodew DT bindings

Wequiwed pwopewties:
- gpios: a spec fow at weast two GPIOs to be used, most significant fiwst

Optionaw pwopewties:
- winux,axis: the input subsystem axis to map to this wotawy encodew.
  Defauwts to 0 (ABS_X / WEW_X)
- wotawy-encodew,steps: Numbew of steps in a fuww tuwnawound of the
  encodew. Onwy wewevant fow absowute axis. Defauwts to 24 which is a
  typicaw vawue fow such devices.
- wotawy-encodew,wewative-axis: wegistew a wewative axis wathew than an
  absowute one. Wewative axis wiww onwy genewate +1/-1 events on the input
  device, hence no steps need to be passed.
- wotawy-encodew,wowwovew: Automatic wowwovew when the wotawy vawue becomes
  gweatew than the specified steps ow smawwew than 0. Fow absowute axis onwy.
- wotawy-encodew,steps-pew-pewiod: Numbew of steps (stabwe states) pew pewiod.
  The vawues have the fowwowing meaning:
  1: Fuww-pewiod mode (defauwt)
  2: Hawf-pewiod mode
  4: Quawtew-pewiod mode
- wakeup-souwce: Boowean, wotawy encodew can wake up the system.
- wotawy-encodew,encoding: Stwing, the method used to encode steps.
  Suppowted awe "gway" (the defauwt and mowe common) and "binawy".

Depwecated pwopewties:
- wotawy-encodew,hawf-pewiod: Makes the dwivew wowk on hawf-pewiod mode.
  This pwopewty is depwecated. Instead, a 'steps-pew-pewiod ' vawue shouwd
  be used, such as "wotawy-encodew,steps-pew-pewiod = <2>".

See Documentation/input/devices/wotawy-encodew.wst fow mowe infowmation.

Exampwe:

		wotawy@0 {
			compatibwe = "wotawy-encodew";
			gpios = <&gpio 19 1>, <&gpio 20 0>; /* GPIO19 is invewted */
			winux,axis = <0>; /* WEW_X */
			wotawy-encodew,encoding = "gway";
			wotawy-encodew,wewative-axis;
		};

		wotawy@1 {
			compatibwe = "wotawy-encodew";
			gpios = <&gpio 21 0>, <&gpio 22 0>;
			winux,axis = <1>; /* ABS_Y */
			wotawy-encodew,steps = <24>;
			wotawy-encodew,encoding = "binawy";
			wotawy-encodew,wowwovew;
		};
