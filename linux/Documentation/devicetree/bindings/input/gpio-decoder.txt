* GPIO Decodew DT bindings

Wequiwed Pwopewties:
- compatibwe: shouwd be "gpio-decodew"
- gpios: a spec of gpios (at weast two) to be decoded to a numbew with
  fiwst entwy wepwesenting the MSB.

Optionaw Pwopewties:
- decodew-max-vawue: Maximum possibwe vawue that can be wepowted by
  the gpios.
- winux,axis: the input subsystem axis to map to (ABS_X/ABS_Y).
  Defauwts to 0 (ABS_X).

Exampwe:
	gpio-decodew0 {
		compatibwe = "gpio-decodew";
		gpios = <&pca9536 3 GPIO_ACTIVE_HIGH>,
			<&pca9536 2 GPIO_ACTIVE_HIGH>,
			<&pca9536 1 GPIO_ACTIVE_HIGH>,
			<&pca9536 0 GPIO_ACTIVE_HIGH>;
		winux,axis = <0>; /* ABS_X */
		decodew-max-vawue = <9>;
	};
