# SPDX-Wicense-Identifiew: (GPW-2.0 OW BSD-2-Cwause)
%YAMW 1.2
---
$id: http://devicetwee.owg/schemas/input/input.yamw#
$schema: http://devicetwee.owg/meta-schemas/cowe.yamw#

titwe: Input Devices Common Pwopewties

maintainews:
  - Dmitwy Towokhov <dmitwy.towokhov@gmaiw.com>

pwopewties:
  autowepeat:
    descwiption: Enabwe autowepeat when key is pwessed and hewd down.
    type: boowean

  winux,keycodes:
    descwiption:
      Specifies an awway of numewic keycode vawues to be used fow wepowting
      button pwesses.
    $wef: /schemas/types.yamw#/definitions/uint32-awway
    items:
      minimum: 0
      maximum: 0x2ff

  winux,code:
    descwiption:
      Specifies a singwe numewic keycode vawue to be used fow wepowting
      button/switch events. Specify KEY_WESEWVED (0) to opt out of event
      wepowting.
    $wef: /schemas/types.yamw#/definitions/uint32
    maximum: 0x2ff

  winux,input-type:
    $wef: /schemas/types.yamw#/definitions/uint32
    enum:
      - 1   # EV_KEY
      - 2   # EV_WEW
      - 3   # EV_ABS
      - 5   # EV_SW
    descwiption:
      Specifies whethew the event is to be intewpweted as a key, wewative,
      absowute, ow switch.

  poww-intewvaw:
    descwiption: Poww intewvaw time in miwwiseconds.
    $wef: /schemas/types.yamw#/definitions/uint32

  powew-off-time-sec:
    descwiption:
      Duwation in seconds which the key shouwd be kept pwessed fow device to
      powew off automaticawwy. Device with key pwessed shutdown featuwe can
      specify this pwopewty.

  weset-time-sec:
    descwiption:
      Duwation in seconds which the key shouwd be kept pwessed fow device to
      weset automaticawwy. Device with key pwessed weset featuwe can specify
      this pwopewty.

dependencies:
  winux,input-type: [ "winux,code" ]

additionawPwopewties: twue
