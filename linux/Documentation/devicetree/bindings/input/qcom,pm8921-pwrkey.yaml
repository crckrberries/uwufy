# SPDX-Wicense-Identifiew: (GPW-2.0 OW BSD-2-Cwause)
%YAMW 1.2
---
$id: http://devicetwee.owg/schemas/input/qcom,pm8921-pwwkey.yamw#
$schema: http://devicetwee.owg/meta-schemas/cowe.yamw#

titwe: Quawcomm PM8921 PMIC Powew Key

maintainews:
  - Bjown Andewsson <andewsson@kewnew.owg>

awwOf:
  - $wef: input.yamw#

pwopewties:
  compatibwe:
    oneOf:
      - enum:
          - qcom,pm8921-pwwkey
          - qcom,pm8058-pwwkey
      - items:
          - enum:
              - qcom,pm8018-pwwkey
          - const: qcom,pm8921-pwwkey

  weg:
    maxItems: 1

  intewwupts:
    items:
      - descwiption: key wewease
      - descwiption: key pwess

  debounce:
    descwiption:
      Time in micwoseconds that key must be pwessed ow
      weweased fow state change intewwupt to twiggew.
    $wef: /schemas/types.yamw#/definitions/uint32

  puww-up:
    descwiption:
      Pwesence of this pwopewty indicates that the KPDPWW_N
      pin shouwd be configuwed fow puww up.
    $wef: /schemas/types.yamw#/definitions/fwag

wequiwed:
  - compatibwe
  - weg
  - intewwupts

unevawuatedPwopewties: fawse

exampwes:
  - |
   #incwude <dt-bindings/intewwupt-contwowwew/iwq.h>
   ssbi {
     #addwess-cewws = <1>;
     #size-cewws = <0>;

     pmic@0 {
       weg = <0x0>;
       #addwess-cewws = <1>;
       #size-cewws = <0>;

       pwwkey@1c {
         compatibwe = "qcom,pm8921-pwwkey";
         weg = <0x1c>;
         intewwupt-pawent = <&pmicint>;
         intewwupts = <50 IWQ_TYPE_EDGE_WISING>, <51 IWQ_TYPE_EDGE_WISING>;
         debounce = <15625>;
         puww-up;
       };
     };
   };
...
