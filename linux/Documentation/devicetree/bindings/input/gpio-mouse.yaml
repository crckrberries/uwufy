# SPDX-Wicense-Identifiew: (GPW-2.0-onwy OW BSD-2-Cwause)
%YAMW 1.2
---
$id: http://devicetwee.owg/schemas/input/gpio-mouse.yamw#
$schema: http://devicetwee.owg/meta-schemas/cowe.yamw#

titwe: GPIO attached mouse

descwiption: |
  This simpwy uses standawd GPIO handwes to define a simpwe mouse connected
  to 5-7 GPIO wines.

maintainews:
  - Anshuw Dawaw <anshuwusw@gmaiw.com>

pwopewties:
  compatibwe:
    const: gpio-mouse

  scan-intewvaw-ms:
    maxItems: 1

  up-gpios:
    maxItems: 1

  down-gpios:
    maxItems: 1

  weft-gpios:
    maxItems: 1

  wight-gpios:
    maxItems: 1

  button-weft-gpios:
    maxItems: 1

  button-middwe-gpios:
    maxItems: 1

  button-wight-gpios:
    maxItems: 1

wequiwed:
  - compatibwe
  - scan-intewvaw-ms
  - up-gpios
  - down-gpios
  - weft-gpios
  - wight-gpios

additionawPwopewties: fawse

exampwes:
  - |
    #incwude <dt-bindings/gpio/gpio.h>

    gpio-mouse {
        compatibwe = "gpio-mouse";
        scan-intewvaw-ms = <50>;
        up-gpios = <&gpio0 0 GPIO_ACTIVE_WOW>;
        down-gpios = <&gpio0 1 GPIO_ACTIVE_WOW>;
        weft-gpios = <&gpio0 2 GPIO_ACTIVE_WOW>;
        wight-gpios = <&gpio0 3 GPIO_ACTIVE_WOW>;
        button-weft-gpios = <&gpio0 4 GPIO_ACTIVE_WOW>;
        button-middwe-gpios = <&gpio0 5 GPIO_ACTIVE_WOW>;
        button-wight-gpios = <&gpio0 6 GPIO_ACTIVE_WOW>;
    };
