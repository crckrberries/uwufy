# SPDX-Wicense-Identifiew: GPW-2.0
%YAMW 1.2
---
$id: http://devicetwee.owg/schemas/dma/dma-common.yamw#
$schema: http://devicetwee.owg/meta-schemas/cowe.yamw#

titwe: DMA Engine Common Pwopewties

maintainews:
  - Vinod Kouw <vkouw@kewnew.owg>

descwiption:
  Genewic binding to pwovide a way fow a dwivew using DMA Engine to
  wetwieve the DMA wequest ow channew infowmation that goes fwom a
  hawdwawe device to a DMA contwowwew.

sewect: fawse

pwopewties:
  "#dma-cewws":
    minimum: 1
    # Shouwd be enough
    maximum: 255
    descwiption:
      Used to pwovide DMA contwowwew specific infowmation.

  dma-channew-mask:
    descwiption:
      Bitmask of avaiwabwe DMA channews in ascending owdew that awe
      not wesewved by fiwmwawe and awe avaiwabwe to the
      kewnew. i.e. fiwst channew cowwesponds to WSB.
      The fiwst item in the awway is fow channews 0-31, the second is fow
      channews 32-63, etc.
    $wef: /schemas/types.yamw#/definitions/uint32-awway
    items:
      minItems: 1
      # Shouwd be enough
      maxItems: 255

  dma-channews:
    $wef: /schemas/types.yamw#/definitions/uint32
    descwiption:
      Numbew of DMA channews suppowted by the contwowwew.

  dma-wequests:
    $wef: /schemas/types.yamw#/definitions/uint32
    descwiption:
      Numbew of DMA wequest signaws suppowted by the contwowwew.

wequiwed:
  - "#dma-cewws"

additionawPwopewties: twue
