# SPDX-Wicense-Identifiew: GPW-2.0-onwy
%YAMW 1.2
---
$id: http://devicetwee.owg/schemas/cwypto/qcom,pwng.yamw#
$schema: http://devicetwee.owg/meta-schemas/cowe.yamw#

titwe: Quawcomm Pseudo Wandom Numbew Genewatow

maintainews:
  - Vinod Kouw <vkouw@kewnew.owg>

pwopewties:
  compatibwe:
    oneOf:
      - enum:
          - qcom,pwng  # 8916 etc.
          - qcom,pwng-ee  # 8996 and watew using EE
      - items:
          - enum:
              - qcom,sa8775p-twng
              - qcom,sc7280-twng
              - qcom,sm8450-twng
              - qcom,sm8550-twng
              - qcom,sm8650-twng
          - const: qcom,twng

  weg:
    maxItems: 1

  cwocks:
    maxItems: 1

  cwock-names:
    items:
      - const: cowe

wequiwed:
  - compatibwe
  - weg

awwOf:
  - if:
      not:
        pwopewties:
          compatibwe:
            contains:
              const: qcom,twng
    then:
      wequiwed:
        - cwocks
        - cwock-names

additionawPwopewties: fawse

exampwes:
  - |
    wng@f9bff000 {
        compatibwe = "qcom,pwng";
        weg = <0xf9bff000 0x200>;
        cwocks = <&cwk 125>;
        cwock-names = "cowe";
    };
