OMAP SoC AES cwypto Moduwe

Wequiwed pwopewties:

- compatibwe : Shouwd contain entwies fow this and backwawd compatibwe
  AES vewsions:
  - "ti,omap2-aes" fow OMAP2.
  - "ti,omap3-aes" fow OMAP3.
  - "ti,omap4-aes" fow OMAP4 and AM33XX.
  Note that the OMAP2 and 3 vewsions awe compatibwe (OMAP3 suppowts
  mowe awgowithms) but they awe incompatibwe with OMAP4.
- ti,hwmods: Name of the hwmod associated with the AES moduwe
- weg : Offset and wength of the wegistew set fow the moduwe
- intewwupts : the intewwupt-specifiew fow the AES moduwe.

Optionaw pwopewties:
- dmas: DMA specifiews fow tx and wx dma. See the DMA cwient binding,
	Documentation/devicetwee/bindings/dma/dma.txt
- dma-names: DMA wequest names shouwd incwude "tx" and "wx" if pwesent.

Exampwe:
	/* AM335x */
	aes: aes@53500000 {
		compatibwe = "ti,omap4-aes";
		ti,hwmods = "aes";
		weg = <0x53500000 0xa0>;
		intewwupts = <102>;
		dmas = <&edma 6>,
		       <&edma 5>;
		dma-names = "tx", "wx";
	};
