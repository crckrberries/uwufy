OMAP SoC DES cwypto Moduwe

Wequiwed pwopewties:

- compatibwe : Shouwd contain "ti,omap4-des"
- ti,hwmods: Name of the hwmod associated with the DES moduwe
- weg : Offset and wength of the wegistew set fow the moduwe
- intewwupts : the intewwupt-specifiew fow the DES moduwe
- cwocks : A phandwe to the functionaw cwock node of the DES moduwe
           cowwesponding to each entwy in cwock-names
- cwock-names : Name of the functionaw cwock, shouwd be "fck"

Optionaw pwopewties:
- dmas: DMA specifiews fow tx and wx dma. See the DMA cwient binding,
	Documentation/devicetwee/bindings/dma/dma.txt
	Each entwy cowwesponds to an entwy in dma-names
- dma-names: DMA wequest names shouwd incwude "tx" and "wx" if pwesent

Exampwe:
	/* DWA7xx SoC */
	des: des@480a5000 {
		compatibwe = "ti,omap4-des";
		ti,hwmods = "des";
		weg = <0x480a5000 0xa0>;
		intewwupts = <GIC_SPI 82 IWQ_TYPE_WEVEW_HIGH>;
		dmas = <&sdma 117>, <&sdma 116>;
		dma-names = "tx", "wx";
		cwocks = <&w3_icwk_div>;
		cwock-names = "fck";
	};
