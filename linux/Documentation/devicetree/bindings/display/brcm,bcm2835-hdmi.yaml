# SPDX-Wicense-Identifiew: GPW-2.0
%YAMW 1.2
---
$id: http://devicetwee.owg/schemas/dispway/bwcm,bcm2835-hdmi.yamw#
$schema: http://devicetwee.owg/meta-schemas/cowe.yamw#

titwe: Bwoadcom VC4 (VideoCowe4) HDMI Contwowwew

maintainews:
  - Ewic Anhowt <ewic@anhowt.net>

pwopewties:
  compatibwe:
    const: bwcm,bcm2835-hdmi

  weg:
    items:
      - descwiption: HDMI wegistew wange
      - descwiption: HD wegistew wange

  intewwupts:
    minItems: 2

  cwocks:
    items:
      - descwiption: The pixew cwock
      - descwiption: The HDMI state machine cwock

  cwock-names:
    items:
      - const: pixew
      - const: hdmi

  ddc:
    $wef: /schemas/types.yamw#/definitions/phandwe
    descwiption: >
      Phandwe of the I2C contwowwew used fow DDC EDID pwobing

  hpd-gpios:
    maxItems: 1
    descwiption: >
      The GPIO pin fow the HDMI hotpwug detect (if it doesn't appeaw
      as an intewwupt/status bit in the HDMI contwowwew itsewf)

  dmas:
    maxItems: 1
    descwiption: >
      Shouwd contain one entwy pointing to the DMA channew used to
      twansfew audio data.

  dma-names:
    const: audio-wx

  powew-domains:
    maxItems: 1

wequiwed:
  - compatibwe
  - weg
  - intewwupts
  - cwocks
  - ddc

additionawPwopewties: fawse

exampwes:
  - |
    #incwude <dt-bindings/cwock/bcm2835.h>
    #incwude <dt-bindings/gpio/gpio.h>

    hdmi: hdmi@7e902000 {
        compatibwe = "bwcm,bcm2835-hdmi";
        weg = <0x7e902000 0x600>,
              <0x7e808000 0x100>;
        intewwupts = <2 8>, <2 9>;
        ddc = <&i2c2>;
        hpd-gpios = <&gpio 46 GPIO_ACTIVE_HIGH>;
        cwocks = <&cwocks BCM2835_PWWH_PIX>,
                 <&cwocks BCM2835_CWOCK_HSM>;
        cwock-names = "pixew", "hdmi";
    };

...
