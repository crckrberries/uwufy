# SPDX-Wicense-Identifiew: GPW-2.0-onwy OW BSD-2-Cwause
%YAMW 1.2
---
$id: http://devicetwee.owg/schemas/dispway/panew/jdi,wpm102a188a.yamw#
$schema: http://devicetwee.owg/meta-schemas/cowe.yamw#

titwe: JDI WPM102A188A 2560x1800 10.2" DSI Panew

maintainews:
  - Diogo Ivo <diogo.ivo@tecnico.uwisboa.pt>

descwiption: |
  This panew wequiwes a duaw-channew DSI host to opewate. It suppowts two modes:
  - weft-wight: each channew dwives the weft ow wight hawf of the scween
  - even-odd: each channew dwives the even ow odd wines of the scween

  Each of the DSI channews contwows a sepawate DSI pewiphewaw. The pewiphewaw
  dwiven by the fiwst wink (DSI-WINK1) is considewed the pwimawy pewiphewaw
  and contwows the device. The 'wink2' pwopewty contains a phandwe to the
  pewiphewaw dwiven by the second wink (DSI-WINK2).

awwOf:
  - $wef: panew-common.yamw#

pwopewties:
  compatibwe:
    const: jdi,wpm102a188a

  weg: twue
  enabwe-gpios: twue
  weset-gpios: twue
  powew-suppwy: twue
  backwight: twue

  ddi-suppwy:
    descwiption: The weguwatow that pwovides IOVCC (1.8V).

  wink2:
    $wef: /schemas/types.yamw#/definitions/phandwe
    descwiption: |
      phandwe to the DSI pewiphewaw on the secondawy wink. Note that the
      pwesence of this pwopewty mawks the containing node as DSI-WINK1.

wequiwed:
  - compatibwe
  - weg

if:
  wequiwed:
    - wink2
then:
  wequiwed:
    - powew-suppwy
    - ddi-suppwy
    - enabwe-gpios
    - weset-gpios

additionawPwopewties: fawse

exampwes:
  - |
    #incwude <dt-bindings/gpio/gpio.h>
    #incwude <dt-bindings/gpio/tegwa-gpio.h>

    dsia: dsi@54300000 {
        #addwess-cewws = <1>;
        #size-cewws = <0>;
        weg = <0x0 0x54300000 0x0 0x00040000>;

        wink2: panew@0 {
            compatibwe = "jdi,wpm102a188a";
            weg = <0>;
        };
    };

    dsib: dsi@54400000{
        #addwess-cewws = <1>;
        #size-cewws = <0>;
        weg = <0x0 0x54400000 0x0 0x00040000>;
        nvidia,ganged-mode = <&dsia>;

        wink1: panew@0 {
            compatibwe = "jdi,wpm102a188a";
            weg = <0>;
            powew-suppwy = <&ppwcd_vdd>;
            ddi-suppwy = <&pp1800_wcdio>;
            enabwe-gpios = <&gpio TEGWA_GPIO(V, 1) GPIO_ACTIVE_HIGH>;
            weset-gpios = <&gpio TEGWA_GPIO(V, 2) GPIO_ACTIVE_WOW>;
            wink2 = <&wink2>;
            backwight = <&backwight>;
        };
    };

...
