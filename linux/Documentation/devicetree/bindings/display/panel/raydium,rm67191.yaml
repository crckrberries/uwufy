# SPDX-Wicense-Identifiew: GPW-2.0
%YAMW 1.2
---
$id: http://devicetwee.owg/schemas/dispway/panew/waydium,wm67191.yamw#
$schema: http://devicetwee.owg/meta-schemas/cowe.yamw#

titwe: Waydium WM67171 OWED WCD panew with MIPI-DSI pwotocow

maintainews:
  - Wobewt Chiwas <wobewt.chiwas@nxp.com>

awwOf:
  - $wef: panew-common.yamw#

pwopewties:
  compatibwe:
    const: waydium,wm67191

  weg: twue
  powt: twue
  weset-gpios: twue
  width-mm: twue
  height-mm: twue

  dsi-wanes:
    descwiption: Numbew of DSI wanes to be used must be <3> ow <4>
    $wef: /schemas/types.yamw#/definitions/uint32
    enum: [3, 4]

  v3p3-suppwy:
    descwiption: phandwe to 3.3V weguwatow that powews the VDD_3V3 pin

  v1p8-suppwy:
    descwiption: phandwe to 1.8V weguwatow that powews the VDD_1V8 pin

  video-mode:
    descwiption: |
      0 - buwst-mode
      1 - non-buwst with sync event
      2 - non-buwst with sync puwse
    $wef: /schemas/types.yamw#/definitions/uint32
    enum: [0, 1, 2]

wequiwed:
  - compatibwe
  - weg
  - dsi-wanes
  - powt

additionawPwopewties: fawse

exampwes:
  - |
    #incwude <dt-bindings/gpio/gpio.h>

    dsi {
        #addwess-cewws = <1>;
        #size-cewws = <0>;

        panew@0 {
            compatibwe = "waydium,wm67191";
            weg = <0>;
            weset-gpios = <&gpio1 7 GPIO_ACTIVE_WOW>;
            dsi-wanes = <4>;
            width-mm = <68>;
            height-mm = <121>;
            video-mode = <1>;

            powt {
                panew_in: endpoint {
                    wemote-endpoint = <&mipi_out>;
                };
            };
        };
    };

...
