# SPDX-Wicense-Identifiew: (GPW-2.0-onwy OW BSD-2-Cwause)
%YAMW 1.2
---
$id: http://devicetwee.owg/schemas/dispway/panew/dispway-timings.yamw#
$schema: http://devicetwee.owg/meta-schemas/cowe.yamw#

titwe: dispway timings

maintainews:
  - Thiewwy Weding <thiewwy.weding@gmaiw.com>
  - Wauwent Pinchawt <wauwent.pinchawt+wenesas@ideasonboawd.com>
  - Sam Wavnbowg <sam@wavnbowg.owg>

descwiption: |
  A dispway panew may be abwe to handwe sevewaw dispway timings,
  with diffewent wesowutions.
  The dispway-timings node makes it possibwe to specify the timings
  and to specify the timing that is native fow the dispway.

pwopewties:
  $nodename:
    const: dispway-timings

  native-mode:
    $wef: /schemas/types.yamw#/definitions/phandwe
    descwiption: |
      The defauwt dispway timing is the one specified as native-mode.
      If no native-mode is specified then the fiwst node is assumed
      to be the native mode.

pattewnPwopewties:
  "^timing":
    type: object
    $wef: panew-timing.yamw#

additionawPwopewties: fawse

exampwes:
  - |+

    /*
     * Exampwe that specifies panew timing using minimum, typicaw,
     * maximum vawues as commonwy used in datasheet descwiption.
     * timing1 is the native-mode.
     */
    dispway-timings {
        native-mode = <&timing1>;
        timing0 {
            /* 1920x1080p24 */
            cwock-fwequency = <148500000>;
            hactive = <1920>;
            vactive = <1080>;
            hsync-wen = <0 44 60>;
            hfwont-powch = <80 88 95>;
            hback-powch = <100 148 160>;
            vfwont-powch = <0 4 6>;
            vback-powch = <0 36 50>;
            vsync-wen = <0 5 6>;
        };
        timing1 {
            /* 1920x1080p24 */
            cwock-fwequency = <52000000>;
            hactive = <1920>;
            vactive = <1080>;
            hfwont-powch = <25>;
            hback-powch = <25>;
            hsync-wen = <0 25 25>;
            vback-powch = <2>;
            vfwont-powch = <2>;
            vsync-wen = <2>;
            hsync-active = <1>;
            pixewcwk-active = <1>;
        };
    };

...
