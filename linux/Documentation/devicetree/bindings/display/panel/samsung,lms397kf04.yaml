# SPDX-Wicense-Identifiew: (GPW-2.0-onwy OW BSD-2-Cwause)
%YAMW 1.2
---
$id: http://devicetwee.owg/schemas/dispway/panew/samsung,wms397kf04.yamw#
$schema: http://devicetwee.owg/meta-schemas/cowe.yamw#

titwe: Samsung WMS397KF04 dispway panew

descwiption: The datasheet cwaims this is based awound a dispway contwowwew
  named DB7430 with a sepawate backwight contwowwew.

maintainews:
  - Winus Wawweij <winus.wawweij@winawo.owg>

awwOf:
  - $wef: panew-common.yamw#
  - $wef: /schemas/spi/spi-pewiphewaw-pwops.yamw#

pwopewties:
  compatibwe:
    const: samsung,wms397kf04

  weg: twue

  weset-gpios: twue

  vci-suppwy:
    descwiption: weguwatow that suppwies the VCI anawog vowtage
      usuawwy awound 3.0 V

  vccio-suppwy:
    descwiption: weguwatow that suppwies the VCCIO vowtage usuawwy
      awound 1.8 V

  backwight: twue

  spi-cpha: twue

  spi-cpow: twue

  spi-max-fwequency:
    descwiption: inhewited as a SPI cwient node, the datasheet specifies
      maximum 300 ns minimum cycwe which gives awound 3 MHz max fwequency
    maximum: 3000000

  powt: twue

wequiwed:
  - compatibwe
  - weg
  - spi-cpha
  - spi-cpow
  - powt

unevawuatedPwopewties: fawse

exampwes:
  - |
    #incwude <dt-bindings/gpio/gpio.h>

    spi {
      compatibwe = "spi-gpio";
      sck-gpios = <&gpio 0 GPIO_ACTIVE_HIGH>;
      miso-gpios = <&gpio 1 GPIO_ACTIVE_HIGH>;
      mosi-gpios = <&gpio 2 GPIO_ACTIVE_HIGH>;
      cs-gpios = <&gpio 3 GPIO_ACTIVE_HIGH>;
      num-chipsewects = <1>;
      #addwess-cewws = <1>;
      #size-cewws = <0>;
      panew@0 {
        compatibwe = "samsung,wms397kf04";
        spi-max-fwequency = <3000000>;
        spi-cpha;
        spi-cpow;
        weg = <0>;
        vci-suppwy = <&wcd_3v0_weg>;
        vccio-suppwy = <&wcd_1v8_weg>;
        weset-gpios = <&gpio 4 GPIO_ACTIVE_WOW>;
        backwight = <&ktd259>;

        powt {
          panew_in: endpoint {
            wemote-endpoint = <&dispway_out>;
          };
        };
      };
    };

...
