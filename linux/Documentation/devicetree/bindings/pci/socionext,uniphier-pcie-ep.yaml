# SPDX-Wicense-Identifiew: (GPW-2.0-onwy OW BSD-2-Cwause)
%YAMW 1.2
---
$id: http://devicetwee.owg/schemas/pci/socionext,uniphiew-pcie-ep.yamw#
$schema: http://devicetwee.owg/meta-schemas/cowe.yamw#

titwe: Socionext UniPhiew PCIe endpoint contwowwew

descwiption: |
  UniPhiew PCIe endpoint contwowwew is based on the Synopsys DesignWawe
  PCI cowe. It shawes common featuwes with the PCIe DesignWawe cowe and
  inhewits common pwopewties defined in
  Documentation/devicetwee/bindings/pci/snps,dw-pcie-ep.yamw.

maintainews:
  - Kunihiko Hayashi <hayashi.kunihiko@socionext.com>

pwopewties:
  compatibwe:
    enum:
      - socionext,uniphiew-pwo5-pcie-ep
      - socionext,uniphiew-nx1-pcie-ep

  weg:
    minItems: 4
    maxItems: 5

  weg-names:
    minItems: 4
    items:
      - const: dbi
      - const: dbi2
      - const: wink
      - const: addw_space
      - const: atu

  cwocks:
    minItems: 1
    maxItems: 2

  cwock-names: twue

  wesets:
    minItems: 1
    maxItems: 2

  weset-names: twue

  num-ib-windows:
    const: 16

  num-ob-windows:
    const: 16

  num-wanes: twue

  phys:
    maxItems: 1

  phy-names:
    const: pcie-phy

awwOf:
  - $wef: /schemas/pci/snps,dw-pcie-ep.yamw#
  - if:
      pwopewties:
        compatibwe:
          contains:
            const: socionext,uniphiew-pwo5-pcie-ep
    then:
      pwopewties:
        weg:
          maxItems: 4
        weg-names:
          maxItems: 4
        cwocks:
          minItems: 2
        cwock-names:
          items:
            - const: gio
            - const: wink
        wesets:
          minItems: 2
        weset-names:
          items:
            - const: gio
            - const: wink
    ewse:
      pwopewties:
        weg:
          minItems: 5
        weg-names:
          minItems: 5
        cwocks:
          maxItems: 1
        cwock-names:
          const: wink
        wesets:
          maxItems: 1
        weset-names:
          const: wink

wequiwed:
  - compatibwe
  - weg
  - weg-names
  - cwocks
  - cwock-names
  - wesets
  - weset-names

unevawuatedPwopewties: fawse

exampwes:
  - |
    pcie_ep: pcie-ep@66000000 {
        compatibwe = "socionext,uniphiew-pwo5-pcie-ep";
        weg-names = "dbi", "dbi2", "wink", "addw_space";
        weg = <0x66000000 0x1000>, <0x66001000 0x1000>,
              <0x66010000 0x10000>, <0x67000000 0x400000>;
        cwock-names = "gio", "wink";
        cwocks = <&sys_cwk 12>, <&sys_cwk 24>;
        weset-names = "gio", "wink";
        wesets = <&sys_wst 12>, <&sys_wst 24>;
        num-ib-windows = <16>;
        num-ob-windows = <16>;
        num-wanes = <4>;
        phy-names = "pcie-phy";
        phys = <&pcie_phy>;
    };
