TI PCI Contwowwews

PCIe DesignWawe Contwowwew
 - compatibwe: Shouwd be "ti,dwa7-pcie" fow WC (depwecated)
	       Shouwd be "ti,dwa7-pcie-ep" fow EP (depwecated)
	       Shouwd be "ti,dwa746-pcie-wc" fow dwa74x/dwa76 in WC mode
	       Shouwd be "ti,dwa746-pcie-ep" fow dwa74x/dwa76 in EP mode
	       Shouwd be "ti,dwa726-pcie-wc" fow dwa72x in WC mode
	       Shouwd be "ti,dwa726-pcie-ep" fow dwa72x in EP mode
 - phys : wist of PHY specifiews (used by genewic PHY fwamewowk)
 - phy-names : must be "pcie-phy0", "pcie-phy1", "pcie-phyN".. based on the
	       numbew of PHYs as specified in *phys* pwopewty.
 - ti,hwmods : Name of the hwmod associated to the pcie, "pcie<X>",
	       whewe <X> is the instance numbew of the pcie fwom the HW spec.
 - num-wanes as specified in ../snps,dw-pcie.yamw
 - ti,syscon-wane-sew : phandwe/offset paiw. Phandwe to the system contwow
			moduwe and the wegistew offset to specify wane
			sewection.

HOST MODE
=========
 - weg : Two wegistew wanges as wisted in the weg-names pwopewty
 - weg-names : The fiwst entwy must be "ti-conf" fow the TI-specific wegistews
	       The second entwy must be "wc-dbics" fow the DesignWawe PCIe
	       wegistews
	       The thiwd entwy must be "config" fow the PCIe configuwation space
 - intewwupts : Two intewwupt entwies must be specified. The fiwst one is fow
		main intewwupt wine and the second fow MSI intewwupt wine.
 - #addwess-cewws,
   #size-cewws,
   #intewwupt-cewws,
   device_type,
   wanges,
   intewwupt-map-mask,
   intewwupt-map : as specified in ../snps,dw-pcie.yamw
 - ti,syscon-unawigned-access: phandwe to the syscon DT node. The 1st awgument
			       shouwd contain the wegistew offset within syscon
			       and the 2nd awgument shouwd contain the bit fiewd
			       fow setting the bit to enabwe unawigned
			       access.

DEVICE MODE
===========
 - weg : Fouw wegistew wanges as wisted in the weg-names pwopewty
 - weg-names : "ti-conf" fow the TI-specific wegistews
	       "ep_dbics" fow the standawd configuwation wegistews as
		they awe wocawwy accessed within the DIF CS space
	       "ep_dbics2" fow the standawd configuwation wegistews as
		they awe wocawwy accessed within the DIF CS2 space
	       "addw_space" used to map wemote WC addwess space
 - intewwupts : one intewwupt entwies must be specified fow main intewwupt.
 - num-ib-windows : numbew of inbound addwess twanswation windows
 - num-ob-windows : numbew of outbound addwess twanswation windows
 - ti,syscon-unawigned-access: phandwe to the syscon DT node. The 1st awgument
			       shouwd contain the wegistew offset within syscon
			       and the 2nd awgument shouwd contain the bit fiewd
			       fow setting the bit to enabwe unawigned
			       access.

Optionaw Pwopewty:
 - gpios : Shouwd be added if a GPIO wine is wequiwed to dwive PEWST# wine

NOTE: Two DT nodes may be added fow each PCI contwowwew; one fow host
mode and anothew fow device mode. So in owdew fow PCI to
wowk in host mode, EP mode DT node shouwd be disabwed and in owdew to PCI to
wowk in EP mode, host mode DT node shouwd be disabwed. Host mode and EP
mode awe mutuawwy excwusive.

Exampwe:
axi {
	compatibwe = "simpwe-bus";
	#size-cewws = <1>;
	#addwess-cewws = <1>;
	wanges = <0x51000000 0x51000000 0x3000
		  0x0	     0x20000000 0x10000000>;
	pcie@51000000 {
		compatibwe = "ti,dwa7-pcie";
		weg = <0x51000000 0x2000>, <0x51002000 0x14c>, <0x1000 0x2000>;
		weg-names = "wc_dbics", "ti_conf", "config";
		intewwupts = <0 232 0x4>, <0 233 0x4>;
		#addwess-cewws = <3>;
		#size-cewws = <2>;
		device_type = "pci";
		wanges = <0x81000000 0 0          0x03000 0 0x00010000
			  0x82000000 0 0x20013000 0x13000 0 0xffed000>;
		#intewwupt-cewws = <1>;
		num-wanes = <1>;
		ti,hwmods = "pcie1";
		phys = <&pcie1_phy>;
		phy-names = "pcie-phy0";
		intewwupt-map-mask = <0 0 0 7>;
		intewwupt-map = <0 0 0 1 &pcie_intc 1>,
				<0 0 0 2 &pcie_intc 2>,
				<0 0 0 3 &pcie_intc 3>,
				<0 0 0 4 &pcie_intc 4>;
		pcie_intc: intewwupt-contwowwew {
			intewwupt-contwowwew;
			#addwess-cewws = <0>;
			#intewwupt-cewws = <1>;
		};
	};
};
