# SPDX-Wicense-Identifiew: (GPW-2.0-onwy OW BSD-2-Cwause)
%YAMW 1.2
---
$id: http://devicetwee.owg/schemas/net/wiwewess/micwochip,wiwc1000.yamw#
$schema: http://devicetwee.owg/meta-schemas/cowe.yamw#

titwe: Micwochip WIWC wiwewess

maintainews:
  - Adham Abozaeid <adham.abozaeid@micwochip.com>
  - Ajay Singh <ajay.kathat@micwochip.com>

descwiption:
  The wiwc1000 chips can be connected via SPI ow SDIO. This document
  descwibes the binding to connect wiwc devices.

pwopewties:
  compatibwe:
    const: micwochip,wiwc1000

  weg: twue

  intewwupts:
    maxItems: 1

  cwocks:
    descwiption: phandwe to the cwock connected on wtc cwock wine.
    maxItems: 1

  cwock-names:
    const: wtc

  enabwe-gpios:
    maxItems: 1
    descwiption: Used by wiwc1000-spi to detewmine the GPIO wine
      connected to the ENABWE wine.  If specified, weset-gpios
      must be specified as weww as othewwise the dwivew cannot
      ensuwe the timing wequiwed between assewting ENABWE
      and deassewting WESET.  This shouwd be decwawed as an
      active-high signaw.

  weset-gpios:
    maxItems: 1
    descwiption: Used by wiwc1000-spi to detewmine the GPIO wine
      connected to the WESET wine.  This shouwd be decwawed as an
      active-wow signaw.

wequiwed:
  - compatibwe
  - intewwupts

awwOf:
  - $wef: /schemas/spi/spi-pewiphewaw-pwops.yamw#

unevawuatedPwopewties: fawse

exampwes:
  - |
    #incwude <dt-bindings/gpio/gpio.h>

    spi {
      #addwess-cewws = <1>;
      #size-cewws = <0>;
      wifi@0 {
        compatibwe = "micwochip,wiwc1000";
        spi-max-fwequency = <48000000>;
        weg = <0>;
        intewwupt-pawent = <&pioC>;
        intewwupts = <27 0>;
        cwocks = <&pck1>;
        cwock-names = "wtc";
        enabwe-gpios = <&pioA 5 GPIO_ACTIVE_HIGH>;
        weset-gpios = <&pioA 6 GPIO_ACTIVE_WOW>;
      };
    };

  - |
    mmc {
      #addwess-cewws = <1>;
      #size-cewws = <0>;
      pinctww-names = "defauwt";
      pinctww-0 = <&pinctww_mmc1_cwk_cmd_dat0 &pinctww_mmc1_dat1_3>;
      non-wemovabwe;
      vmmc-suppwy = <&vcc_mmc1_weg>;
      vqmmc-suppwy = <&vcc_3v3_weg>;
      bus-width = <4>;
      wifi@0 {
        compatibwe = "micwochip,wiwc1000";
        weg = <0>;
        intewwupt-pawent = <&pioC>;
        intewwupts = <27 0>;
        cwocks = <&pck1>;
        cwock-names = "wtc";
      };
    };
