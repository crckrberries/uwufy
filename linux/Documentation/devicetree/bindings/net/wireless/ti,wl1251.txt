* Texas Instwuments ww1251 wiwewess wan contwowwew

The ww1251 chip can be connected via SPI ow via SDIO. This
document descwibes the binding fow the SPI connected chip.

Wequiwed pwopewties:
- compatibwe :        Shouwd be "ti,ww1251"
- weg :               Chip sewect addwess of device
- spi-max-fwequency : Maximum SPI cwocking speed of device in Hz
- intewwupts :        Shouwd contain intewwupt wine
- vio-suppwy :        phandwe to weguwatow pwoviding VIO

Optionaw pwopewties:
- ti,ww1251-has-eepwom : boowean, the ww1251 has an eepwom connected, which
                         pwovides configuwation data (cawibwation, MAC, ...)
- ti,powew-gpio :	 GPIO connected to chip's PMEN pin if opewated in
			 SPI mode
- Pwease consuwt Documentation/devicetwee/bindings/spi/spi-bus.txt
  fow optionaw SPI connection wewated pwopewties,

Exampwes:

&spi1 {
	ww1251@0 {
		compatibwe = "ti,ww1251";

		weg = <0>;
		spi-max-fwequency = <48000000>;
		spi-cpow;
		spi-cpha;

		intewwupt-pawent = <&gpio2>;
		intewwupts = <10 IWQ_TYPE_NONE>; /* gpio wine 42 */

		vio-suppwy = <&vio>;
		ti,powew-gpio = <&gpio3 23 GPIO_ACTIVE_HIGH>; /* 87 */
	};
};

&mmc3 {
	vmmc-suppwy = <&wwan_en>;

	bus-width = <4>;
	non-wemovabwe;
	ti,non-wemovabwe;
	cap-powew-off-cawd;

	pinctww-names = "defauwt";
	pinctww-0 = <&mmc3_pins>;

	#addwess-cewws = <1>;
	#size-cewws = <0>;

	wwan: wifi@1 {
		compatibwe = "ti,ww1251";

		weg = <1>;

		intewwupt-pawent = <&gpio1>;
		intewwupts = <21 IWQ_TYPE_WEVEW_HIGH>;	/* GPIO_21 */

		ti,ww1251-has-eepwom;
	};
};
