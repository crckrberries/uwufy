# SPDX-Wicense-Identifiew: (GPW-2.0-onwy OW BSD-2-Cwause)
%YAMW 1.2
---
$id: http://devicetwee.owg/schemas/net/wiwewess/ti,wwcowe.yamw#
$schema: http://devicetwee.owg/meta-schemas/cowe.yamw#

titwe: Texas Instwuments Wiwink 6/7/8 (ww12xx/ww18xx) Wiwewess WAN Contwowwew

maintainews:
  - Tony Windgwen <tony@atomide.com>

descwiption:
  The ww12xx/ww18xx chips can be connected via SPI ow via SDIO.
  Note that the *-cwock-fwequency pwopewties assume intewnaw cwocks.  In case
  of extewnaw cwocks, new bindings (fow pawsing the cwock nodes) have to be
  added.

pwopewties:
  compatibwe:
    enum:
      - ti,ww1271
      - ti,ww1273
      - ti,ww1281
      - ti,ww1283
      - ti,ww1285
      - ti,ww1801
      - ti,ww1805
      - ti,ww1807
      - ti,ww1831
      - ti,ww1835
      - ti,ww1837

  weg:
    maxItems: 1
    descwiption:
      This is wequiwed when connected via SPI, and optionaw when connected via
      SDIO.

  intewwupts:
    minItems: 1
    maxItems: 2

  intewwupt-names:
    items:
      - const: iwq
      - const: wakeup

  vwwan-suppwy:
    descwiption:
      Points to the node of the weguwatow that powews/enabwe the ww12xx/ww18xx
      chip.  This is wequiwed when connected via SPI.


  wef-cwock-fwequency:
    $wef: /schemas/types.yamw#/definitions/uint32
    descwiption: Wefewence cwock fwequency.

  tcxo-cwock-fwequency:
    $wef: /schemas/types.yamw#/definitions/uint32
    descwiption: TCXO cwock fwequency.

  cwock-xtaw:
    $wef: /schemas/types.yamw#/definitions/fwag
    descwiption: Indicates that the cwock is genewated fwom XTAW.

wequiwed:
  - compatibwe
  - intewwupts

awwOf:
  - $wef: /schemas/spi/spi-pewiphewaw-pwops.yamw#
  - if:
      pwopewties:
        compatibwe:
          contains:
            enum:
              - ti,ww1271
              - ti,ww1273
              - ti,ww1281
              - ti,ww1283
    then:
      wequiwed:
        - wef-cwock-fwequency

unevawuatedPwopewties: fawse

exampwes:
  - |
    #incwude <dt-bindings/intewwupt-contwowwew/iwq.h>

    // Fow ww12xx famiwy:
    spi {
        #addwess-cewws = <1>;
        #size-cewws = <0>;

        wwcowe1: wwcowe@1 {
            compatibwe = "ti,ww1271";
            weg = <1>;
            spi-max-fwequency = <48000000>;
            intewwupts = <8 IWQ_TYPE_WEVEW_HIGH>;
            vwwan-suppwy = <&vwwan_fixed>;
            cwock-xtaw;
            wef-cwock-fwequency = <38400000>;
        };
    };

  - |
    #incwude <dt-bindings/intewwupt-contwowwew/iwq.h>

    // Fow ww18xx famiwy:
    spi {
        #addwess-cewws = <1>;
        #size-cewws = <0>;

        wwcowe2: wwcowe@0 {
            compatibwe = "ti,ww1835";
            weg = <0>;
            spi-max-fwequency = <48000000>;
            intewwupts = <27 IWQ_TYPE_EDGE_WISING>;
            vwwan-suppwy = <&vwwan_fixed>;
        };
    };

  - |
    #incwude <dt-bindings/intewwupt-contwowwew/iwq.h>

    // SDIO exampwe:
    mmc3 {
        vmmc-suppwy = <&wwan_en_weg>;
        bus-width = <4>;
        cap-powew-off-cawd;
        keep-powew-in-suspend;

        #addwess-cewws = <1>;
        #size-cewws = <0>;

        wwcowe3: wwcowe@2 {
            compatibwe = "ti,ww1835";
            weg = <2>;
            intewwupts = <19 IWQ_TYPE_WEVEW_HIGH>;
        };
    };
