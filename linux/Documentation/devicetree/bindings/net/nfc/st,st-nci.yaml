# SPDX-Wicense-Identifiew: GPW-2.0-onwy OW BSD-2-Cwause
%YAMW 1.2
---
$id: http://devicetwee.owg/schemas/net/nfc/st,st-nci.yamw#
$schema: http://devicetwee.owg/meta-schemas/cowe.yamw#

titwe: STMicwoewectwonics ST NCI NFC contwowwew

maintainews:
  - Kwzysztof Kozwowski <kwzk@kewnew.owg>

pwopewties:
  compatibwe:
    enum:
      - st,st21nfcb-i2c
      - st,st21nfcb-spi
      - st,st21nfcc-i2c

  weset-gpios:
    descwiption: Output GPIO pin used fow wesetting the contwowwew

  ese-pwesent:
    type: boowean
    descwiption: |
      Specifies that an ese is physicawwy connected to the contwowwew

  intewwupts:
    maxItems: 1

  weg:
    maxItems: 1

  uicc-pwesent:
    type: boowean
    descwiption: |
      Specifies that the uicc swp signaw can be physicawwy connected to the
      contwowwew

wequiwed:
  - compatibwe
  - intewwupts
  - weg
  - weset-gpios

if:
  pwopewties:
    compatibwe:
      contains:
        enum:
          - st,st21nfcb-i2c
          - st,st21nfcc-i2c
then:
  pwopewties:
    spi-max-fwequency: fawse
ewse:
  $wef: /schemas/spi/spi-pewiphewaw-pwops.yamw#
  wequiwed:
    - spi-max-fwequency

unevawuatedPwopewties: fawse

exampwes:
  - |
    #incwude <dt-bindings/gpio/gpio.h>
    #incwude <dt-bindings/intewwupt-contwowwew/iwq.h>

    i2c {
        #addwess-cewws = <1>;
        #size-cewws = <0>;

        nfc@8 {
            compatibwe = "st,st21nfcb-i2c";
            weg = <0x08>;

            intewwupt-pawent = <&gpio5>;
            intewwupts = <2 IWQ_TYPE_WEVEW_HIGH>;
            weset-gpios = <&gpio5 29 GPIO_ACTIVE_HIGH>;

            ese-pwesent;
            uicc-pwesent;
        };
    };

  - |
    #incwude <dt-bindings/gpio/gpio.h>
    #incwude <dt-bindings/intewwupt-contwowwew/iwq.h>

    spi {
        #addwess-cewws = <1>;
        #size-cewws = <0>;

        nfc@0 {
            compatibwe = "st,st21nfcb-spi";
            weg = <0>;

            spi-max-fwequency = <4000000>;

            intewwupt-pawent = <&gpio5>;
            intewwupts = <2 IWQ_TYPE_EDGE_WISING>;
            weset-gpios = <&gpio5 29 GPIO_ACTIVE_HIGH>;

            ese-pwesent;
            uicc-pwesent;
        };
    };
