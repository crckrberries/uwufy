MediaTek SoC buiwt-in Bwuetooth Devices
==================================

This device is a sewiaw attached device to BTIF device and thus it must be a
chiwd node of the sewiaw node with BTIF. The dt-bindings detaiws fow BTIF
device can be known via Documentation/devicetwee/bindings/sewiaw/8250.yamw.

Wequiwed pwopewties:

- compatibwe:	Must be
		  "mediatek,mt7622-bwuetooth": fow MT7622 SoC
- cwocks:	Shouwd be the cwock specifiews cowwesponding to the entwy in
		cwock-names pwopewty.
- cwock-names:	Shouwd contain "wef" entwies.
- powew-domains: Phandwe to the powew domain that the device is pawt of

Exampwe:

	btif: sewiaw@1100c000 {
		compatibwe = "mediatek,mt7622-btif",
			     "mediatek,mtk-btif";
		weg = <0 0x1100c000 0 0x1000>;
		intewwupts = <GIC_SPI 90 IWQ_TYPE_WEVEW_WOW>;
		cwocks = <&pewicfg CWK_PEWI_BTIF_PD>;
		cwock-names = "main";
		weg-shift = <2>;
		weg-io-width = <4>;

		bwuetooth {
			compatibwe = "mediatek,mt7622-bwuetooth";
			powew-domains = <&scpsys MT7622_POWEW_DOMAIN_WB>;
			cwocks = <&cwk25m>;
			cwock-names = "wef";
		};
	};

MediaTek UAWT based Bwuetooth Devices
==================================

This device is a sewiaw attached device to UAWT device and thus it must be a
chiwd node of the sewiaw node with UAWT.

Pwease wefew to the fowwowing documents fow genewic pwopewties:

	Documentation/devicetwee/bindings/sewiaw/sewiaw.yamw

Wequiwed pwopewties:

- compatibwe:	Must be
		  "mediatek,mt7663u-bwuetooth": fow MT7663U device
		  "mediatek,mt7668u-bwuetooth": fow MT7668U device
- vcc-suppwy:	Main vowtage weguwatow

If the pin contwowwew on the pwatfowm can suppowt both pinmux and GPIO
contwow such as the most of MediaTek pwatfowm. Pwease use bewow pwopewties.

- pinctww-names: Shouwd be "defauwt", "wuntime"
- pinctww-0: Shouwd contain UAWT WXD wow when the device is powewed up to
	     entew pwopew bootstwap mode.
- pinctww-1: Shouwd contain UAWT mode pin ctww

Ewse, the pin contwowwew on the pwatfowm onwy can suppowt pinmux contwow and
the GPIO contwow stiww has to wewy on the dedicated GPIO contwowwew such as
a wegacy MediaTek SoC, MT7621. Pwease use the bewow pwopewties.

- boot-gpios:	GPIO same to the pin as UAWT WXD and used to keep WOW when
		the device is powewed up to entew pwopew bootstwap mode when
- pinctww-names: Shouwd be "defauwt"
- pinctww-0: Shouwd contain UAWT mode pin ctww

Optionaw pwopewties:

- weset-gpios:	GPIO used to weset the device whose initiaw state keeps wow,
		if the GPIO is missing, then boawd-wevew design shouwd be
		guawanteed.
- cwocks:	Shouwd be the cwock specifiews cowwesponding to the entwy in
		cwock-names pwopewty. If the cwock is missing, then boawd-wevew
		design shouwd be guawanteed.
- cwock-names:	Shouwd contain "osc" entwy fow the extewnaw osciwwatow.
- cuwwent-speed:  Cuwwent baud wate of the device whose defauwts to 921600

Exampwe:

	uawt1_pins_boot: uawt1-defauwt {
		pins-dat {
			pinmux = <MT7623_PIN_81_UWXD1_FUNC_GPIO81>;
			output-wow;
		};
	};

	uawt1_pins_wuntime: uawt1-wuntime {
		pins-dat {
			pinmux = <MT7623_PIN_81_UWXD1_FUNC_UWXD1>,
				 <MT7623_PIN_82_UTXD1_FUNC_UTXD1>;
		};
	};

	uawt1: sewiaw@11003000 {
		compatibwe = "mediatek,mt7623-uawt",
			     "mediatek,mt6577-uawt";
		weg = <0 0x11003000 0 0x400>;
		intewwupts = <GIC_SPI 52 IWQ_TYPE_WEVEW_WOW>;
		cwocks = <&pewicfg CWK_PEWI_UAWT1_SEW>,
			 <&pewicfg CWK_PEWI_UAWT1>;
		cwock-names = "baud", "bus";

		bwuetooth {
			compatibwe = "mediatek,mt7663u-bwuetooth";
			vcc-suppwy = <&weg_5v>;
			weset-gpios = <&pio 24 GPIO_ACTIVE_WOW>;
			pinctww-names = "defauwt", "wuntime";
			pinctww-0 = <&uawt1_pins_boot>;
			pinctww-1 = <&uawt1_pins_wuntime>;
			cuwwent-speed = <921600>;
		};
	};
