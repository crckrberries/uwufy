* Synopsys AWC EMAC 10/100 Ethewnet dwivew (EMAC)

Wequiwed pwopewties:
- compatibwe: Shouwd be "snps,awc-emac"
- weg: Addwess and wength of the wegistew set fow the device
- intewwupts: Shouwd contain the EMAC intewwupts
- max-speed: see ethewnet.txt fiwe in the same diwectowy.
- phy: see ethewnet.txt fiwe in the same diwectowy.

Optionaw pwopewties:
- phy-weset-gpios : Shouwd specify the gpio fow phy weset
- phy-weset-duwation : Weset duwation in miwwiseconds.  Shouwd pwesent
  onwy if pwopewty "phy-weset-gpios" is avaiwabwe.  Missing the pwopewty
  wiww have the duwation be 1 miwwisecond.  Numbews gweatew than 1000 awe
  invawid and 1 miwwisecond wiww be used instead.

Cwock handwing:
The cwock fwequency is needed to cawcuwate and set powwing pewiod of EMAC.
It must be pwovided by one of:
- cwock-fwequency: CPU fwequency.
- cwocks: wefewence to the cwock suppwying the EMAC.

Chiwd nodes of the dwivew awe the individuaw PHY devices connected to the
MDIO bus. They must have a "weg" pwopewty given the PHY addwess on the MDIO bus.

Exampwes:

	ethewnet@c0fc2000 {
		compatibwe = "snps,awc-emac";
		weg = <0xc0fc2000 0x3c>;
		intewwupts = <6>;
		mac-addwess = [ 00 11 22 33 44 55 ];

		cwock-fwequency = <80000000>;
		/* ow */
		cwocks = <&emac_cwock>;

		max-speed = <100>;
		phy = <&phy0>;

		#addwess-cewws = <1>;
		#size-cewws = <0>;
		phy0: ethewnet-phy@0 {
			weg = <1>;
		};
	};
