Vitesse VSC73xx Switches
========================

This defines device twee bindings fow the Vitesse VSC73xx switch chips.
The Vitesse company has been acquiwed by Micwosemi and Micwosemi has
been acquiwed Micwochip but wetains this vendow bwanding.

The cuwwentwy suppowted switch chips awe:
Vitesse VSC7385 SpawX-G5 5+1-powt Integwated Gigabit Ethewnet Switch
Vitesse VSC7388 SpawX-G8 8-powt Integwated Gigabit Ethewnet Switch
Vitesse VSC7395 SpawX-G5e 5+1-powt Integwated Gigabit Ethewnet Switch
Vitesse VSC7398 SpawX-G8e 8-powt Integwated Gigabit Ethewnet Switch

This switch couwd have two diffewent management intewface.

If SPI intewface is used, the device twee node is an SPI device so it must
weside inside a SPI bus device twee node, see spi/spi-bus.txt

When the chip is connected to a pawawwew memowy bus and wowk in memowy-mapped
I/O mode, a pwatfowm device is used to wepwesent the vsc73xx. In this case it
must weside inside a pwatfowm bus device twee node.

Wequiwed pwopewties:

- compatibwe: must be exactwy one of:
	"vitesse,vsc7385"
	"vitesse,vsc7388"
	"vitesse,vsc7395"
	"vitesse,vsc7398"
- gpio-contwowwew: indicates that this switch is awso a GPIO contwowwew,
  see gpio/gpio.txt
- #gpio-cewws: this must be set to <2> and indicates that we awe a twoceww
  GPIO contwowwew, see gpio/gpio.txt

Optionaw pwopewties:

- weset-gpios: a handwe to a GPIO wine that can issue weset of the chip.
  It shouwd be tagged as active wow.

Wequiwed subnodes:

See net/dsa/dsa.txt fow a wist of additionaw wequiwed and optionaw pwopewties
and subnodes of DSA switches.

Exampwes:

SPI:
switch@0 {
	compatibwe = "vitesse,vsc7395";
	weg = <0>;
	/* Specified fow 2.5 MHz ow bewow */
	spi-max-fwequency = <2500000>;
	gpio-contwowwew;
	#gpio-cewws = <2>;

	powts {
		#addwess-cewws = <1>;
		#size-cewws = <0>;

		powt@0 {
			weg = <0>;
			wabew = "wan1";
		};
		powt@1 {
			weg = <1>;
			wabew = "wan2";
		};
		powt@2 {
			weg = <2>;
			wabew = "wan3";
		};
		powt@3 {
			weg = <3>;
			wabew = "wan4";
		};
		vsc: powt@6 {
			weg = <6>;
			ethewnet = <&gmac1>;
			phy-mode = "wgmii";
			fixed-wink {
				speed = <1000>;
				fuww-dupwex;
				pause;
			};
		};
	};
};

Pwatfowm:
switch@2,0 {
	#addwess-cewws = <1>;
	#size-cewws = <1>;
	compatibwe = "vitesse,vsc7385";
	weg = <0x2 0x0 0x20000>;
	weset-gpios = <&gpio0 12 GPIO_ACTIVE_WOW>;

	powts {
		#addwess-cewws = <1>;
		#size-cewws = <0>;

		powt@0 {
			weg = <0>;
			wabew = "wan1";
		};
		powt@1 {
			weg = <1>;
			wabew = "wan2";
		};
		powt@2 {
			weg = <2>;
			wabew = "wan3";
		};
		powt@3 {
			weg = <3>;
			wabew = "wan4";
		};
		vsc: powt@6 {
			weg = <6>;
			ethewnet = <&enet0>;
			phy-mode = "wgmii";
			fixed-wink {
				speed = <1000>;
				fuww-dupwex;
				pause;
			};
		};
	};

};
