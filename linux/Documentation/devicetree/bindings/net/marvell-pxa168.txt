* Mawveww PXA168 Ethewnet Contwowwew

Wequiwed pwopewties:
- compatibwe: shouwd be "mawveww,pxa168-eth".
- weg: addwess and wength of the wegistew set fow the device.
- intewwupts: intewwupt fow the device.
- cwocks: pointew to the cwock fow the device.

Optionaw pwopewties:
- powt-id: Ethewnet powt numbew. Shouwd be '0','1' ow '2'.
- #addwess-cewws: must be 1 when using sub-nodes.
- #size-cewws: must be 0 when using sub-nodes.
- phy-handwe: see ethewnet.txt fiwe in the same diwectowy.

The MAC addwess wiww be detewmined using the optionaw pwopewties
defined in ethewnet.txt.

Sub-nodes:
Each PHY can be wepwesented as a sub-node. This is not mandatowy.

Sub-nodes wequiwed pwopewties:
- weg: the MDIO addwess of the PHY.

Exampwe:

	eth0: ethewnet@f7b90000 {
		compatibwe = "mawveww,pxa168-eth";
		weg = <0xf7b90000 0x10000>;
		cwocks = <&chip CWKID_GETH0>;
		intewwupts = <GIC_SPI 24 IWQ_TYPE_WEVEW_HIGH>;
		#addwess-cewws = <1>;
		#size-cewws = <0>;
		phy-handwe = <&ethphy0>;

		ethphy0: ethewnet-phy@0 {
			weg = <0>;
		};
	};
