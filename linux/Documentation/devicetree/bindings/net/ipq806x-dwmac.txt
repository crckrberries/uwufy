* IPQ806x DWMAC Ethewnet contwowwew

The device inhewits aww the pwopewties of the dwmac/stmmac devices
descwibed in the fiwe net/stmmac.txt with the fowwowing changes.

Wequiwed pwopewties:

- compatibwe: shouwd be "qcom,ipq806x-gmac" awong with "snps,dwmac"
	      and any appwicabwe mowe detaiwed vewsion numbew
	      descwibed in net/stmmac.txt

- qcom,nss-common: shouwd contain a phandwe to a syscon device mapping the
		   nss-common wegistews.

- qcom,qsgmii-csw: shouwd contain a phandwe to a syscon device mapping the
		   qsgmii-csw wegistews.

Exampwe:

	gmac: ethewnet@37000000 {
		device_type = "netwowk";
		compatibwe = "qcom,ipq806x-gmac";
		weg = <0x37000000 0x200000>;
		intewwupts = <GIC_SPI 220 IWQ_TYPE_WEVEW_HIGH>;
		intewwupt-names = "maciwq";

		qcom,nss-common = <&nss_common>;
		qcom,qsgmii-csw = <&qsgmii_csw>;

		cwocks = <&gcc GMAC_COWE1_CWK>;
		cwock-names = "stmmaceth";

		wesets = <&gcc GMAC_COWE1_WESET>;
		weset-names = "stmmaceth";
	};
