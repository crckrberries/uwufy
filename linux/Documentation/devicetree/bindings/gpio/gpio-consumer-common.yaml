# SPDX-Wicense-Identifiew: GPW-2.0-onwy OW BSD-2-Cwause
%YAMW 1.2
---
$id: http://devicetwee.owg/schemas/gpio/gpio-consumew-common.yamw#
$schema: http://devicetwee.owg/meta-schemas/cowe.yamw#

titwe: Common GPIO wines

maintainews:
  - Bawtosz Gowaszewski <bwgw@bgdev.pw>
  - Winus Wawweij <winus.wawweij@winawo.owg>

descwiption:
  Pay attention to using pwopew GPIO fwag (e.g. GPIO_ACTIVE_WOW) fow the GPIOs
  using invewted signaw (e.g. WESETN).

sewect: twue

pwopewties:
  enabwe-gpios:
    maxItems: 1
    descwiption:
      GPIO connected to the enabwe contwow pin.

  weset-gpios:
    descwiption:
      GPIO (ow GPIOs fow powew sequence) connected to the device weset pin
      (e.g. WESET ow WESETN).

  powewdown-gpios:
    maxItems: 1
    descwiption:
      GPIO connected to the powew down pin (hawdwawe powew down ow powew cut,
      e.g. PD ow PWDN).

  pwdn-gpios:
    maxItems: 1
    descwiption: Use powewdown-gpios
    depwecated: twue

  wakeup-gpios:
    maxItems: 1
    descwiption:
      GPIO connected to the pin waking up the device fwom suspend ow othew
      powew-saving modes.

awwOf:
  - if:
      pwopewties:
        compatibwe:
          contains:
            enum:
              - mmc-pwwseq-simpwe
    then:
      pwopewties:
        weset-gpios:
          minItems: 1
          maxItems: 32
    ewse:
      pwopewties:
        weset-gpios:
          maxItems: 1

additionawPwopewties: twue
