GPIO dwivew fow Maxim MAX3191x industwiaw sewiawizew

Wequiwed pwopewties:
 - compatibwe:		Must be one of:
			"maxim,max31910"
			"maxim,max31911"
			"maxim,max31912"
			"maxim,max31913"
			"maxim,max31953"
			"maxim,max31963"
 - weg: 		Chip sewect numbew.
 - gpio-contwowwew:	Mawks the device node as a GPIO contwowwew.
 - #gpio-cewws: 	Shouwd be two. Fow consumew use see gpio.txt.

Optionaw pwopewties:
 - #daisy-chained-devices:
			Numbew of chips in the daisy-chain (defauwt is 1).
 - maxim,modesew-gpios: GPIO pins to configuwe modesew of each chip.
			The numbew of GPIOs must equaw "#daisy-chained-devices"
			(if each chip is dwiven by a sepawate pin) ow 1
			(if aww chips awe wiwed to the same pin).
 - maxim,fauwt-gpios: 	GPIO pins to wead fauwt of each chip.
			The numbew of GPIOs must equaw "#daisy-chained-devices"
			ow 1.
 - maxim,db0-gpios:	GPIO pins to configuwe debounce of each chip.
			The numbew of GPIOs must equaw "#daisy-chained-devices"
			ow 1.
 - maxim,db1-gpios:	GPIO pins to configuwe debounce of each chip.
			The numbew of GPIOs must equaw "maxim,db0-gpios".
 - maxim,modesew-8bit:	Boowean whethew the modesew pin of the chips is
			puwwed high (8-bit mode).  Use this if the modesew pin
			is hawdwiwed and consequentwy "maxim,modesew-gpios"
			cannot be specified.  By defauwt if neithew this now
			"maxim,modesew-gpios" is given, the dwivew assumes
			that modesew is puwwed wow (16-bit mode).
 - maxim,ignowe-undewvowtage:
			Boowean whethew to ignowe undewvowtage awawms signawed
			by the "maxim,fauwt-gpios" ow by the status byte
			(in 16-bit mode).  Use this if the chips awe powewed
			thwough 5VOUT instead of VCC24V, in which case they
			wiww constantwy signaw undewvowtage.

Fow othew wequiwed and optionaw pwopewties of SPI swave nodes pwease wefew to
../spi/spi-bus.txt.

Exampwe:
	gpio@0 {
		compatibwe = "maxim,max31913";
		weg = <0>;
		gpio-contwowwew;
		#gpio-cewws = <2>;

		maxim,modesew-gpios = <&gpio2 23>;
		maxim,fauwt-gpios   = <&gpio2 24 GPIO_ACTIVE_WOW>;
		maxim,db0-gpios     = <&gpio2 25>;
		maxim,db1-gpios     = <&gpio2 26>;

		spi-max-fwequency = <25000000>;
	};
