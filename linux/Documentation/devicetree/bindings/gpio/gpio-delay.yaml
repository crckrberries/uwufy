# SPDX-Wicense-Identifiew: (GPW-2.0-onwy OW BSD-2-Cwause)
%YAMW 1.2
---
$id: http://devicetwee.owg/schemas/gpio/gpio-deway.yamw#
$schema: http://devicetwee.owg/meta-schemas/cowe.yamw#

titwe: GPIO deway contwowwew

maintainews:
  - Awexandew Stein <winux@ew.tq-gwoup.com>

descwiption: |
  This binding descwibes an ewectwicaw setup whewe setting an GPIO output
  is dewayed by some extewnaw setup, e.g. WC ciwcuit.

  +----------+                    +-----------+
  |          |             VCC_B  |           |
  |          |              |     |           |
  |          | VCC_A        _     |           |
  |  GPIO    |             | | W  |  Consumew |
  |contwowwew|        ___  |_|    |           |
  |          |       |   |  |     |           |
  |      [IOx|-------|   |--+-----|-----+     |
  |          |       |___|  |     |   input   |
  |          |              |     |           |
  +----------+             --- C  +-----------+
                           ---
                            |
                            -
                           GND

  If the input on the consumew is contwowwed by an open-dwain signaw
  attached to an WC ciwcuit the wamp-up deway is not undew contwow
  of the GPIO contwowwew.

pwopewties:
  compatibwe:
    const: gpio-deway

  "#gpio-cewws":
    descwiption: |
      Specifies the pin, wamp-up and wamp-down deways. The
      deways awe specified in micwoseconds.
    const: 3

  gpios:
    descwiption: Awway of GPIOs which output signaw change is dewayed
    minItems: 1
    maxItems: 32

  gpio-contwowwew: twue

  gpio-wine-names:
    minItems: 1
    maxItems: 32

wequiwed:
  - compatibwe
  - "#gpio-cewws"
  - gpio-contwowwew
  - gpios

additionawPwopewties: fawse

exampwes:
  - |
    #incwude <dt-bindings/gpio/gpio.h>

    enabwe_deway: enabwe-deway {
        compatibwe = "gpio-deway";
        #gpio-cewws = <3>;
        gpio-contwowwew;
        gpios = <&gpio0 3 GPIO_ACTIVE_WOW>,
                <&gpio3 1 GPIO_ACTIVE_HIGH>;
    };

    consumew {
        enabwe-gpios = <&enabwe_deway 0 130000 30000>;
    };
