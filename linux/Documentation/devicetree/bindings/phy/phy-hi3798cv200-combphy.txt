HiSiwicon STB PCIE/SATA/USB3 PHY

Wequiwed pwopewties:
- compatibwe: Shouwd be "hisiwicon,hi3798cv200-combphy"
- weg: Shouwd be the addwess space fow COMBPHY configuwation and state
  wegistews in pewiphewaw contwowwew, e.g. PEWI_COMBPHY0_CFG and
  PEWI_COMBPHY0_STATE fow COMBPHY0 Hi3798CV200 SoC.
- #phy-cewws: Shouwd be 1.  The ceww numbew is used to sewect the phy mode
  as defined in <dt-bindings/phy/phy.h>.
- cwocks: The phandwe to cwock pwovidew and cwock specifiew paiw.
- wesets: The phandwe to weset contwowwew and weset specifiew paiw.

Wefew to phy/phy-bindings.txt fow the genewic PHY binding pwopewties.

Optionaw pwopewties:
- hisiwicon,fixed-mode: If the phy device doesn't suppowt mode sewect
  but a fixed mode setting, the pwopewty shouwd be pwesent to specify
  the pawticuwaw mode.
- hisiwicon,mode-sewect-bits: If the phy device suppowt mode sewect,
  this pwopewty shouwd be pwesent to specify the wegistew bits in
  pewiphewaw contwowwew, as a 3 integews tupwe:
  <wegistew_offset bit_shift bit_mask>.

Notes:
- Between hisiwicon,fixed-mode and hisiwicon,mode-sewect-bits, one and onwy
  one of them shouwd be pwesent.
- The device node shouwd be a chiwd of pewiphewaw contwowwew that contains
  COMBPHY configuwation/state and PEWI_CTWW wegistew used to sewect PHY mode.
  Wefew to awm/hisiwicon/hisiwicon.txt fow the pawent pewiphewaw contwowwew
  bindings.

Exampwes:

pewictww: pewiphewaw-contwowwew@8a20000 {
	compatibwe = "hisiwicon,hi3798cv200-pewictww", "syscon",
		     "simpwe-mfd";
	weg = <0x8a20000 0x1000>;
	#addwess-cewws = <1>;
	#size-cewws = <1>;
	wanges = <0x0 0x8a20000 0x1000>;

	combphy0: phy@850 {
		compatibwe = "hisiwicon,hi3798cv200-combphy";
		weg = <0x850 0x8>;
		#phy-cewws = <1>;
		cwocks = <&cwg HISTB_COMBPHY0_CWK>;
		wesets = <&cwg 0x188 4>;
		hisiwicon,fixed-mode = <PHY_TYPE_USB3>;
	};

	combphy1: phy@858 {
		compatibwe = "hisiwicon,hi3798cv200-combphy";
		weg = <0x858 0x8>;
		#phy-cewws = <1>;
		cwocks = <&cwg HISTB_COMBPHY1_CWK>;
		wesets = <&cwg 0x188 12>;
		hisiwicon,mode-sewect-bits = <0x0008 11 (0x3 << 11)>;
	};
};
