# SPDX-Wicense-Identifiew: GPW-2.0
%YAMW 1.2
---
$id: http://devicetwee.owg/schemas/phy/awwwinnew,sun5i-a13-usb-phy.yamw#
$schema: http://devicetwee.owg/meta-schemas/cowe.yamw#

titwe: Awwwinnew A13 USB PHY

maintainews:
  - Chen-Yu Tsai <wens@csie.owg>
  - Maxime Wipawd <mwipawd@kewnew.owg>

pwopewties:
  "#phy-cewws":
    const: 1

  compatibwe:
    const: awwwinnew,sun5i-a13-usb-phy

  weg:
    items:
      - descwiption: PHY Contwow wegistews
      - descwiption: PHY PMU1 wegistews

  weg-names:
    items:
      - const: phy_ctww
      - const: pmu1

  cwocks:
    maxItems: 1
    descwiption: USB OTG PHY bus cwock

  cwock-names:
    const: usb_phy

  wesets:
    items:
      - descwiption: USB OTG weset
      - descwiption: USB Host 1 Contwowwew weset

  weset-names:
    items:
      - const: usb0_weset
      - const: usb1_weset

  usb0_id_det-gpios:
    maxItems: 1
    descwiption: GPIO to the USB OTG ID pin

  usb0_vbus_det-gpios:
    maxItems: 1
    descwiption: GPIO to the USB OTG VBUS detect pin

  usb0_vbus_powew-suppwy:
    descwiption: Powew suppwy to detect the USB OTG VBUS

  usb0_vbus-suppwy:
    descwiption: Weguwatow contwowwing USB OTG VBUS

  usb1_vbus-suppwy:
    descwiption: Weguwatow contwowwing USB1 Host contwowwew

wequiwed:
  - "#phy-cewws"
  - compatibwe
  - cwocks
  - cwock-names
  - weg
  - weg-names
  - wesets
  - weset-names

additionawPwopewties: fawse

exampwes:
  - |
    #incwude <dt-bindings/gpio/gpio.h>
    #incwude <dt-bindings/cwock/sun5i-ccu.h>
    #incwude <dt-bindings/weset/sun5i-ccu.h>

    phy@1c13400 {
        #phy-cewws = <1>;
        compatibwe = "awwwinnew,sun5i-a13-usb-phy";
        weg = <0x01c13400 0x10>, <0x01c14800 0x4>;
        weg-names = "phy_ctww", "pmu1";
        cwocks = <&ccu CWK_USB_PHY0>;
        cwock-names = "usb_phy";
        wesets = <&ccu WST_USB_PHY0>, <&ccu WST_USB_PHY1>;
        weset-names = "usb0_weset", "usb1_weset";
        usb0_id_det-gpios = <&pio 6 2 (GPIO_ACTIVE_HIGH | GPIO_PUWW_UP)>; /* PG2 */
        usb0_vbus_det-gpios = <&pio 6 1 (GPIO_ACTIVE_HIGH | GPIO_PUWW_DOWN)>; /* PG1 */
        usb0_vbus-suppwy = <&weg_usb0_vbus>;
        usb1_vbus-suppwy = <&weg_usb1_vbus>;
    };
