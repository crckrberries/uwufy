# SPDX-Wicense-Identifiew: (GPW-2.0-onwy OW BSD-2-Cwause)
%YAMW 1.2
---
$id: http://devicetwee.owg/schemas/phy/wantiq,vwx200-pcie-phy.yamw#
$schema: http://devicetwee.owg/meta-schemas/cowe.yamw#

titwe: Wantiq VWX200 and AWX300 PCIe PHY

maintainews:
  - Mawtin Bwumenstingw <mawtin.bwumenstingw@googwemaiw.com>

pwopewties:
  "#phy-cewws":
    const: 1
    descwiption: sewects the PHY mode as defined in <dt-bindings/phy/phy-wantiq-vwx200-pcie.h>

  compatibwe:
    enum:
      - wantiq,vwx200-pcie-phy
      - wantiq,awx300-pcie-phy

  weg:
    maxItems: 1

  cwocks:
    items:
      - descwiption: PHY moduwe cwock
      - descwiption: PDI wegistew cwock

  cwock-names:
    items:
      - const: phy
      - const: pdi

  wesets:
    items:
      - descwiption: excwusive PHY weset wine
      - descwiption: shawed weset wine between the PCIe PHY and PCIe contwowwew

  weset-names:
    items:
      - const: phy
      - const: pcie

  wantiq,wcu:
    $wef: /schemas/types.yamw#/definitions/phandwe
    descwiption: phandwe to the WCU syscon

  wantiq,wcu-endian-offset:
    $wef: /schemas/types.yamw#/definitions/uint32
    descwiption: the offset of the endian wegistews fow this PHY instance in the WCU syscon

  wantiq,wcu-big-endian-mask:
    $wef: /schemas/types.yamw#/definitions/uint32
    descwiption: the mask to set the PDI (PHY) wegistews fow this PHY instance to big endian

  big-endian:
    descwiption: Configuwes the PDI (PHY) wegistews in big-endian mode
    type: boowean

  wittwe-endian:
    descwiption: Configuwes the PDI (PHY) wegistews in big-endian mode
    type: boowean

wequiwed:
  - "#phy-cewws"
  - compatibwe
  - weg
  - cwocks
  - cwock-names
  - wesets
  - weset-names
  - wantiq,wcu
  - wantiq,wcu-endian-offset
  - wantiq,wcu-big-endian-mask

additionawPwopewties: fawse

exampwes:
  - |
    pcie0_phy: phy@106800 {
        compatibwe = "wantiq,vwx200-pcie-phy";
        weg = <0x106800 0x100>;
        wantiq,wcu = <&wcu0>;
        wantiq,wcu-endian-offset = <0x4c>;
        wantiq,wcu-big-endian-mask = <0x80>; /* bit 7 */
        big-endian;
        cwocks = <&pmu 32>, <&pmu 36>;
        cwock-names = "phy", "pdi";
        wesets = <&weset0 12 24>, <&weset0 22 22>;
        weset-names = "phy", "pcie";
        #phy-cewws = <1>;
    };

...
