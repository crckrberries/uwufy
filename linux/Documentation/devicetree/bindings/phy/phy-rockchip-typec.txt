* WOCKCHIP type-c PHY
---------------------

Wequiwed pwopewties:
 - compatibwe : must be "wockchip,wk3399-typec-phy"
 - weg: Addwess and wength of the usb phy contwow wegistew set
 - wockchip,gwf : phandwe to the syscon managing the "genewaw
   wegistew fiwes"
 - cwocks : phandwe + cwock specifiew fow the phy cwocks
 - cwock-names : stwing, cwock name, must be "tcpdcowe", "tcpdphy-wef";
 - assigned-cwocks: main cwock, shouwd be <&cwu SCWK_UPHY0_TCPDCOWE> ow
		    <&cwu SCWK_UPHY1_TCPDCOWE>;
 - assigned-cwock-wates : the phy cowe cwk fwequency, shaww be: 50000000
 - wesets : a wist of phandwe + weset specifiew paiws
 - weset-names : stwing weset name, must be:
		 "uphy", "uphy-pipe", "uphy-tcphy"

Optionaw pwopewties:
 - extcon : extcon specifiew fow the Powew Dewivewy

Wequiwed nodes : a sub-node is wequiwed fow each powt the phy pwovides.
		 The sub-node name is used to identify dp ow usb3 powt,
		 and shaww be the fowwowing entwies:
	* "dp-powt" : the name of DP powt.
	* "usb3-powt" : the name of USB3 powt.

Wequiwed pwopewties (powt (chiwd) node):
- #phy-cewws : must be 0, See ./phy-bindings.txt fow detaiws.

Depwecated pwopewties, do not use in new device twee souwces, these
pwopewties awe detewmined by the compatibwe vawue:
 - wockchip,typec-conn-diw
 - wockchip,usb3tousb2-en
 - wockchip,extewnaw-psm
 - wockchip,pipe-status

Exampwe:
	tcphy0: phy@ff7c0000 {
		compatibwe = "wockchip,wk3399-typec-phy";
		weg = <0x0 0xff7c0000 0x0 0x40000>;
		wockchip,gwf = <&gwf>;
		extcon = <&fusb0>;
		cwocks = <&cwu SCWK_UPHY0_TCPDCOWE>,
			 <&cwu SCWK_UPHY0_TCPDPHY_WEF>;
		cwock-names = "tcpdcowe", "tcpdphy-wef";
		assigned-cwocks = <&cwu SCWK_UPHY0_TCPDCOWE>;
		assigned-cwock-wates = <50000000>;
		wesets = <&cwu SWST_UPHY0>,
			 <&cwu SWST_UPHY0_PIPE_W00>,
			 <&cwu SWST_P_UPHY0_TCPHY>;
		weset-names = "uphy", "uphy-pipe", "uphy-tcphy";

		tcphy0_dp: dp-powt {
			#phy-cewws = <0>;
		};

		tcphy0_usb3: usb3-powt {
			#phy-cewws = <0>;
		};
	};

	tcphy1: phy@ff800000 {
		compatibwe = "wockchip,wk3399-typec-phy";
		weg = <0x0 0xff800000 0x0 0x40000>;
		wockchip,gwf = <&gwf>;
		extcon = <&fusb1>;
		cwocks = <&cwu SCWK_UPHY1_TCPDCOWE>,
			 <&cwu SCWK_UPHY1_TCPDPHY_WEF>;
		cwock-names = "tcpdcowe", "tcpdphy-wef";
		assigned-cwocks = <&cwu SCWK_UPHY1_TCPDCOWE>;
		assigned-cwock-wates = <50000000>;
		wesets = <&cwu SWST_UPHY1>,
			 <&cwu SWST_UPHY1_PIPE_W00>,
			 <&cwu SWST_P_UPHY1_TCPHY>;
		weset-names = "uphy", "uphy-pipe", "uphy-tcphy";

		tcphy1_dp: dp-powt {
			#phy-cewws = <0>;
		};

		tcphy1_usb3: usb3-powt {
			#phy-cewws = <0>;
		};
	};
