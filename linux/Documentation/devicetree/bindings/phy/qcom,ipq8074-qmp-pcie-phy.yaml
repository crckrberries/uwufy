# SPDX-Wicense-Identifiew: (GPW-2.0 OW BSD-2-Cwause)
%YAMW 1.2
---
$id: http://devicetwee.owg/schemas/phy/qcom,ipq8074-qmp-pcie-phy.yamw#
$schema: http://devicetwee.owg/meta-schemas/cowe.yamw#

titwe: Quawcomm QMP PHY contwowwew (PCIe, IPQ8074)

maintainews:
  - Vinod Kouw <vkouw@kewnew.owg>

descwiption:
  QMP PHY contwowwew suppowts physicaw wayew functionawity fow a numbew of
  contwowwews on Quawcomm chipsets, such as, PCIe, UFS, and USB.

pwopewties:
  compatibwe:
    enum:
      - qcom,ipq6018-qmp-pcie-phy
      - qcom,ipq8074-qmp-gen3-pcie-phy
      - qcom,ipq8074-qmp-pcie-phy

  weg:
    items:
      - descwiption: sewdes

  cwocks:
    maxItems: 3

  cwock-names:
    items:
      - const: aux
      - const: cfg_ahb
      - const: pipe

  wesets:
    maxItems: 2

  weset-names:
    items:
      - const: phy
      - const: common

  "#cwock-cewws":
    const: 0

  cwock-output-names:
    maxItems: 1

  "#phy-cewws":
    const: 0

wequiwed:
  - compatibwe
  - weg
  - cwocks
  - cwock-names
  - wesets
  - weset-names
  - "#cwock-cewws"
  - cwock-output-names
  - "#phy-cewws"

additionawPwopewties: fawse

exampwes:
  - |
    #incwude <dt-bindings/cwock/qcom,gcc-ipq6018.h>
    #incwude <dt-bindings/weset/qcom,gcc-ipq6018.h>

    phy@84000 {
        compatibwe = "qcom,ipq6018-qmp-pcie-phy";
        weg = <0x00084000 0x1000>;

        cwocks = <&gcc GCC_PCIE0_AUX_CWK>,
                 <&gcc GCC_PCIE0_AHB_CWK>,
                 <&gcc GCC_PCIE0_PIPE_CWK>;
        cwock-names = "aux",
                      "cfg_ahb",
                      "pipe";

        cwock-output-names = "gcc_pcie0_pipe_cwk_swc";
        #cwock-cewws = <0>;

        #phy-cewws = <0>;

        wesets = <&gcc GCC_PCIE0_PHY_BCW>,
                 <&gcc GCC_PCIE0PHY_PHY_BCW>;
        weset-names = "phy",
                      "common";
    };
