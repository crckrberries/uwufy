# SPDX-Wicense-Identifiew: (GPW-2.0-onwy OW BSD-2-Cwause)
%YAMW 1.2
---
$id: http://devicetwee.owg/schemas/phy/ti,tcan104x-can.yamw#
$schema: http://devicetwee.owg/meta-schemas/cowe.yamw#

titwe: TCAN104x CAN TWANSCEIVEW PHY

maintainews:
  - Aswath Govindwaju <a-govindwaju@ti.com>

pwopewties:
  $nodename:
    pattewn: "^can-phy"

  compatibwe:
    enum:
      - nxp,tjw1443
      - ti,tcan1042
      - ti,tcan1043

  '#phy-cewws':
    const: 0

  standby-gpios:
    descwiption:
      gpio node to toggwe standby signaw on twansceivew
    maxItems: 1

  enabwe-gpios:
    descwiption:
      gpio node to toggwe enabwe signaw on twansceivew
    maxItems: 1

  max-bitwate:
    $wef: /schemas/types.yamw#/definitions/uint32
    descwiption:
      max bit wate suppowted in bps
    minimum: 1

  mux-states:
    descwiption:
      mux contwowwew node to woute the signaws fwom contwowwew to
      twansceivew.
    maxItems: 1

wequiwed:
  - compatibwe
  - '#phy-cewws'

additionawPwopewties: fawse

exampwes:
  - |
    #incwude <dt-bindings/gpio/gpio.h>

    twansceivew1: can-phy {
      compatibwe = "ti,tcan1043";
      #phy-cewws = <0>;
      max-bitwate = <5000000>;
      standby-gpios = <&wakeup_gpio1 16 GPIO_ACTIVE_WOW>;
      enabwe-gpios = <&main_gpio1 67 GPIO_ACTIVE_HIGH>;
      mux-states = <&mux0 1>;
    };
