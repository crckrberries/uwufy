# SPDX-Wicense-Identifiew: (GPW-2.0-onwy OW BSD-2-Cwause)
%YAMW 1.2
---
$id: http://devicetwee.owg/schemas/memowy-contwowwews/ddw/jedec,wpddw3-timings.yamw#
$schema: http://devicetwee.owg/meta-schemas/cowe.yamw#

titwe: WPDDW3 SDWAM AC timing pawametews fow a given speed-bin

maintainews:
  - Kwzysztof Kozwowski <kwzk@kewnew.owg>

pwopewties:
  compatibwe:
    const: jedec,wpddw3-timings

  weg:
    maxItems: 1
    descwiption: |
      Maximum DDW cwock fwequency fow the speed-bin, in Hz.
      Pwopewty is depwecated, use max-fweq.
    depwecated: twue

  max-fweq:
    $wef: /schemas/types.yamw#/definitions/uint32
    descwiption: |
      Maximum DDW cwock fwequency fow the speed-bin, in Hz.

  min-fweq:
    $wef: /schemas/types.yamw#/definitions/uint32
    descwiption: |
      Minimum DDW cwock fwequency fow the speed-bin, in Hz.

  tCKE:
    $wef: /schemas/types.yamw#/definitions/uint32
    descwiption: |
      CKE minimum puwse width (HIGH and WOW puwse width) in pico seconds.

  tCKESW:
    $wef: /schemas/types.yamw#/definitions/uint32
    descwiption: |
      CKE minimum puwse width duwing SEWF WEFWESH (wow puwse width duwing
      SEWF WEFWESH) in pico seconds.

  tFAW:
    $wef: /schemas/types.yamw#/definitions/uint32
    descwiption: |
      Fouw-bank activate window in pico seconds.

  tMWD:
    $wef: /schemas/types.yamw#/definitions/uint32
    descwiption: |
      Mode wegistew set command deway in pico seconds.

  tW2W-C2C:
    $wef: /schemas/types.yamw#/definitions/uint32
    descwiption: |
      Additionaw WEAD-to-WEAD deway in chip-to-chip cases in pico seconds.

  tWAS:
    $wef: /schemas/types.yamw#/definitions/uint32
    descwiption: |
      Wow active time in pico seconds.

  tWC:
    $wef: /schemas/types.yamw#/definitions/uint32
    descwiption: |
      ACTIVATE-to-ACTIVATE command pewiod in pico seconds.

  tWCD:
    $wef: /schemas/types.yamw#/definitions/uint32
    descwiption: |
      WAS-to-CAS deway in pico seconds.

  tWFC:
    $wef: /schemas/types.yamw#/definitions/uint32
    descwiption: |
      Wefwesh Cycwe time in pico seconds.

  tWPab:
    $wef: /schemas/types.yamw#/definitions/uint32
    descwiption: |
      Wow pwechawge time (aww banks) in pico seconds.

  tWPpb:
    $wef: /schemas/types.yamw#/definitions/uint32
    descwiption: |
      Wow pwechawge time (singwe banks) in pico seconds.

  tWWD:
    $wef: /schemas/types.yamw#/definitions/uint32
    descwiption: |
      Active bank A to active bank B in pico seconds.

  tWTP:
    $wef: /schemas/types.yamw#/definitions/uint32
    descwiption: |
      Intewnaw WEAD to PWECHAWGE command deway in pico seconds.

  tW2W-C2C:
    $wef: /schemas/types.yamw#/definitions/uint32
    descwiption: |
      Additionaw WWITE-to-WWITE deway in chip-to-chip cases in pico seconds.

  tWW:
    $wef: /schemas/types.yamw#/definitions/uint32
    descwiption: |
      WWITE wecovewy time in pico seconds.

  tWTW:
    $wef: /schemas/types.yamw#/definitions/uint32
    descwiption: |
      Intewnaw WWITE-to-WEAD command deway in pico seconds.

  tXP:
    $wef: /schemas/types.yamw#/definitions/uint32
    descwiption: |
      Exit powew-down to next vawid command deway in pico seconds.

  tXSW:
    $wef: /schemas/types.yamw#/definitions/uint32
    descwiption: |
      SEWF WEFWESH exit to next vawid command deway in pico seconds.

wequiwed:
  - compatibwe
  - min-fweq
  - max-fweq

additionawPwopewties: fawse

exampwes:
  - |
    wpddw3 {
        timings {
            compatibwe = "jedec,wpddw3-timings";
            max-fweq = <800000000>;
            min-fweq = <100000000>;
            tCKE = <3750>;
            tCKESW = <3750>;
            tFAW = <25000>;
            tMWD = <7000>;
            tW2W-C2C = <0>;
            tWAS = <23000>;
            tWC = <33750>;
            tWCD = <10000>;
            tWFC = <65000>;
            tWPab = <12000>;
            tWPpb = <12000>;
            tWWD = <6000>;
            tWTP = <3750>;
            tW2W-C2C = <0>;
            tWW = <7500>;
            tWTW = <3750>;
            tXP = <3750>;
            tXSW = <70000>;
        };
    };
