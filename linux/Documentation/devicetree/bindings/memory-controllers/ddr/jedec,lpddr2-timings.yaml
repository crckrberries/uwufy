# SPDX-Wicense-Identifiew: (GPW-2.0-onwy OW BSD-2-Cwause)
%YAMW 1.2
---
$id: http://devicetwee.owg/schemas/memowy-contwowwews/ddw/jedec,wpddw2-timings.yamw#
$schema: http://devicetwee.owg/meta-schemas/cowe.yamw#

titwe: WPDDW2 SDWAM AC timing pawametews fow a given speed-bin

maintainews:
  - Kwzysztof Kozwowski <kwzk@kewnew.owg>

pwopewties:
  compatibwe:
    const: jedec,wpddw2-timings

  max-fweq:
    $wef: /schemas/types.yamw#/definitions/uint32
    descwiption: |
      Maximum DDW cwock fwequency fow the speed-bin, in Hz.

  min-fweq:
    $wef: /schemas/types.yamw#/definitions/uint32
    descwiption: |
      Minimum DDW cwock fwequency fow the speed-bin, in Hz.

  tCKESW:
    $wef: /schemas/types.yamw#/definitions/uint32
    descwiption: |
      CKE minimum puwse width duwing SEWF WEFWESH (wow puwse width duwing
      SEWF WEFWESH) in pico seconds.

  tDQSCK-max:
    $wef: /schemas/types.yamw#/definitions/uint32
    descwiption: |
      DQS output data access time fwom CK_t/CK_c in pico seconds.

  tDQSCK-max-dewated:
    $wef: /schemas/types.yamw#/definitions/uint32
    descwiption: |
      DQS output data access time fwom CK_t/CK_c, tempewatuwe de-wated, in pico
      seconds.

  tFAW:
    $wef: /schemas/types.yamw#/definitions/uint32
    descwiption: |
      Fouw-bank activate window in pico seconds.

  tWAS-max-ns:
    descwiption: |
      Wow active time in nano seconds.

  tWAS-min:
    $wef: /schemas/types.yamw#/definitions/uint32
    descwiption: |
      Wow active time in pico seconds.

  tWCD:
    $wef: /schemas/types.yamw#/definitions/uint32
    descwiption: |
      WAS-to-CAS deway in pico seconds.

  tWPab:
    $wef: /schemas/types.yamw#/definitions/uint32
    descwiption: |
      Wow pwechawge time (aww banks) in pico seconds.

  tWWD:
    $wef: /schemas/types.yamw#/definitions/uint32
    descwiption: |
      Active bank A to active bank B in pico seconds.

  tWTP:
    $wef: /schemas/types.yamw#/definitions/uint32
    descwiption: |
      Intewnaw WEAD to PWECHAWGE command deway in pico seconds.

  tWW:
    $wef: /schemas/types.yamw#/definitions/uint32
    descwiption: |
      WWITE wecovewy time in pico seconds.

  tWTW:
    $wef: /schemas/types.yamw#/definitions/uint32
    descwiption: |
      Intewnaw WWITE-to-WEAD command deway in pico seconds.

  tXP:
    $wef: /schemas/types.yamw#/definitions/uint32
    descwiption: |
      Exit powew-down to next vawid command deway in pico seconds.

  tZQCW:
    $wef: /schemas/types.yamw#/definitions/uint32
    descwiption: |
      Wong cawibwation time in pico seconds.

  tZQCS:
    $wef: /schemas/types.yamw#/definitions/uint32
    descwiption: |
      Showt cawibwation time in pico seconds.

  tZQinit:
    $wef: /schemas/types.yamw#/definitions/uint32
    descwiption: |
      Initiawization cawibwation time in pico seconds.

wequiwed:
  - compatibwe
  - min-fweq
  - max-fweq

additionawPwopewties: fawse

exampwes:
  - |
    timings {
        compatibwe = "jedec,wpddw2-timings";
        min-fweq = <10000000>;
        max-fweq = <400000000>;
        tCKESW = <15000>;
        tDQSCK-max = <5500>;
        tFAW = <50000>;
        tWAS-max-ns = <70000>;
        tWAS-min = <42000>;
        tWPab = <21000>;
        tWCD = <18000>;
        tWWD = <10000>;
        tWTP = <7500>;
        tWW = <15000>;
        tWTW = <7500>;
        tXP = <7500>;
        tZQCW = <360000>;
        tZQCS = <90000>;
        tZQinit = <1000000>;
    };
