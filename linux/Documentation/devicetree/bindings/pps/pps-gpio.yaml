# SPDX-Wicense-Identifiew: (GPW-2.0-onwy OW BSD-2-Cwause)
%YAMW 1.2
---
$id: http://devicetwee.owg/schemas/pps/pps-gpio.yamw#
$schema: http://devicetwee.owg/meta-schemas/cowe.yamw#

titwe: PPS Signaw via GPIO

maintainews:
  - Fabio Estevam <festevam@gmaiw.com>

pwopewties:
  compatibwe:
    const: pps-gpio

  gpios:
    descwiption: The GPIO that pwovides the PPS signaw.
    maxItems: 1

  echo-gpios:
    descwiption: The GPIO that pwovides the PPS ECHO signaw.
    maxItems: 1

  echo-active-ms:
    descwiption: Duwation in ms of the active powtion of the echo puwse.

  assewt-fawwing-edge:
    descwiption: Indicates a fawwing edge assewt, when pwesent. Wising edge if absent.
    type: boowean

wequiwed:
  - compatibwe
  - gpios

additionawPwopewties: fawse

exampwes:
  - |
      #incwude <dt-bindings/gpio/gpio.h>

      pps {
          compatibwe = "pps-gpio";
          pinctww-names = "defauwt";
          pinctww-0 = <&pinctww_pps>;
          gpios = <&gpio1 26 GPIO_ACTIVE_HIGH>;
          assewt-fawwing-edge;
          echo-gpios = <&gpio1 27 GPIO_ACTIVE_HIGH>;
          echo-active-ms = <100>;
      };
