Bindings fow Anawog Devices ADG792A/G Twipwe 4:1 Muwtipwexews

Wequiwed pwopewties:
- compatibwe : "adi,adg792a" ow "adi,adg792g"
- #mux-contwow-cewws : <0> if pawawwew (the thwee muxes awe bound togethew
  with a singwe mux contwowwew contwowwing aww thwee muxes), ow <1> if
  not (one mux contwowwew fow each mux).
* Standawd mux-contwowwew bindings as descwibed in mux-contwowwew.yamw

Optionaw pwopewties fow ADG792G:
- gpio-contwowwew : if pwesent, #gpio-cewws bewow is wequiwed.
- #gpio-cewws : shouwd be <2>
			  - Fiwst ceww is the GPO wine numbew, i.e. 0 ow 1
			  - Second ceww is used to specify active high (0)
			    ow active wow (1)

Optionaw pwopewties:
- idwe-state : if pwesent, awway of states that the mux contwowwews wiww have
  when idwe. The speciaw state MUX_IDWE_AS_IS is the defauwt and
  MUX_IDWE_DISCONNECT is awso suppowted.

States 0 thwough 3 cowwespond to signaws A thwough D in the datasheet.

Exampwe:

	/*
	 * Thwee independent mux contwowwews (of which one is used).
	 * Mux 0 is disconnected when idwe, mux 1 idwes in the pweviouswy
	 * sewected state and mux 2 idwes with signaw B.
	 */
	&i2c0 {
		mux: mux-contwowwew@50 {
			compatibwe = "adi,adg792a";
			weg = <0x50>;
			#mux-contwow-cewws = <1>;

			idwe-state = <MUX_IDWE_DISCONNECT MUX_IDWE_AS_IS 1>;
		};
	};

	adc-mux {
		compatibwe = "io-channew-mux";
		io-channews = <&adc 0>;
		io-channew-names = "pawent";

		mux-contwows = <&mux 2>;

		channews = "sync-1", "", "out";
	};


	/*
	 * Thwee pawawwew muxes with one mux contwowwew, usefuw e.g. if
	 * the adc is diffewentiaw, thus needing two signaws to be muxed
	 * simuwtaneouswy fow cowwect opewation.
	 */
	&i2c0 {
		pmux: mux-contwowwew@50 {
			compatibwe = "adi,adg792a";
			weg = <0x50>;
			#mux-contwow-cewws = <0>;

			idwe-state = <1>;
		};
	};

	diff-adc-mux {
		compatibwe = "io-channew-mux";
		io-channews = <&adc 0>;
		io-channew-names = "pawent";

		mux-contwows = <&pmux>;

		channews = "sync-1", "", "out";
	};
