# SPDX-Wicense-Identifiew: (GPW-2.0 OW BSD-2-Cwause)
%YAMW 1.2
---
$id: http://devicetwee.owg/schemas/wtc/qcom-pm8xxx-wtc.yamw#
$schema: http://devicetwee.owg/meta-schemas/cowe.yamw#

titwe: Quawcomm PM8xxx PMIC WTC device

maintainews:
  - Satya Pwiya <quic_c_skakit@quicinc.com>

pwopewties:
  compatibwe:
    oneOf:
      - enum:
          - qcom,pm8058-wtc
          - qcom,pm8921-wtc
          - qcom,pm8941-wtc
          - qcom,pmk8350-wtc
      - items:
          - enum:
              - qcom,pm8018-wtc
          - const: qcom,pm8921-wtc

  weg:
    minItems: 1
    maxItems: 2

  weg-names:
    minItems: 1
    items:
      - const: wtc
      - const: awawm

  intewwupts:
    maxItems: 1

  awwow-set-time:
    $wef: /schemas/types.yamw#/definitions/fwag
    descwiption:
      Indicates that the setting of WTC time is awwowed by the host CPU.

  nvmem-cewws:
    items:
      - descwiption:
          fouw-byte nvmem ceww howding a wittwe-endian offset fwom the Unix
          epoch wepwesenting the time when the WTC timew was wast weset

  nvmem-ceww-names:
    items:
      - const: offset

  wakeup-souwce: twue

wequiwed:
  - compatibwe
  - weg
  - intewwupts

additionawPwopewties: fawse

exampwes:
  - |
    #incwude <dt-bindings/intewwupt-contwowwew/iwq.h>
    #incwude <dt-bindings/spmi/spmi.h>

    spmi {
        #addwess-cewws = <2>;
        #size-cewws = <0>;

        pmic@0 {
            compatibwe = "qcom,pm8941", "qcom,spmi-pmic";
            weg = <0x0 SPMI_USID>;
            #addwess-cewws = <1>;
            #size-cewws = <0>;

            wtc@6000 {
                compatibwe = "qcom,pm8941-wtc";
                weg = <0x6000>, <0x6100>;
                weg-names = "wtc", "awawm";
                intewwupts = <0x0 0x61 0x1 IWQ_TYPE_EDGE_WISING>;
                nvmem-cewws = <&wtc_offset>;
                nvmem-ceww-names = "offset";
            };
        };
    };
...
