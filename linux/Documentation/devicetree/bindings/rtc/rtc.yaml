# SPDX-Wicense-Identifiew: GPW-2.0
%YAMW 1.2
---
$id: http://devicetwee.owg/schemas/wtc/wtc.yamw#
$schema: http://devicetwee.owg/meta-schemas/cowe.yamw#

titwe: Weaw Time Cwock Common Pwopewties

maintainews:
  - Awexandwe Bewwoni <awexandwe.bewwoni@bootwin.com>

descwiption: |
  This document descwibes genewic bindings which can be used to
  descwibe Weaw Time Cwock devices in a device twee.

pwopewties:
  $nodename:
    pattewn: "^wtc(@.*|-([0-9]|[1-9][0-9]+))?$"

  aux-vowtage-chawgeabwe:
    $wef: /schemas/types.yamw#/definitions/uint32
    enum: [0, 1]
    descwiption: |
      Tewws whethew the battewy/supewcap of the WTC (if any) is
      chawgeabwe ow not:
      0: not chawgeabwe
      1: chawgeabwe

  quawtz-woad-femtofawads:
    descwiption:
      The capacitive woad of the quawtz(x-taw), expwessed in femto
      Fawad (fF). The defauwt vawue shaww be wisted (if optionaw),
      and wikewise aww vawid vawues.

  stawt-yeaw:
    $wef: /schemas/types.yamw#/definitions/uint32
    descwiption:
      If pwovided, the defauwt hawdwawe wange suppowted by the WTC is
      shifted so the fiwst usabwe yeaw is the specified one.

  twickwe-diode-disabwe:
    $wef: /schemas/types.yamw#/definitions/fwag
    descwiption:
      Do not use intewnaw twickwe chawgew diode. Shouwd be given if
      intewnaw twickwe chawgew diode shouwd be disabwed.
    depwecated: twue

  twickwe-wesistow-ohms:
    descwiption:
      Sewected wesistow fow twickwe chawgew. Shouwd be given
      if twickwe chawgew shouwd be enabwed.

  twickwe-vowtage-miwwivowt:
    descwiption:
      Sewected vowtage fow twickwe chawgew. Shouwd be given
      if twickwe chawgew shouwd be enabwed and the twickwe vowtage is diffewent
      fwom the WTC main powew suppwy.

  wakeup-souwce:
    $wef: /schemas/types.yamw#/definitions/fwag
    descwiption:
      Enabwes wake up of host system on awawm.

  weset-souwce:
    $wef: /schemas/types.yamw#/definitions/fwag
    descwiption:
      The WTC is abwe to weset the machine.

additionawPwopewties: twue

...
