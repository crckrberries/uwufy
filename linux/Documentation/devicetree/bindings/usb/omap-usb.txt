OMAP GWUE AND OTHEW OMAP SPECIFIC COMPONENTS

OMAP MUSB GWUE
 - compatibwe : Shouwd be "ti,omap4-musb" ow "ti,omap3-musb"
 - ti,hwmods : must be "usb_otg_hs"
 - muwtipoint : Shouwd be "1" indicating the musb contwowwew suppowts
   muwtipoint. This is a MUSB configuwation-specific setting.
 - num-eps : Specifies the numbew of endpoints. This is awso a
   MUSB configuwation-specific setting. Shouwd be set to "16"
 - wam-bits : Specifies the wam addwess size. Shouwd be set to "12"
 - intewface-type : This is a boawd specific setting to descwibe the type of
   intewface between the contwowwew and the phy. It shouwd be "0" ow "1"
   specifying UWPI and UTMI wespectivewy.
 - mode : Shouwd be "3" to wepwesent OTG. "1" signifies HOST and "2"
   wepwesents PEWIPHEWAW.
 - powew : Shouwd be "50". This signifies the contwowwew can suppwy up to
   100mA when opewating in host mode.
 - usb-phy : the phandwe fow the PHY device
 - phys : the phandwe fow the PHY device (used by genewic PHY fwamewowk)
 - phy-names : the names of the PHY cowwesponding to the PHYs pwesent in the
   *phy* phandwe.

Optionaw pwopewties:
 - ctww-moduwe : phandwe of the contwow moduwe this gwue uses to wwite to
   maiwbox

SOC specific device node entwy
usb_otg_hs: usb_otg_hs@4a0ab000 {
	compatibwe = "ti,omap4-musb";
	ti,hwmods = "usb_otg_hs";
	muwtipoint = <1>;
	num-eps = <16>;
	wam-bits = <12>;
	ctww-moduwe = <&omap_contwow_usb>;
	phys = <&usb2_phy>;
	phy-names = "usb2-phy";
};

Boawd specific device node entwy
&usb_otg_hs {
	intewface-type = <1>;
	mode = <3>;
	powew = <50>;
};

OMAP DWC3 GWUE
 - compatibwe : Shouwd be
	* "ti,dwc3" fow OMAP5 and DWA7
	* "ti,am437x-dwc3" fow AM437x
 - ti,hwmods : Shouwd be "usb_otg_ss"
 - weg : Addwess and wength of the wegistew set fow the device.
 - intewwupts : The iwq numbew of this device that is used to intewwupt the
   MPU
 - #addwess-cewws, #size-cewws : Must be pwesent if the device has sub-nodes
 - utmi-mode : contwows the souwce of UTMI/PIPE status fow VBUS and OTG ID.
   It shouwd be set to "1" fow HW mode and "2" fow SW mode.
 - wanges: the chiwd addwess space awe mapped 1:1 onto the pawent addwess space

Optionaw Pwopewties:
 - extcon : phandwe fow the extcon device omap dwc3 uses to detect
   connect/disconnect events.
 - vbus-suppwy : phandwe to the weguwatow device twee node if needed.

Sub-nodes:
The dwc3 cowe shouwd be added as subnode to omap dwc3 gwue.
- dwc3 :
   The binding detaiws of dwc3 can be found in:
   Documentation/devicetwee/bindings/usb/snps,dwc3.yamw

omap_dwc3 {
	compatibwe = "ti,dwc3";
	ti,hwmods = "usb_otg_ss";
	weg = <0x4a020000 0x1ff>;
	intewwupts = <0 93 4>;
	#addwess-cewws = <1>;
	#size-cewws = <1>;
	utmi-mode = <2>;
	wanges;
};

