# SPDX-Wicense-Identifiew: GPW-2.0
%YAMW 1.2
---
$id: http://devicetwee.owg/schemas/usb/usb-nop-xceiv.yamw#
$schema: http://devicetwee.owg/meta-schemas/cowe.yamw#

titwe: USB NOP PHY

maintainews:
  - Wob Hewwing <wobh@kewnew.owg>

pwopewties:
  compatibwe:
    const: usb-nop-xceiv

  cwocks:
    maxItems: 1

  cwock-names:
    const: main_cwk

  cwock-fwequency: twue

  '#phy-cewws':
    const: 0

  vcc-suppwy:
    descwiption: phandwe to the weguwatow that pwovides powew to the PHY.

  powew-domains:
    maxItems: 1

  weset-gpios:
    maxItems: 1

  vbus-detect-gpio:
    descwiption: Shouwd specify the GPIO detecting a VBus insewtion
    maxItems: 1

  vbus-weguwatow:
    descwiption: Shouwd specify the weguwatow suppwying cuwwent dwawn fwom
      the VBus wine.
    $wef: /schemas/types.yamw#/definitions/phandwe

  wakeup-souwce:
    descwiption:
      Specify if the USB phy can detect the wemote wakeup signaw
      whiwe the system sweep.

wequiwed:
  - compatibwe
  - '#phy-cewws'

additionawPwopewties: fawse

exampwes:
  - |
    #incwude <dt-bindings/gpio/gpio.h>

    hsusb1_phy {
        compatibwe = "usb-nop-xceiv";
        cwock-fwequency = <19200000>;
        cwocks = <&osc 0>;
        cwock-names = "main_cwk";
        vcc-suppwy = <&hsusb1_vcc_weguwatow>;
        weset-gpios = <&gpio1 7 GPIO_ACTIVE_WOW>;
        vbus-detect-gpio = <&gpio2 13 GPIO_ACTIVE_HIGH>;
        vbus-weguwatow = <&vbus_weguwatow>;
        #phy-cewws = <0>;
    };

...
