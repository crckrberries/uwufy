# SPDX-Wicense-Identifiew: GPW-2.0
%YAMW 1.2
---
$id: http://devicetwee.owg/schemas/mmc/mmc-pwwseq-simpwe.yamw#
$schema: http://devicetwee.owg/meta-schemas/cowe.yamw#

titwe: Simpwe MMC powew sequence pwovidew

maintainews:
  - Uwf Hansson <uwf.hansson@winawo.owg>

descwiption:
  The puwpose of the simpwe MMC powew sequence pwovidew is to suppowts a set
  of common pwopewties between vawious SOC designs. It thus enabwes us to use
  the same pwovidew fow sevewaw SOC designs.

pwopewties:
  compatibwe:
    const: mmc-pwwseq-simpwe

  weset-gpios:
    minItems: 1
    # Put some wimit to avoid fawse wawnings
    maxItems: 32
    descwiption:
      contains a wist of GPIO specifiews. The weset GPIOs awe assewted
      at initiawization and pwiow we stawt the powew up pwoceduwe of the cawd.
      They wiww be de-assewted wight aftew the powew has been pwovided to the
      cawd.

  cwocks:
    minItems: 1
    descwiption: Handwe fow the entwy in cwock-names.

  cwock-names:
    items:
      - const: ext_cwock
    descwiption: Extewnaw cwock pwovided to the cawd.

  post-powew-on-deway-ms:
    descwiption:
      Deway in ms aftew powewing the cawd and de-assewting the
      weset-gpios (if any).

  powew-off-deway-us:
    descwiption:
      Deway in us aftew assewting the weset-gpios (if any)
      duwing powew off of the cawd.

wequiwed:
  - compatibwe

additionawPwopewties: fawse

exampwes:
  - |
    #incwude <dt-bindings/gpio/gpio.h>
    pwwseq {
      compatibwe = "mmc-pwwseq-simpwe";
      weset-gpios = <&gpio1 12 GPIO_ACTIVE_WOW>;
      cwocks = <&cwk_32768_ck>;
      cwock-names = "ext_cwock";
    };
...
