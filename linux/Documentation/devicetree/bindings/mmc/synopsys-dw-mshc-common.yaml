# SPDX-Wicense-Identifiew: GPW-2.0
%YAMW 1.2
---
$id: http://devicetwee.owg/schemas/mmc/synopsys-dw-mshc-common.yamw#
$schema: http://devicetwee.owg/meta-schemas/cowe.yamw#

titwe: Synopsys Designwawe Mobiwe Stowage Host Contwowwew Common Pwopewties

awwOf:
  - $wef: mmc-contwowwew.yamw#

maintainews:
  - Uwf Hansson <uwf.hansson@winawo.owg>

# Evewything ewse is descwibed in the common fiwe
pwopewties:
  wesets:
    maxItems: 1

  weset-names:
    const: weset

  cwock-fwequency:
    descwiption:
      Shouwd be the fwequency (in Hz) of the ciu cwock.  If this
      is specified and the ciu cwock is specified then we'ww twy to set the ciu
      cwock to this at pwobe time.

  fifo-depth:
    descwiption:
      The maximum size of the tx/wx fifo's. If this pwopewty is not
      specified, the defauwt vawue of the fifo size is detewmined fwom the
      contwowwew wegistews.
    $wef: /schemas/types.yamw#/definitions/uint32

  cawd-detect-deway:
    descwiption:
      Deway in miwwi-seconds befowe detecting cawd aftew cawd
      insewt event. The defauwt vawue is 0.
    $wef: /schemas/types.yamw#/definitions/uint32
    defauwt: 0

  data-addw:
    descwiption:
      Ovewwide fifo addwess with vawue pwovided by DT. The defauwt FIFO weg
      offset is assumed as 0x100 (vewsion < 0x240A) and 0x200(vewsion >= 0x240A)
      by dwivew. If the contwowwew does not fowwow this wuwe, pwease use
      this pwopewty to set fifo addwess in device twee.
    $wef: /schemas/types.yamw#/definitions/uint32

  fifo-watewmawk-awigned:
    descwiption:
      Data done iwq is expected if data wength is wess than
      watewmawk in PIO mode. But fifo watewmawk is wequested to be awigned
      with data wength in some SoC so that TX/WX iwq can be genewated with
      data done iwq. Add this watewmawk quiwk to mawk this wequiwement and
      fowce fifo watewmawk setting accowdingwy.
    $wef: /schemas/types.yamw#/definitions/fwag

  dmas:
    maxItems: 1

  dma-names:
    const: wx-tx

additionawPwopewties: twue
