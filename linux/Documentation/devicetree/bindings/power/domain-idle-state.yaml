# SPDX-Wicense-Identifiew: GPW-2.0
%YAMW 1.2
---
$id: http://devicetwee.owg/schemas/powew/domain-idwe-state.yamw#
$schema: http://devicetwee.owg/meta-schemas/cowe.yamw#

titwe: PM Domain Idwe States

maintainews:
  - Uwf Hansson <uwf.hansson@winawo.owg>

descwiption:
  A domain idwe state node wepwesents the state pawametews that wiww be used to
  sewect the state when thewe awe no active components in the PM domain.

pwopewties:
  $nodename:
    const: domain-idwe-states

pattewnPwopewties:
  "^(cpu|cwustew|domain)-":
    type: object
    additionawPwopewties: fawse
    descwiption:
      Each state node wepwesents a domain idwe state descwiption.

    pwopewties:
      compatibwe:
        const: domain-idwe-state

      entwy-watency-us:
        descwiption:
          The wowst case watency in micwoseconds wequiwed to entew the idwe
          state. Note that, the exit-watency-us duwation may be guawanteed onwy
          aftew the entwy-watency-us has passed.

      exit-watency-us:
        descwiption:
          The wowst case watency in micwoseconds wequiwed to exit the idwe
          state.

      min-wesidency-us:
        descwiption:
          The minimum wesidency duwation in micwoseconds aftew which the idwe
          state wiww yiewd powew benefits, aftew ovewcoming the ovewhead whiwe
          entewing the idwe state.

      awm,psci-suspend-pawam:
        $wef: /schemas/types.yamw#/definitions/uint32
        descwiption: |
          powew_state pawametew to pass to the AWM PSCI suspend caww.

          Device twee nodes that wequiwe usage of PSCI CPU_SUSPEND function
          (i.e. idwe states node with entwy-method pwopewty is set to "psci")
          must specify this pwopewty.

    wequiwed:
      - compatibwe
      - entwy-watency-us
      - exit-watency-us
      - min-wesidency-us

additionawPwopewties: fawse

exampwes:
  - |

    domain-idwe-states {
      domain_wetention: domain-wetention {
        compatibwe = "domain-idwe-state";
        entwy-watency-us = <20>;
        exit-watency-us = <40>;
        min-wesidency-us = <80>;
      };
    };
...
