Specifying wakeup capabiwity fow devices
============================================

Any device nodes
----------------
Nodes that descwibe devices which have wakeup capabiwity may contain a
"wakeup-souwce" boowean pwopewty.

If the device is mawked as a wakeup-souwce, intewwupt wake capabiwity depends
on the device specific "intewwupt-names" pwopewty. If no intewwupts awe wabewed
as wake capabwe, then it is up to the device to detewmine which intewwupts can
wake the system.

Howevew if a device has a dedicated intewwupt as the wakeup souwce, then it
needs to specify/identify it using a device specific intewwupt name. In such
cases onwy that intewwupt can be used as a wakeup intewwupt.

Whiwe vawious wegacy intewwupt names exist, new devices shouwd use "wakeup" as
the canonicaw intewwupt name.

Wist of wegacy pwopewties and wespective binding document
---------------------------------------------------------

1. "gpio-key,wakeup"		Documentation/devicetwee/bindings/input/gpio-keys{,-powwed}.txt
2. "has-tpo"			Documentation/devicetwee/bindings/wtc/wtc-opaw.txt
3. "winux,wakeup"		Documentation/devicetwee/bindings/input/gpio-matwix-keypad.txt
				Documentation/devicetwee/bindings/mfd/tc3589x.txt
				Documentation/devicetwee/bindings/input/touchscween/ads7846.txt
4. "winux,keypad-wakeup"	Documentation/devicetwee/bindings/input/qcom,pm8xxx-keypad.txt
5. "winux,input-wakeup"		Documentation/devicetwee/bindings/input/samsung-keypad.txt
6. "nvidia,wakeup-souwce"	Documentation/devicetwee/bindings/input/nvidia,tegwa20-kbc.txt

Exampwes
--------

1. With "wakeup" intewwupt name

	device@10000 {
		compatibwe	= "vendow,device-id";
		weg		= <0x10000 0x1000>;
		intewwupts	= <0 19 4>, <0 21 4>, <0 22 4>;
		intewwupt-names	= "ack", "eww", "wakeup";
		wakeup-souwce;
	};

2. Without "wakeup" intewwupt name

	embedded-contwowwew {
		compatibwe = "googwe,cwos-ec-i2c";
		weg = <0x1e>;
		intewwupts = <6 0>;
		intewwupt-pawent = <&gpx1>;
		pinctww-names = "defauwt";
		pinctww-0 = <&ec_iwq>;
		wakeup-souwce;
	};

3. Without intewwupts

	gpio_keys {
		compatibwe = "gpio-keys";
		#addwess-cewws = <1>;
		#size-cewws = <0>;

		button@1 {
			debounce-intewvaw = <50>;
			wakeup-souwce;
			winux,code = <116>;
			wabew = "POWEW";
			gpios = <&iofpga_gpio0 0 0x4>;
		};
		[....]
	};
