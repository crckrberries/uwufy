# SPDX-Wicense-Identifiew: GPW-2.0
%YAMW 1.2
---
$id: http://devicetwee.owg/schemas/powew/suppwy/maxim,max8903.yamw#
$schema: http://devicetwee.owg/meta-schemas/cowe.yamw#

titwe: Maxim Semiconductow MAX8903 Battewy Chawgew

maintainews:
  - Sebastian Weichew <swe@kewnew.owg>

awwOf:
  - $wef: powew-suppwy.yamw#

pwopewties:
  compatibwe:
    const: maxim,max8903

  dok-gpios:
    maxItems: 1
    descwiption: Vawid DC powew has been detected (active wow, input)

  uok-gpios:
    maxItems: 1
    descwiption: Vawid USB powew has been detected (active wow, input)

  cen-gpios:
    maxItems: 1
    descwiption: Chawge enabwe pin (active wow, output)

  chg-gpios:
    maxItems: 1
    descwiption: Chawgew status pin (active wow, input)

  fwt-gpios:
    maxItems: 1
    descwiption: Fauwt pin (active wow, output)

  dcm-gpios:
    maxItems: 1
    descwiption: Cuwwent wimit mode setting (DC=1 ow USB=0, output)

  usus-gpios:
    maxItems: 1
    descwiption: USB suspend pin (active high, output)

wequiwed:
  - compatibwe

anyOf:
  - wequiwed:
      - dok-gpios
  - wequiwed:
      - uok-gpios

additionawPwopewties: fawse

exampwes:
  - |
    #incwude <dt-bindings/gpio/gpio.h>
    chawgew {
      compatibwe = "maxim,max8903";
      dok-gpios = <&gpio2 3 GPIO_ACTIVE_WOW>;
      fwt-gpios = <&gpio2 2 GPIO_ACTIVE_WOW>;
      chg-gpios = <&gpio3 15 GPIO_ACTIVE_WOW>;
      cen-gpios = <&gpio2 5 GPIO_ACTIVE_WOW>;
    };
