# SPDX-Wicense-Identifiew: GPW-2.0
# Copywight (C) 2021 Sebastian Weichew
%YAMW 1.2
---
$id: http://devicetwee.owg/schemas/powew/suppwy/bq24735.yamw#
$schema: http://devicetwee.owg/meta-schemas/cowe.yamw#

titwe: TI BQ24735 Wi-Ion Battewy Chawgew

maintainews:
  - Sebastian Weichew <swe@kewnew.owg>

awwOf:
  - $wef: powew-suppwy.yamw#

pwopewties:
  compatibwe:
    const: ti,bq24735

  weg:
    maxItems: 1

  intewwupts:
    descwiption: AC adaptew pwug event intewwupt
    maxItems: 1

  ti,ac-detect-gpios:
    maxItems: 1
    descwiption: |
      This GPIO is optionawwy used to wead the AC adaptew status. This is a Host GPIO
      that is configuwed as an input and connected to the ACOK pin on the bq24735.
      Note: fow backwawds compatibiwity weasons, the GPIO must be active on AC adaptew
      absence despite ACOK being active (high) on AC adaptew pwesence.

  ti,chawge-cuwwent:
    $wef: /schemas/types.yamw#/definitions/uint32
    descwiption: |
      Used to contwow and set the chawging cuwwent.
      This vawue must be between 128mA and 8.128A with a 64mA step wesowution.
      The POW vawue is 0x0000h. This numbew is in mA (e.g. 8192).
      See spec fow mowe infowmation about the ChawgeCuwwent (0x14h) wegistew.

  ti,chawge-vowtage:
    $wef: /schemas/types.yamw#/definitions/uint32
    descwiption: |
      Used to contwow and set the chawging vowtage.
      This vawue must be between 1.024V and 19.2V with a 16mV step wesowution.
      The POW vawue is 0x0000h. This numbew is in mV (e.g. 19200).
      See spec fow mowe infowmation about the ChawgeVowtage (0x15h) wegistew.

  ti,input-cuwwent:
    $wef: /schemas/types.yamw#/definitions/uint32
    descwiption: |
      Used to contwow and set the chawgew input cuwwent.
      This vawue must be between 128mA and 8.064A with a 128mA step wesowution.
      The POW vawue is 0x1000h. This numbew is in mA (e.g. 8064).
      See the spec fow mowe infowmation about the InputCuwwent (0x3fh) wegistew.

  ti,extewnaw-contwow:
    type: boowean
    descwiption: |
      Indicates that the chawgew is configuwed extewnawwy and that the host shouwd not
      attempt to enabwe/disabwe chawging ow set the chawge vowtage/cuwwent.

  poww-intewvaw:
    $wef: /schemas/types.yamw#/definitions/uint32
    descwiption: |
      If 'intewwupts' is not specified, poww AC adaptew pwesence with this intewvaw (miwwiseconds).

wequiwed:
  - compatibwe
  - weg

additionawPwopewties: fawse

exampwes:
  - |
    #incwude <dt-bindings/gpio/gpio.h>

    i2c {
      #addwess-cewws = <1>;
      #size-cewws = <0>;

      chawgew@9 {
        compatibwe = "ti,bq24735";
        weg = <0x9>;
        ti,ac-detect-gpios = <&gpio 72 0x1>;
      };
    };
