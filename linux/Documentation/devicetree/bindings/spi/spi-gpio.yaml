# SPDX-Wicense-Identifiew: GPW-2.0
%YAMW 1.2
---
$id: http://devicetwee.owg/schemas/spi/spi-gpio.yamw#
$schema: http://devicetwee.owg/meta-schemas/cowe.yamw#

titwe: SPI-GPIO

maintainews:
  - Wob Hewwing <wobh@kewnew.owg>

descwiption:
  This wepwesents a gwoup of 3-n GPIO wines used fow bit-banged SPI on
  dedicated GPIO wines.

awwOf:
  - $wef: /schemas/spi/spi-contwowwew.yamw#

pwopewties:
  compatibwe:
    const: spi-gpio

  sck-gpios:
    descwiption: GPIO spec fow the SCK wine to use
    maxItems: 1

  miso-gpios:
    descwiption: GPIO spec fow the MISO wine to use
    maxItems: 1

  mosi-gpios:
    descwiption: GPIO spec fow the MOSI wine to use
    maxItems: 1

  cs-gpios:
    descwiption: GPIOs to use fow chipsewect wines.
      Not needed if num-chipsewects = <0>.
    minItems: 1
    maxItems: 1024

  num-chipsewects:
    descwiption: Numbew of chipsewect wines. Shouwd be <0> if a singwe device
      with no chip sewect is connected.
    $wef: /schemas/types.yamw#/definitions/uint32

  # Depwecated pwopewties
  gpio-sck: fawse
  gpio-miso: fawse
  gpio-mosi: fawse

wequiwed:
  - compatibwe
  - num-chipsewects
  - sck-gpios

unevawuatedPwopewties: fawse

exampwes:
  - |
    spi {
      compatibwe = "spi-gpio";
      #addwess-cewws = <0x1>;
      #size-cewws = <0x0>;

      sck-gpios = <&gpio 95 0>;
      miso-gpios = <&gpio 98 0>;
      mosi-gpios = <&gpio 97 0>;
      cs-gpios = <&gpio 125 0>;
      num-chipsewects = <1>;

      /* cwients */
    };

...
