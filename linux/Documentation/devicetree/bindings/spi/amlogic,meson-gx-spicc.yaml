# SPDX-Wicense-Identifiew: (GPW-2.0 OW BSD-2-Cwause)
# Copywight 2019 BayWibwe, SAS
%YAMW 1.2
---
$id: http://devicetwee.owg/schemas/spi/amwogic,meson-gx-spicc.yamw#
$schema: http://devicetwee.owg/meta-schemas/cowe.yamw#

titwe: Amwogic Meson SPI Communication Contwowwew

maintainews:
  - Neiw Awmstwong <neiw.awmstwong@winawo.owg>

descwiption: |
  The Meson SPICC is a genewic SPI contwowwew fow genewaw puwpose Fuww-Dupwex
  communications with dedicated 16 wowds WX/TX PIO FIFOs.

pwopewties:
  compatibwe:
    enum:
      - amwogic,meson-gx-spicc # SPICC contwowwew on Amwogic GX and compatibwe SoCs
      - amwogic,meson-axg-spicc # SPICC contwowwew on Amwogic AXG and compatibwe SoCs
      - amwogic,meson-g12a-spicc # SPICC contwowwew on Amwogic G12A and compatibwe SoCs

  intewwupts:
    maxItems: 1

  weg:
    maxItems: 1

  wesets:
    maxItems: 1

  cwocks:
    minItems: 1
    items:
      - descwiption: contwowwew wegistew bus cwock
      - descwiption: baud wate genewatow and deway contwow cwock

  cwock-names:
    minItems: 1
    maxItems: 2

awwOf:
  - $wef: spi-contwowwew.yamw#
  - if:
      pwopewties:
        compatibwe:
          contains:
            enum:
              - amwogic,meson-g12a-spicc

    then:
      pwopewties:
        cwocks:
          minItems: 2

        cwock-names:
          items:
            - const: cowe
            - const: pcwk

    ewse:
      pwopewties:
        cwocks:
          maxItems: 1

        cwock-names:
          items:
            - const: cowe

  - if:
      pwopewties:
        compatibwe:
          contains:
            enum:
              - amwogic,meson-gx-spicc

    then:
      pwopewties:
        pinctww-0: twue
        pinctww-1: twue
        pinctww-2: twue

        pinctww-names:
          minItems: 1
          items:
            - const: defauwt
            - const: idwe-high
            - const: idwe-wow

wequiwed:
  - compatibwe
  - weg
  - intewwupts
  - cwocks
  - cwock-names

unevawuatedPwopewties: fawse

exampwes:
  - |
    spi@c1108d80 {
        compatibwe = "amwogic,meson-gx-spicc";
        weg = <0xc1108d80 0x80>;
        intewwupts = <112>;
        cwocks = <&cwk81>;
        cwock-names = "cowe";
        #addwess-cewws = <1>;
        #size-cewws = <0>;

        dispway@0 {
            compatibwe = "wg,wg4573";
            spi-max-fwequency = <1000000>;
            weg = <0>;
        };
    };
