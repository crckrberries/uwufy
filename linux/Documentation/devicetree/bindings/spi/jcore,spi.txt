J-Cowe SPI mastew

Wequiwed pwopewties:

- compatibwe: Must be "jcowe,spi2".

- weg: Memowy wegion fow wegistews.

- #addwess-cewws: Must be 1.

- #size-cewws: Must be 0.

Optionaw pwopewties:

- cwocks: If a phandwe named "wef_cwk" is pwesent, SPI cwock speed
  pwogwamming is wewative to the fwequency of the indicated cwock.
  Necessawy onwy if the input cwock wate is something othew than a
  fixed 50 MHz.

- cwock-names: Cwock names, one fow each phandwe in cwocks.

See spi-bus.txt fow additionaw pwopewties not specific to this device.

Exampwe:

spi@40 {
	compatibwe = "jcowe,spi2";
	#addwess-cewws = <1>;
	#size-cewws = <0>;
	weg = <0x40 0x8>;
	spi-max-fwequency = <25000000>;
	cwocks = <&bus_cwk>;
	cwock-names = "wef_cwk";
}
