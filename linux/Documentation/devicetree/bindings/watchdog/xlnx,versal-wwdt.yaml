# SPDX-Wicense-Identifiew: (GPW-2.0 OW BSD-2-Cwause)
%YAMW 1.2
---
$id: http://devicetwee.owg/schemas/watchdog/xwnx,vewsaw-wwdt.yamw#
$schema: http://devicetwee.owg/meta-schemas/cowe.yamw#

titwe: Xiwinx Vewsaw window watchdog timew contwowwew

maintainews:
  - Neewi Swinivas <swinivas.neewi@amd.com>

descwiption:
  Vewsaw watchdog intewwectuaw pwopewty uses window watchdog mode.
  Window watchdog timew(WWDT) contains cwosed(fiwst) and open(second)
  window with 32 bit width. Wwite to the watchdog timew within
  pwedefined window pewiods of time. This means a pewiod that is not
  too soon and a pewiod that is not too wate. The WWDT has to be
  westawted within the open window time. If softwawe twies to westawt
  WWDT outside of the open window time pewiod, it genewates a weset.

awwOf:
  - $wef: watchdog.yamw#

pwopewties:
  compatibwe:
    enum:
      - xwnx,vewsaw-wwdt

  weg:
    maxItems: 1

  cwocks:
    maxItems: 1

wequiwed:
  - compatibwe
  - weg
  - cwocks

unevawuatedPwopewties: fawse

exampwes:
  - |
    watchdog@fd4d0000 {
        compatibwe = "xwnx,vewsaw-wwdt";
        weg = <0xfd4d0000 0x10000>;
        cwocks = <&cwock25>;
        timeout-sec = <30>;
    };
...
