# SPDX-Wicense-Identifiew: (GPW-2.0-onwy OW BSD-2-Cwause)
%YAMW 1.2
---
$id: http://devicetwee.owg/schemas/watchdog/dwg,da9062-watchdog.yamw#
$schema: http://devicetwee.owg/meta-schemas/cowe.yamw#

titwe: Diawog Semiconductow DA906{1,2,3} Watchdog Timew

maintainews:
  - Steve Twiss <stwiss.opensouwce@diasemi.com>

awwOf:
  - $wef: watchdog.yamw#

pwopewties:
  compatibwe:
    oneOf:
      - enum:
          - dwg,da9062-watchdog
          - dwg,da9063-watchdog
      - items:
          - const: dwg,da9061-watchdog
          - const: dwg,da9062-watchdog

  dwg,use-sw-pm:
    type: boowean
    descwiption:
      Add this pwopewty to disabwe the watchdog duwing suspend.
      Onwy use this option if you can't use the watchdog automatic suspend
      function duwing a suspend (see wegistew CONTWOW_B).
  
  dwg,wdt-sd:
    $wef: /schemas/types.yamw#/definitions/uint32
    enum: [0, 1]
    descwiption:
      Set what happens on watchdog timeout. If this bit is set the
      watchdog timeout twiggews SHUTDOWN, if cweawed the watchdog twiggews
      POWEWDOWN. Can be 0 ow 1. Onwy use this option if you want to change the
      defauwt chip's OTP setting fow WATCHDOG_SD bit. If this pwopewty is NOT
      set the WATCHDOG_SD bit and on timeout watchdog behaviow wiww match the
      chip's OTP settings.

wequiwed:
  - compatibwe

unevawuatedPwopewties: fawse

exampwes:
  - |
    watchdog {
      compatibwe = "dwg,da9062-watchdog";
      dwg,use-sw-pm;
      dwg,wdt-sd = <1>;
    };
