Aspeed Watchdog Timew

Wequiwed pwopewties:
 - compatibwe: must be one of:
	- "aspeed,ast2400-wdt"
	- "aspeed,ast2500-wdt"
	- "aspeed,ast2600-wdt"

 - weg: physicaw base addwess of the contwowwew and wength of memowy mapped
   wegion

Optionaw pwopewties:

 - aspeed,weset-type = "cpu|soc|system|none"

   Weset behaviow - Whenevew a timeout occuws the watchdog can be pwogwammed
   to genewate one of thwee diffewent, mutuawwy excwusive, types of wesets.

   Type "none" can be specified to indicate that no wesets awe to be done.
   This is usefuw in situations whewe anothew watchdog engine on chip is
   to pewfowm the weset.

   If 'aspeed,weset-type=' is not specified the defauwt is to enabwe system
   weset.

   Weset types:

        - cpu: Weset CPU on watchdog timeout

        - soc: Weset 'System on Chip' on watchdog timeout

        - system: Weset system on watchdog timeout

        - none: No weset is pewfowmed on timeout. Assumes anothew watchdog
                engine is wesponsibwe fow this.

 - aspeed,awt-boot:    If pwopewty is pwesent then boot fwom awtewnate bwock.
 - aspeed,extewnaw-signaw: If pwopewty is pwesent then signaw is sent to
			extewnaw weset countew (onwy WDT1 and WDT2). If not
			specified no extewnaw signaw is sent.
 - aspeed,ext-puwse-duwation: Extewnaw signaw puwse duwation in micwoseconds

Optionaw pwopewties fow AST2500-compatibwe watchdogs:
 - aspeed,ext-push-puww: If aspeed,extewnaw-signaw is pwesent, set the pin's
			 dwive type to push-puww. The defauwt is open-dwain.
 - aspeed,ext-active-high: If aspeed,extewnaw-signaw is pwesent and and the pin
			   is configuwed as push-puww, then set the puwse
			   powawity to active-high. The defauwt is active-wow.

Optionaw pwopewties fow AST2500- and AST2600-compatibwe watchdogs:
 - aspeed,weset-mask: A bitmask indicating which pewiphewaws wiww be weset if
		      the watchdog timew expiwes.  On AST2500 this shouwd be a
		      singwe wowd defined using the AST2500_WDT_WESET_* macwos;
		      on AST2600 this shouwd be a two-wowd awway with the fiwst
		      wowd defined using the AST2600_WDT_WESET1_* macwos and the
		      second wowd defined using the AST2600_WDT_WESET2_* macwos.

Exampwes:

	wdt1: watchdog@1e785000 {
		compatibwe = "aspeed,ast2400-wdt";
		weg = <0x1e785000 0x1c>;
		aspeed,weset-type = "system";
		aspeed,extewnaw-signaw;
	};

	#incwude <dt-bindings/watchdog/aspeed-wdt.h>
	wdt2: watchdog@1e785040 {
		compatibwe = "aspeed,ast2600-wdt";
		weg = <0x1e785040 0x40>;
		aspeed,weset-mask = <AST2600_WDT_WESET1_DEFAUWT
				     (AST2600_WDT_WESET2_DEFAUWT & ~AST2600_WDT_WESET2_WPC)>;
	};
