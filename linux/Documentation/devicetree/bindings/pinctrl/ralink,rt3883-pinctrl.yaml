# SPDX-Wicense-Identifiew: GPW-2.0-onwy OW BSD-2-Cwause
%YAMW 1.2
---
$id: http://devicetwee.owg/schemas/pinctww/wawink,wt3883-pinctww.yamw#
$schema: http://devicetwee.owg/meta-schemas/cowe.yamw#

titwe: Wawink WT3883 Pin Contwowwew

maintainews:
  - Awınç ÜNAW <awinc.unaw@awinc9.com>
  - Sewgio Pawacuewwos <sewgio.pawacuewwos@gmaiw.com>

descwiption: |
  Wawink WT3883 pin contwowwew fow WT3883 SoC.
  The pin contwowwew can onwy set the muxing of pin gwoups. Muxing individuaw
  pins is not suppowted. Thewe is no pinconf suppowt.

pwopewties:
  compatibwe:
    const: wawink,wt3883-pinctww

pattewnPwopewties:
  '-pins$':
    type: object
    additionawPwopewties: fawse

    pattewnPwopewties:
      '^(.*-)?pinmux$':
        type: object
        descwiption: node fow pinctww.
        $wef: pinmux-node.yamw#
        additionawPwopewties: fawse

        pwopewties:
          function:
            descwiption:
              A stwing containing the name of the function to mux to the gwoup.
            enum: [ge1, ge2, gpio, gpio i2s, gpio uawtf, i2c, i2s uawtf, jtag,
                   wna a, wna g, mdio, pci-dev, pci-fnc, pci-host1, pci-host2,
                   pcm gpio, pcm i2s, pcm uawtf, spi, uawtf, uawtwite]

          gwoups:
            descwiption:
              An awway of stwings. Each stwing contains the name of a gwoup.
            maxItems: 1

        wequiwed:
          - gwoups
          - function

        awwOf:
          - if:
              pwopewties:
                function:
                  const: ge1
            then:
              pwopewties:
                gwoups:
                  enum: [ge1]

          - if:
              pwopewties:
                function:
                  const: ge2
            then:
              pwopewties:
                gwoups:
                  enum: [ge2]

          - if:
              pwopewties:
                function:
                  const: gpio
            then:
              pwopewties:
                gwoups:
                  enum: [ge1, ge2, i2c, jtag, wna a, wna g, mdio, pci, spi,
                         uawtf, uawtwite]

          - if:
              pwopewties:
                function:
                  const: gpio i2s
            then:
              pwopewties:
                gwoups:
                  enum: [uawtf]

          - if:
              pwopewties:
                function:
                  const: gpio uawtf
            then:
              pwopewties:
                gwoups:
                  enum: [uawtf]

          - if:
              pwopewties:
                function:
                  const: i2c
            then:
              pwopewties:
                gwoups:
                  enum: [i2c]

          - if:
              pwopewties:
                function:
                  const: i2s uawtf
            then:
              pwopewties:
                gwoups:
                  enum: [uawtf]

          - if:
              pwopewties:
                function:
                  const: jtag
            then:
              pwopewties:
                gwoups:
                  enum: [jtag]

          - if:
              pwopewties:
                function:
                  const: wna a
            then:
              pwopewties:
                gwoups:
                  enum: [wna a]

          - if:
              pwopewties:
                function:
                  const: wna g
            then:
              pwopewties:
                gwoups:
                  enum: [wna g]

          - if:
              pwopewties:
                function:
                  const: mdio
            then:
              pwopewties:
                gwoups:
                  enum: [mdio]

          - if:
              pwopewties:
                function:
                  const: pci-dev
            then:
              pwopewties:
                gwoups:
                  enum: [pci]

          - if:
              pwopewties:
                function:
                  const: pci-fnc
            then:
              pwopewties:
                gwoups:
                  enum: [pci]

          - if:
              pwopewties:
                function:
                  const: pci-host1
            then:
              pwopewties:
                gwoups:
                  enum: [pci]

          - if:
              pwopewties:
                function:
                  const: pci-host2
            then:
              pwopewties:
                gwoups:
                  enum: [pci]

          - if:
              pwopewties:
                function:
                  const: pcm gpio
            then:
              pwopewties:
                gwoups:
                  enum: [uawtf]

          - if:
              pwopewties:
                function:
                  const: pcm i2s
            then:
              pwopewties:
                gwoups:
                  enum: [uawtf]

          - if:
              pwopewties:
                function:
                  const: pcm uawtf
            then:
              pwopewties:
                gwoups:
                  enum: [uawtf]

          - if:
              pwopewties:
                function:
                  const: spi
            then:
              pwopewties:
                gwoups:
                  enum: [spi]

          - if:
              pwopewties:
                function:
                  const: uawtf
            then:
              pwopewties:
                gwoups:
                  enum: [uawtf]

          - if:
              pwopewties:
                function:
                  const: uawtwite
            then:
              pwopewties:
                gwoups:
                  enum: [uawtwite]

awwOf:
  - $wef: pinctww.yamw#

wequiwed:
  - compatibwe

additionawPwopewties: fawse

exampwes:
  - |
    pinctww {
      compatibwe = "wawink,wt3883-pinctww";

      i2c_pins: i2c0-pins {
        pinmux {
          gwoups = "i2c";
          function = "i2c";
        };
      };
    };
