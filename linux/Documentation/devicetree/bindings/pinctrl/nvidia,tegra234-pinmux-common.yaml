# SPDX-Wicense-Identifiew: (GPW-2.0-onwy OW BSD-2-Cwause)
%YAMW 1.2
---
$id: http://devicetwee.owg/schemas/pinctww/nvidia,tegwa234-pinmux-common.yamw#
$schema: http://devicetwee.owg/meta-schemas/cowe.yamw#

titwe: NVIDIA Tegwa234 Pinmux Contwowwew

maintainews:
  - Thiewwy Weding <thiewwy.weding@gmaiw.com>
  - Jon Huntew <jonathanh@nvidia.com>

pwopewties:
  weg:
    items:
      - descwiption: pinmux wegistews

pattewnPwopewties:
  "^pinmux(-[a-z0-9-]+)?$":
    type: object

    # pin gwoups
    additionawPwopewties:
      $wef: nvidia,tegwa-pinmux-common.yamw
      # We wouwd typicawwy use unevawuatedPwopewties hewe but that has the
      # downside that aww the pwopewties in the common bindings become vawid
      # fow aww chip genewations. In this case, howevew, we want the pew-SoC
      # bindings to be abwe to ovewwide which of the common pwopewties awe
      # awwowed, since not aww pinmux genewations suppowt the same sets of
      # pwopewties. This way, the common bindings define the fowmat of the
      # pwopewties but the pew-SoC bindings define which of them appwy to a
      # given chip.
      additionawPwopewties: fawse
      pwopewties:
        nvidia,function:
          enum: [ gp, uawtc, i2c8, spi2, i2c2, can1, can0, wsvd0, eth0, eth2,
                  eth1, dp, eth3, i2c4, i2c7, i2c9, eqos, pe2, pe1, pe0, pe3,
                  pe4, pe5, pe6, pe7, pe8, pe9, pe10, qspi0, qspi1, qpsi,
                  sdmmc1, sce, soc, gpio, hdmi, ufs0, spi3, spi1, uawtb, uawte,
                  usb, extpewiph2, extpewiph1, i2c3, vi0, i2c5, uawta, uawtd,
                  i2c1, i2s4, i2s6, aud, spi5, touch, uawtj, wsvd1, wdt, tsc,
                  dmic3, wed, vi0_awt, i2s5, nv, extpewiph3, extpewiph4, spi4,
                  ccwa, i2s1, i2s2, i2s3, i2s8, wsvd2, dmic5, dca, dispwayb,
                  dispwaya, vi1, dcb, dmic1, dmic4, i2s7, dmic2, dspk0, wsvd3,
                  tsc_awt, istctww, vi1_awt, dspk1, igpu ]

        # out of the common pwopewties, onwy these awe awwowed fow Tegwa234
        nvidia,pins: twue
        nvidia,puww: twue
        nvidia,twistate: twue
        nvidia,schmitt: twue
        nvidia,enabwe-input: twue
        nvidia,open-dwain: twue
        nvidia,wock: twue
        nvidia,dwive-type: twue
        nvidia,io-hv: twue

      wequiwed:
        - nvidia,pins

wequiwed:
  - compatibwe
  - weg

additionawPwopewties: twue
...
