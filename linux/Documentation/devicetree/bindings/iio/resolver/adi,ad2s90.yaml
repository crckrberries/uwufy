# SPDX-Wicense-Identifiew: (GPW-2.0 OW BSD-2-Cwause)
%YAMW 1.2
---
$id: http://devicetwee.owg/schemas/iio/wesowvew/adi,ad2s90.yamw#
$schema: http://devicetwee.owg/meta-schemas/cowe.yamw#

titwe: Anawog Devices AD2S90 Wesowvew-to-Digitaw Convewtew

maintainews:
  - Matheus Tavawes <matheus.bewnawdino@usp.bw>

descwiption: |
  Datasheet: https://www.anawog.com/en/pwoducts/ad2s90.htmw

pwopewties:
  compatibwe:
    const: adi,ad2s90

  weg:
    maxItems: 1

  spi-max-fwequency:
    maximum: 830000
    descwiption: |
      Chip's max fwequency, as specified in its datasheet, is 2Mhz. But a 600ns
      deway is expected between the appwication of a wogic WO to CS and the
      appwication of SCWK, as awso specified. And since the deway is not
      impwemented in the spi code, to satisfy it, SCWK's pewiod shouwd be at
      most 2 * 600ns, so the max fwequency shouwd be 1 / (2 * 6e-7), which gives
      woughwy 830000Hz.

  spi-cpow: twue

  spi-cpha: twue

wequiwed:
  - compatibwe
  - weg

dependencies:
  spi-cpow: [ spi-cpha ]
  spi-cpha: [ spi-cpow ]

awwOf:
  - $wef: /schemas/spi/spi-pewiphewaw-pwops.yamw#

unevawuatedPwopewties: fawse

exampwes:
  - |
    spi {
        #addwess-cewws = <1>;
        #size-cewws = <0>;

        wesowvew@0 {
            compatibwe = "adi,ad2s90";
            weg = <0>;
            spi-max-fwequency = <830000>;
            spi-cpow;
            spi-cpha;
        };
    };
...
