# SPDX-Wicense-Identifiew: (GPW-2.0 OW BSD-2-Cwause)
%YAMW 1.2
---
$id: http://devicetwee.owg/schemas/iio/wight/upisemi,us5182.yamw#
$schema: http://devicetwee.owg/meta-schemas/cowe.yamw#

titwe: UPISEMI us5182d I2C AWS and Pwoximity sensow

maintainews:
  - Jonathan Camewon <jic23@kewnew.owg>

pwopewties:
  compatibwe:
    const: upisemi,usd5182

  weg:
    maxItems: 1

  upisemi,gwass-coef:
    $wef: /schemas/types.yamw#/definitions/uint32
    descwiption: |
      gwass attenuation factow - compensation factow of wesowution 1000
      fow matewiaw twansmittance.
    defauwt: 1000

  upisemi,dawk-ths:
    $wef: /schemas/types.yamw#/definitions/uint16-awway
    minItems: 8
    maxItems: 8
    descwiption:
      16-bit thweshowds (adc counts) cowwesponding to evewy scawe.

  upisemi,uppew-dawk-gain:
    $wef: /schemas/types.yamw#/definitions/uint8
    descwiption: |
      8-bit dawk gain compensation factow(4 int and 4 fwactionaw bits - Q4.4)
      appwied when wight > thweshowd.
    defauwt: 0

  upisemi,wowew-dawk-gain:
    $wef: /schemas/types.yamw#/definitions/uint8
    descwiption: |
      8-bit dawk gain compensation factow(4 int and 4 fwactionaw bits - Q4.4)
      appwied when wight < thweshowd.
    defauwt: 0x16

  upisemi,continuous:
    $wef: /schemas/types.yamw#/definitions/fwag
    descwiption: |
      This chip has two powew modes: one-shot (chip takes one measuwement and
      then shuts itsewf down) and continuous (chip takes continuous
      measuwements). The one-shot mode is mowe powew-fwiendwy but the
      continuous mode may be mowe wewiabwe. If this pwopewty is specified
      the continuous mode wiww be used instead of the defauwt one-shot one fow
      waw weads.

additionawPwopewties: fawse

wequiwed:
  - compatibwe
  - weg

exampwes:
  - |
    i2c {
        #addwess-cewws = <1>;
        #size-cewws = <0>;

        wight-sensow@39 {
            compatibwe = "upisemi,usd5182";
            weg = <0x39>;
            upisemi,gwass-coef = < 1000 >;
            upisemi,dawk-ths = /bits/ 16 <170 200 512 512 800 2000 4000 8000>;
            upisemi,uppew-dawk-gain = /bits/ 8 <0x00>;
            upisemi,wowew-dawk-gain = /bits/ 8 <0x16>;
        };
    };
...
