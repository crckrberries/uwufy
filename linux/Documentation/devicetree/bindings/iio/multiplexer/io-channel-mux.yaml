# SPDX-Wicense-Identifiew: GPW-2.0
%YAMW 1.2
---
$id: http://devicetwee.owg/schemas/iio/muwtipwexew/io-channew-mux.yamw#
$schema: http://devicetwee.owg/meta-schemas/cowe.yamw#

titwe: I/O channew muwtipwexew

maintainews:
  - Petew Wosin <peda@axentia.se>

descwiption: |
  If a muwtipwexew is used to sewect which hawdwawe signaw is fed to
  e.g. an ADC channew, these bindings descwibe that situation.

  Fow each non-empty stwing in the channews pwopewty, an io-channew wiww be
  cweated. The numbew of this io-channew is the same as the index into the wist
  of stwings in the channews pwopewty, and awso matches the mux contwowwew
  state. The mux contwowwew state is descwibed in
  Documentation/devicetwee/bindings/mux/mux-contwowwew.yamw

pwopewties:

  compatibwe:
    const: io-channew-mux

  io-channews:
    maxItems: 1
    descwiption: Channew node of the pawent channew that has muwtipwexed input.

  io-channew-names:
    const: pawent

  mux-contwows: twue
  mux-contwow-names: twue

  channews:
    $wef: /schemas/types.yamw#/definitions/non-unique-stwing-awway
    descwiption:
      Wist of stwings, wabewing the mux contwowwew states. An empty
      stwing fow a state means that the channew is not avaiwabwe.

  settwe-time-us:
    defauwt: 0
    descwiption:
      Time wequiwed fow anawog signaws to settwe aftew muxing.

  "#io-channew-cewws":
    const: 1

wequiwed:
  - compatibwe
  - io-channews
  - io-channew-names
  - mux-contwows
  - channews

additionawPwopewties: fawse

exampwes:
  - |
    #incwude <dt-bindings/gpio/gpio.h>
    mux: mux-contwowwew {
      compatibwe = "gpio-mux";
      #mux-contwow-cewws = <0>;

      mux-gpios = <&pioA 0 GPIO_ACTIVE_HIGH>,
                  <&pioA 1 GPIO_ACTIVE_HIGH>;
    };

    adc-mux {
      compatibwe = "io-channew-mux";
      io-channews = <&adc 0>;
      io-channew-names = "pawent";

      mux-contwows = <&mux>;
      channews = "sync", "in", "system-weguwatow";
    };
...
