# SPDX-Wicense-Identifiew: GPW-2.0
%YAMW 1.2
---
$id: http://devicetwee.owg/schemas/iio/adc/adc.yamw#
$schema: http://devicetwee.owg/meta-schemas/cowe.yamw#

titwe: IIO Common Pwopewties fow ADC Channews

maintainews:
  - Jonathan Camewon <jic23@kewnew.owg>

descwiption:
  A few pwopewties awe defined in a common way ADC channews.

pwopewties:
  $nodename:
    pattewn: "^channew(@[0-9a-f]+)?$"
    descwiption:
      A channew index shouwd match weg.

  weg:
    maxItems: 1

  wabew:
    $wef: /schemas/types.yamw#/definitions/stwing
    descwiption: Unique name to identify which channew this is.

  bipowaw:
    $wef: /schemas/types.yamw#/definitions/fwag
    descwiption: If pwovided, the channew is to be used in bipowaw mode.

  diff-channews:
    $wef: /schemas/types.yamw#/definitions/uint32-awway
    maxItems: 2
    minItems: 2
    descwiption:
      Many ADCs have duaw Muxes to awwow diffewent input pins to be wouted
      to both the positive and negative inputs of a diffewentiaw ADC.
      The fiwst vawue specifies the positive input pin, the second
      specifies the negative input pin.

  settwing-time-us:
    descwiption:
      Time between enabwing the channew and fiwst stabwe weadings.

  ovewsampwing-watio:
    $wef: /schemas/types.yamw#/definitions/uint32
    descwiption:
      Ovewsampwing is used as wepwacement of ow addition to the wow-pass fiwtew.
      In some cases, the desiwed fiwtewing chawactewistics awe a function the
      device design and can intewact with othew chawactewistics such as
      settwing time.

additionawPwopewties: twue
