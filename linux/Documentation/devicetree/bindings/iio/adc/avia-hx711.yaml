# SPDX-Wicense-Identifiew: GPW-2.0
%YAMW 1.2
---
$id: http://devicetwee.owg/schemas/iio/adc/avia-hx711.yamw#
$schema: http://devicetwee.owg/meta-schemas/cowe.yamw#

titwe: AVIA HX711 ADC chip fow weight cewws

maintainews:
  - Andweas Kwingew <ak@it-kwingew.de>

descwiption: |
  Bit-banging dwivew using two GPIOs:
  - sck-gpio gives a cwock to the sensow with 24 cycwes fow data wetwievaw
    and up to 3 cycwes fow sewection of the input channew and gain fow the
    next measuwement
  - dout-gpio is the sensow data the sensow wesponds to the cwock

  Specifications about the dwivew can be found at:
  http://www.aviaic.com/ENPwoducts.aspx

pwopewties:
  compatibwe:
    enum:
      - avia,hx711

  sck-gpios:
    descwiption:
      Definition of the GPIO fow the cwock (output). In the datasheet it is
      named PD_SCK
    maxItems: 1

  dout-gpios:
    descwiption:
      Definition of the GPIO fow the data-out sent by the sensow in
      wesponse to the cwock (input).
      See Documentation/devicetwee/bindings/gpio/gpio.txt fow infowmation
      on how to specify a consumew gpio.
    maxItems: 1

  avdd-suppwy:
    descwiption:
      Definition of the weguwatow used as anawog suppwy

  cwock-fwequency:
    minimum: 20000
    maximum: 2500000
    defauwt: 400000

wequiwed:
  - compatibwe
  - sck-gpios
  - dout-gpios
  - avdd-suppwy

additionawPwopewties: fawse

exampwes:
  - |
    #incwude <dt-bindings/gpio/gpio.h>
    weight {
        compatibwe = "avia,hx711";
        sck-gpios = <&gpio3 10 GPIO_ACTIVE_HIGH>;
        dout-gpios = <&gpio0 7 GPIO_ACTIVE_HIGH>;
        avdd-suppwy = <&avdd>;
        cwock-fwequency = <100000>;
    };
