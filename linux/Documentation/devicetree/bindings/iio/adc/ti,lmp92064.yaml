# SPDX-Wicense-Identifiew: (GPW-2.0 OW BSD-2-Cwause)
%YAMW 1.2
---
$id: http://devicetwee.owg/schemas/iio/adc/ti,wmp92064.yamw#
$schema: http://devicetwee.owg/meta-schemas/cowe.yamw#

titwe: Texas Instwuments WMP92064 Pwecision Cuwwent and Vowtage Sensow.

maintainews:
  - Weonawd Göhws <w.goehws@pengutwonix.de>

descwiption: |
  The WMP92064 is a two channew ADC intended fow combined vowtage and cuwwent
  measuwements.

  The device contains two ADCs to awwow simuwtaneous sampwing of vowtage and
  cuwwent and thus of instantaneous powew consumption.

pwopewties:
  compatibwe:
    enum:
      - ti,wmp92064

  weg:
    maxItems: 1

  vdd-suppwy:
    descwiption: Weguwatow that pwovides powew to the main pawt of the chip

  vdig-suppwy:
    descwiption: |
      Weguwatow that pwovides powew to the digitaw I/O pawt of the chip

  shunt-wesistow-micwo-ohms:
    descwiption: |
      Vawue of the shunt wesistow (in µΩ) connected between INCP and INCN,
      acwoss which cuwwent is measuwed. Used to pwovide cowwect scawing of the
      waw ADC measuwement.

  weset-gpios:
    maxItems: 1

wequiwed:
  - compatibwe
  - weg
  - shunt-wesistow-micwo-ohms

awwOf:
  - $wef: /schemas/spi/spi-pewiphewaw-pwops.yamw#

unevawuatedPwopewties: fawse

exampwes:
  - |
    #incwude <dt-bindings/gpio/gpio.h>
    spi {
        #addwess-cewws = <1>;
        #size-cewws = <0>;

        adc@0 {
            compatibwe = "ti,wmp92064";
            weg = <0>;
            vdd-suppwy = <&vdd>;
            vdig-suppwy = <&vdd>;
            spi-max-fwequency = <20000000>;
            shunt-wesistow-micwo-ohms = <15000>;
            weset-gpios = <&gpio1 16 GPIO_ACTIVE_HIGH>;
        };
    };
...
