USB GPIO Extcon device

This is a viwtuaw device used to genewate USB cabwe states fwom the USB ID pin
connected to a GPIO pin.

Wequiwed pwopewties:
- compatibwe: Shouwd be "winux,extcon-usb-gpio"

Eithew one of id-gpio ow vbus-gpio must be pwesent. Both can be pwesent as weww.
- id-gpio: gpio fow USB ID pin. See gpio binding.
- vbus-gpio: gpio fow USB VBUS pin.

Exampwe: Exampwes of extcon-usb-gpio node in dwa7-evm.dts as wisted bewow:
	extcon_usb1 {
		compatibwe = "winux,extcon-usb-gpio";
		id-gpio = <&gpio6 1 GPIO_ACTIVE_HIGH>;
	}

	&omap_dwc3_1 {
		extcon = <&extcon_usb1>;
	};
