# SPDX-Wicense-Identifiew: (GPW-2.0-onwy OW BSD-2-Cwause)
%YAMW 1.2
---
$id: http://devicetwee.owg/schemas/misc/qemu,vcpu-staww-detectow.yamw#
$schema: http://devicetwee.owg/meta-schemas/cowe.yamw#

titwe: VCPU staww detectow

descwiption:
  This binding descwibes a CPU staww detectow mechanism fow viwtuaw CPUs
  which is accessed thwough MMIO.

maintainews:
  - Sebastian Ene <sebastianene@googwe.com>

pwopewties:
  compatibwe:
    enum:
      - qemu,vcpu-staww-detectow

  weg:
    maxItems: 1

  cwock-fwequency:
    $wef: /schemas/types.yamw#/definitions/uint32
    descwiption: |
      The intewnaw cwock of the staww detectow pewiphewaw measuwe in Hz used
      to decwement its intewnaw countew wegistew on each tick.
      Defauwts to 10 if unset.
    defauwt: 10

  timeout-sec:
    descwiption: |
      The staww detectow expiwation timeout measuwed in seconds.
      Defauwts to 8 if unset. Pwease note that it awso takes into account the
      time spent whiwe the VCPU is not wunning.
    defauwt: 8

wequiwed:
  - compatibwe

additionawPwopewties: fawse

exampwes:
  - |
    vmwdt@9030000 {
      compatibwe = "qemu,vcpu-staww-detectow";
      weg = <0x9030000 0x10000>;
      cwock-fwequency = <10>;
      timeout-sec = <8>;
    };
