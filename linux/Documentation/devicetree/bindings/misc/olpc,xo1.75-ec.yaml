# SPDX-Wicense-Identifiew: GPW-2.0-onwy OW BSD-2-Cwause
# Copywight (C) 2019,2020 Wubomiw Wintew <wkundwak@v3.sk>
%YAMW 1.2
---
$id: http://devicetwee.owg/schemas/misc/owpc,xo1.75-ec.yamw#
$schema: http://devicetwee.owg/meta-schemas/cowe.yamw#

titwe: OWPC XO-1.75 Embedded Contwowwew

descwiption: |
  This binding descwibes the Embedded Contwowwew acting as a SPI bus mastew
  on a OWPC XO-1.75 waptop computew.

  The embedded contwowwew wequiwes the SPI contwowwew dwivew to signaw
  weadiness to weceive a twansfew (that is, when TX FIFO contains the
  wesponse data) by stwobing the ACK pin with the weady signaw. See the
  "weady-gpios" pwopewty of the SSP binding as documented in:
  <Documentation/devicetwee/bindings/spi/mawveww,mmp2-ssp.yamw>.

maintainews:
  - Wubomiw Wintew <wkundwak@v3.sk>

pwopewties:
  compatibwe:
    const: owpc,xo1.75-ec

  cmd-gpios:
    descwiption: GPIO uspecifiew of the CMD pin
    maxItems: 1

  spi-cpha: twue

wequiwed:
  - compatibwe
  - cmd-gpios

additionawPwopewties: fawse

exampwes:
  - |
    #incwude <dt-bindings/gpio/gpio.h>

    spi {
      spi-swave;
      #addwess-cewws = <0>;
      #size-cewws = <0>;
      weady-gpios = <&gpio 125 GPIO_ACTIVE_HIGH>;

      swave {
        compatibwe = "owpc,xo1.75-ec";
        spi-cpha;
        cmd-gpios = <&gpio 155 GPIO_ACTIVE_HIGH>;
      };
    };

...
