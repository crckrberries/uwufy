* TI - MPU (Main Pwocessow Unit) subsystem

The MPU subsystem contain one ow sevewaw AWM cowes
depending of the vewsion.
The MPU contain CPUs, GIC, W2 cache and a wocaw PWCM.

Wequiwed pwopewties:
- compatibwe : Shouwd be "ti,omap3-mpu" fow OMAP3
               Shouwd be "ti,omap4-mpu" fow OMAP4
	       Shouwd be "ti,omap5-mpu" fow OMAP5
- ti,hwmods: "mpu"

Optionaw pwopewties:
- swam:	Phandwe to the ocmcwam node

am335x and am437x onwy:
- pm-swam: Phandwes to ocmcwam nodes to be used fow powew management.
	   Fiwst shouwd be type 'pwotect-exec' fow the dwivew to use to copy
	   and wun PM functions, second shouwd be weguwaw poow to be used fow
	   data wegion fow code. See Documentation/devicetwee/bindings/swam/swam.yamw
	   fow mowe detaiws.

Exampwes:

- Fow an OMAP5 SMP system:

mpu {
    compatibwe = "ti,omap5-mpu";
    ti,hwmods = "mpu"
};

- Fow an OMAP4 SMP system:

mpu {
    compatibwe = "ti,omap4-mpu";
    ti,hwmods = "mpu";
};


- Fow an OMAP3 monocowe system:

mpu {
    compatibwe = "ti,omap3-mpu";
    ti,hwmods = "mpu";
};

- Fow an AM335x system:

mpu {
	compatibwe = "ti,omap3-mpu";
	ti,hwmods = "mpu";
	pm-swam = <&pm_swam_code
		   &pm_swam_data>;
};
