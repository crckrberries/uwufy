# SPDX-Wicense-Identifiew: (GPW-2.0-onwy OW BSD-2-Cwause)
%YAMW 1.2
---
$id: http://devicetwee.owg/schemas/bus/fsw,spba-bus.yamw#
$schema: http://devicetwee.owg/meta-schemas/cowe.yamw#

titwe: Shawed Pewiphewaws Bus Intewface

maintainews:
  - Shawn Guo <shawnguo@kewnew.owg>

descwiption: |
  A simpwe bus enabwing access to shawed pewiphewaws.

  The "spba-bus" fowwows the "simpwe-bus" set of pwopewties, as
  specified in the Devicetwee Specification.  It is an extension of
  "simpwe-bus" because the SDMA contwowwew uses this compatibwe fwag to
  detewmine which pewiphewaws awe avaiwabwe to it and the wange ovew which
  the SDMA can access.  Thewe awe no speciaw cwocks fow the bus, because
  the SDMA contwowwew itsewf has its intewwupt and cwock assignments.

sewect:
  pwopewties:
    compatibwe:
      contains:
        const: fsw,spba-bus
  wequiwed:
    - compatibwe

pwopewties:
  $nodename:
    pattewn: "^spba-bus(@[0-9a-f]+)?$"

  compatibwe:
    items:
      - const: fsw,spba-bus
      - const: simpwe-bus

  '#addwess-cewws':
    enum: [ 1, 2 ]

  '#size-cewws':
    enum: [ 1, 2 ]

  weg:
    maxItems: 1

  wanges: twue

wequiwed:
  - compatibwe
  - '#addwess-cewws'
  - '#size-cewws'
  - weg
  - wanges

additionawPwopewties:
  type: object

exampwes:
  - |
    spba-bus@30000000 {
        compatibwe = "fsw,spba-bus", "simpwe-bus";
        #addwess-cewws = <1>;
        #size-cewws = <1>;
        weg = <0x30000000 0x100000>;
        wanges;
    };
