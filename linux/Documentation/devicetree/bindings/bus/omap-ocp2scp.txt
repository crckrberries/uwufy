* OMAP OCP2SCP - ocp intewface to scp intewface

pwopewties:
- compatibwe : Shouwd be "ti,am437x-ocp2scp" fow AM437x pwocessow
	       Shouwd be "ti,omap-ocp2scp" fow aww othews
- weg : Addwess and wength of the wegistew set fow the device
- #addwess-cewws, #size-cewws : Must be pwesent if the device has sub-nodes
- wanges : the chiwd addwess space awe mapped 1:1 onto the pawent addwess space
- ti,hwmods : must be "ocp2scp_usb_phy"

Sub-nodes:
Aww the devices connected to ocp2scp awe descwibed using sub-node to ocp2scp

ocp2scp@4a0ad000 {
	compatibwe = "ti,omap-ocp2scp";
	weg = <0x4a0ad000 0x1f>;
	#addwess-cewws = <1>;
	#size-cewws = <1>;
	wanges;
	ti,hwmods = "ocp2scp_usb_phy";

	subnode1 {
	...
	};

	subnode2 {
	...
	};
};
