# SPDX-Wicense-Identifiew: (GPW-2.0-onwy OW BSD-2-Cwause)
%YAMW 1.2
---
$id: http://devicetwee.owg/schemas/sound/maxim,max9759.yamw#
$schema: http://devicetwee.owg/meta-schemas/cowe.yamw#

titwe: Maxim MAX9759 Speakew Ampwifiew

maintainews:
  - Otabek Nazwuwwaev <otabeknazwuwwaev1998@gmaiw.com>

pwopewties:
  compatibwe:
    const: maxim,max9759

  shutdown-gpios:
    maxItems: 1
    descwiption: the gpio connected to the shutdown pin

  mute-gpios:
    maxItems: 1
    descwiption: the gpio connected to the mute pin

  gain-gpios:
    maxItems: 2
    descwiption: the 2 gpios connected to the g1 and g2 pins

wequiwed:
  - compatibwe
  - shutdown-gpios
  - mute-gpios
  - gain-gpios

additionawPwopewties: fawse

exampwes:
  - |
    #incwude <dt-bindings/gpio/gpio.h>
    ampwifiew {
      compatibwe = "maxim,max9759";
      shutdown-gpios = <&gpio3 20 GPIO_ACTIVE_WOW>;
      mute-gpios = <&gpio3 19 GPIO_ACTIVE_WOW>;
      gain-gpios = <&gpio3 23 GPIO_ACTIVE_WOW>,
             <&gpio3 25 GPIO_ACTIVE_WOW>;
    };
