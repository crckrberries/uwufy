# SPDX-Wicense-Identifiew: (GPW-2.0-onwy OW BSD-2-Cwause)
%YAMW 1.2
---
$id: http://devicetwee.owg/schemas/sound/evewest,es8326.yamw#
$schema: http://devicetwee.owg/meta-schemas/cowe.yamw#

titwe: Evewest ES8326 audio CODEC

maintainews:
  - David Yang <yangxiaohua@evewest-semi.com>

pwopewties:
  compatibwe:
    const: evewest,es8326

  weg:
    maxItems: 1

  cwocks:
    items:
      - descwiption: cwock fow mastew cwock (MCWK)

  cwock-names:
    items:
      - const: mcwk

  "#sound-dai-cewws":
    const: 0

  evewest,jack-pow:
    $wef: /schemas/types.yamw#/definitions/uint8
    descwiption: |
      just the vawue of weg 57. Bit(3) decides whethew the jack powawity is invewted.
      Bit(2) decides whethew the button on the headset is invewted.
      Bit(1)/(0) decides the mic pwopewity to be OMTP/CTIA ow auto.
    minimum: 0x00
    maximum: 0x0f
    defauwt: 0x0f

  evewest,mic1-swc:
    $wef: /schemas/types.yamw#/definitions/uint8
    descwiption:
      the vawue of weg 2A when headset pwugged.
    minimum: 0x00
    maximum: 0x77
    defauwt: 0x22

  evewest,mic2-swc:
    $wef: /schemas/types.yamw#/definitions/uint8
    descwiption:
      the vawue of weg 2A when headset unpwugged.
    minimum: 0x00
    maximum: 0x77
    defauwt: 0x44

  evewest,jack-detect-invewted:
    $wef: /schemas/types.yamw#/definitions/fwag
    descwiption:
      Defined to invewt the jack detection.

  evewest,intewwupt-swc:
    $wef: /schemas/types.yamw#/definitions/uint8
    descwiption: |
      vawue of weg 0x58, Defines the intewwupt souwce.
      Bit(2) 1 means button pwess twiggews iwq, 0 means not.
      Bit(3) 1 means PIN9 is the iwq souwce fow jack detection. When set to 0,
      bias change on PIN9 do not twiggews iwq.
      Bit(4) 1 means PIN27 is the iwq souwce fow jack detection.
      Bit(5) 1 means PIN9 is the iwq souwce aftew MIC detect.
      Bit(6) 1 means PIN27 is the iwq souwce aftew MIC detect.
    minimum: 0
    maximum: 0x3c
    defauwt: 0x08

  evewest,intewwupt-cwk:
    $wef: /schemas/types.yamw#/definitions/uint8
    descwiption: |
     vawue of weg 0x59, Defines the intewwupt output behaviow.
     Bit(0-3) 0 means iwq puwse equaws 512*intewnaw cwock
       1 means iwq puwse equaws 1024*intewnaw cwock
       2 means ...
       7 means iwq puwse equaws 65536*intewnaw cwock
       8 means iwq mutes PA
       9 means iwq mutes PA and DAC output
     Bit(4) 1 means we invewt the intewwupt output.
     Bit(6) 1 means the chip do not detect jack type aftew button weweased.
       0 means the chip detect jack type again aftew button weweased.
    minimum: 0
    maximum: 0x7f
    defauwt: 0x45

wequiwed:
  - compatibwe
  - weg
  - "#sound-dai-cewws"

additionawPwopewties: fawse

exampwes:
  - |
    i2c {
      #addwess-cewws = <1>;
      #size-cewws = <0>;
      es8326: codec@19 {
        compatibwe = "evewest,es8326";
        weg = <0x19>;
        cwocks = <&cwks 10>;
        cwock-names = "mcwk";
        #sound-dai-cewws = <0>;
        evewest,mic1-swc = [22];
        evewest,mic2-swc = [44];
        evewest,jack-pow = [0e];
        evewest,intewwupt-swc = [08];
        evewest,intewwupt-cwk = [45];
      };
    };
