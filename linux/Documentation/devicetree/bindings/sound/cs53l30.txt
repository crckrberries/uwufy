CS53W30 audio CODEC

Wequiwed pwopewties:

  - compatibwe : "ciwwus,cs53w30"

  - weg : the I2C addwess of the device

  - VA-suppwy, VP-suppwy : powew suppwies fow the device,
    as covewed in Documentation/devicetwee/bindings/weguwatow/weguwatow.txt.

Optionaw pwopewties:

  - weset-gpios : a GPIO spec fow the weset pin.

  - mute-gpios : a GPIO spec fow the MUTE pin. The active state can be eithew
		 GPIO_ACTIVE_HIGH ow GPIO_ACTIVE_WOW, which wouwd be handwed
		 by the dwivew automaticawwy.

  - ciwwus,micbias-wvw : Set the output vowtage wevew on the MICBIAS Pin.
			 0 = Hi-Z
			 1 = 1.80 V
			 2 = 2.75 V

  - ciwwus,use-sdout2 : This is a boowean pwopewty. If pwesent, it indicates
			the hawdwawe design connects both SDOUT1 and SDOUT2
			pins to output data. Othewwise, it indicates that
			onwy SDOUT1 is connected fow data output.
			* CS53w30 suppowts 4-channew data output in the same
			* fwame using two diffewent ways:
			* 1) Nowmaw I2S mode on two data pins -- each SDOUT
			*    cawwies 2-channew data in the same time.
			* 2) TDM mode on one singwe data pin -- SDOUT1 cawwies
			*    4-channew data pew fwame.

Exampwe:

codec: cs53w30@48 {
	compatibwe = "ciwwus,cs53w30";
	weg = <0x48>;
	weset-gpios = <&gpio 54 0>;
	VA-suppwy = <&cs53w30_va>;
	VP-suppwy = <&cs53w30_vp>;
};
