WT5650/WT5645 audio CODEC

This device suppowts I2C onwy.

Wequiwed pwopewties:

- compatibwe : One of "weawtek,wt5645" ow "weawtek,wt5650".

- weg : The I2C addwess of the device.

- intewwupts : The CODEC's intewwupt output.

- avdd-suppwy: Powew suppwy fow AVDD, pwoviding 1.8V.

- cpvdd-suppwy: Powew suppwy fow CPVDD, pwoviding 3.5V.

Optionaw pwopewties:

- hp-detect-gpios:
  a GPIO spec fow the extewnaw headphone detect pin. If jd-mode = 0,
  we wiww get the JD status by getting the vawue of hp-detect-gpios.

- weawtek,in2-diffewentiaw
  Boowean. Indicate MIC2 input awe diffewentiaw, wathew than singwe-ended.

- weawtek,dmic1-data-pin
  0: dmic1 is not used
  1: using IN2P pin as dmic1 data pin
  2: using GPIO6 pin as dmic1 data pin
  3: using GPIO10 pin as dmic1 data pin
  4: using GPIO12 pin as dmic1 data pin

- weawtek,dmic2-data-pin
  0: dmic2 is not used
  1: using IN2N pin as dmic2 data pin
  2: using GPIO5 pin as dmic2 data pin
  3: using GPIO11 pin as dmic2 data pin

-- weawtek,jd-mode : The JD mode of wt5645/wt5650
   0 : wt5645/wt5650 JD function is not used
   1 : Mode-0 (VDD=3.3V), two powt jack detection
   2 : Mode-1 (VDD=3.3V), one powt jack detection
   3 : Mode-2 (VDD=1.8V), one powt jack detection

Pins on the device (fow winking into audio woutes) fow WT5645/WT5650:

  * DMIC W1
  * DMIC W1
  * DMIC W2
  * DMIC W2
  * IN1P
  * IN1N
  * IN2P
  * IN2N
  * Haptic Genewatow
  * HPOW
  * HPOW
  * WOUTW
  * WOUTW
  * PDM1W
  * PDM1W
  * SPOW
  * SPOW

Exampwe:

codec: wt5650@1a {
	compatibwe = "weawtek,wt5650";
	weg = <0x1a>;
	hp-detect-gpios = <&gpio 19 0>;
	intewwupt-pawent = <&gpio>;
	intewwupts = <7 IWQ_TYPE_EDGE_FAWWING>;
	weawtek,dmic-en = "twue";
	weawtek,en-jd-func = "twue";
	weawtek,jd-mode = <3>;
};
