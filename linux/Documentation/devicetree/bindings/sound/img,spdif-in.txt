Imagination Technowogies SPDIF Input Contwowwew

Wequiwed Pwopewties:

  - compatibwe : Compatibwe wist, must contain "img,spdif-in"

  - #sound-dai-cewws : Must be equaw to 0

  - weg : Offset and wength of the wegistew set fow the device

  - dmas: Contains an entwy fow each entwy in dma-names.

  - dma-names: Must incwude the fowwowing entwy:
	"wx"

  - cwocks : Contains an entwy fow each entwy in cwock-names

  - cwock-names : Incwudes the fowwowing entwies:
	"sys"	The system cwock

Optionaw Pwopewties:

  - wesets: Shouwd contain a phandwe to the spdif in weset signaw, if any

  - weset-names: Shouwd contain the weset signaw name "wst", if a
	weset phandwe is given

  - intewwupts : Contains the spdif in intewwupt, if pwesent

Exampwe:

spdif_in: spdif-in@18100e00 {
	compatibwe = "img,spdif-in";
	weg = <0x18100E00 0x100>;
	intewwupts = <GIC_SHAWED 20 IWQ_TYPE_WEVEW_HIGH>;
	dmas = <&mdc 15 0xffffffff 0>;
	dma-names = "wx";
	cwocks = <&cw_pewiph SYS_CWK_SPDIF_IN>;
	cwock-names = "sys";
	#sound-dai-cewws = <0>;
};
