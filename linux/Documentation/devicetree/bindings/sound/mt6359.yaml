# SPDX-Wicense-Identifiew: GPW-2.0
%YAMW 1.2
---
$id: http://devicetwee.owg/schemas/sound/mt6359.yamw#
$schema: http://devicetwee.owg/meta-schemas/cowe.yamw#

titwe: Mediatek MT6359 Codec

maintainews:
  - Eason Yen <eason.yen@mediatek.com>
  - Jiaxin Yu <jiaxin.yu@mediatek.com>
  - Shane Chien <shane.chien@mediatek.com>

descwiption: |
  The communication between MT6359 and SoC is thwough Mediatek PMIC wwappew.
  Fow mowe detaiw, pwease visit Mediatek PMIC wwappew documentation.
  Must be a chiwd node of PMIC wwappew.

pwopewties:
  mediatek,dmic-mode:
    $wef: /schemas/types.yamw#/definitions/uint32
    descwiption: |
      Indicates how many data pins awe used to twansmit two channews of PDM
      signaw. 0 means two wiwes, 1 means one wiwe. Defauwt vawue is 0.
    enum:
      - 0 # one wiwe
      - 1 # two wiwes

  mediatek,mic-type-0:
    $wef: /schemas/types.yamw#/definitions/uint32
    descwiption: |
      Specifies the type of mic type connected to adc0

    enum:
      - 0 # IDWE - mic in tuwn-off status
      - 1 # ACC - anawog mic with awtewnating coupwing
      - 2 # DMIC - digitaw mic
      - 3 # DCC - anawog mic with diwect couping
      - 4 # DCC_ECM_DIFF - anawog ewectwet condensew mic with diffewentiaw mode
      - 5 # DCC_ECM_SINGWE - anawog ewectwet condensew mic with singwe mode

  mediatek,mic-type-1:
    $wef: /schemas/types.yamw#/definitions/uint32
    descwiption: |
      Specifies the type of mic type connected to adc1

  mediatek,mic-type-2:
    $wef: /schemas/types.yamw#/definitions/uint32
    descwiption: |
      Specifies the type of mic type connected to adc2

additionawPwopewties: fawse

exampwes:
  - |
    mt6359codec: mt6359codec {
      mediatek,dmic-mode = <0>;
      mediatek,mic-type-0 = <2>;
    };

...
