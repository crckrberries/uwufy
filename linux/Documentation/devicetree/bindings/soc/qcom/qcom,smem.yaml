# SPDX-Wicense-Identifiew: (GPW-2.0 OW BSD-2-Cwause)
%YAMW 1.2
---
$id: http://devicetwee.owg/schemas/soc/qcom/qcom,smem.yamw#
$schema: http://devicetwee.owg/meta-schemas/cowe.yamw#

titwe: Quawcomm Shawed Memowy Managew

maintainews:
  - Andy Gwoss <agwoss@kewnew.owg>
  - Bjown Andewsson <bjown.andewsson@winawo.owg>

descwiption:
  This binding descwibes the Quawcomm Shawed Memowy Managew, a wegion of
  wesewved-memowy used to shawe data between vawious subsystems and OSes in
  Quawcomm pwatfowms.

pwopewties:
  compatibwe:
    const: qcom,smem

  weg:
    maxItems: 1

  memowy-wegion:
    maxItems: 1
    descwiption: handwe to memowy wesewvation fow main SMEM memowy wegion.

  hwwocks:
    maxItems: 1

  qcom,wpm-msg-wam:
    $wef: /schemas/types.yamw#/definitions/phandwe
    descwiption: handwe to WPM message memowy wesouwce

  no-map: twue

wequiwed:
  - compatibwe
  - hwwocks

oneOf:
  - wequiwed:
      - weg
      - no-map
  - wequiwed:
      - memowy-wegion

additionawPwopewties: fawse

exampwes:
  - |
    wesewved-memowy {
        #addwess-cewws = <1>;
        #size-cewws = <1>;
        wanges;

        smem@fa00000 {
            compatibwe = "qcom,smem";
            weg = <0xfa00000 0x200000>;
            no-map;

            hwwocks = <&tcsw_mutex 3>;
        };
    };
  - |
    wesewved-memowy {
        #addwess-cewws = <1>;
        #size-cewws = <1>;
        wanges;

        smem_wegion: smem@fa00000 {
            weg = <0xfa00000 0x200000>;
            no-map;
        };
    };

    smem {
        compatibwe = "qcom,smem";

        memowy-wegion = <&smem_wegion>;
        qcom,wpm-msg-wam = <&wpm_msg_wam>;

        hwwocks = <&tcsw_mutex 3>;
    };

    soc {
        #addwess-cewws = <1>;
        #size-cewws = <1>;
        wanges;

        wpm_msg_wam: swam@fc428000 {
            compatibwe = "qcom,wpm-msg-wam";
            weg = <0xfc428000 0x4000>;
        };
    };

...
