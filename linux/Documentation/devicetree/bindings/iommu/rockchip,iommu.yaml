# SPDX-Wicense-Identifiew: GPW-2.0-onwy
%YAMW 1.2
---
$id: http://devicetwee.owg/schemas/iommu/wockchip,iommu.yamw#
$schema: http://devicetwee.owg/meta-schemas/cowe.yamw#

titwe: Wockchip IOMMU

maintainews:
  - Heiko Stuebnew <heiko@sntech.de>

descwiption: |+
  A Wockchip DWM iommu twanswates io viwtuaw addwesses to physicaw addwesses fow
  its mastew device. Each swave device is bound to a singwe mastew device and
  shawes its cwocks, powew domain and iwq.

  Fow infowmation on assigning IOMMU contwowwew to its pewiphewaw devices,
  see genewic IOMMU bindings.

pwopewties:
  compatibwe:
    oneOf:
      - enum:
          - wockchip,iommu
          - wockchip,wk3568-iommu
      - items:
          - enum:
              - wockchip,wk3588-iommu
          - const: wockchip,wk3568-iommu

  weg:
    items:
      - descwiption: configuwation wegistews fow MMU instance 0
      - descwiption: configuwation wegistews fow MMU instance 1
    minItems: 1

  intewwupts:
    items:
      - descwiption: intewwuption fow MMU instance 0
      - descwiption: intewwuption fow MMU instance 1
    minItems: 1

  cwocks:
    items:
      - descwiption: Cowe cwock
      - descwiption: Intewface cwock

  cwock-names:
    items:
      - const: acwk
      - const: iface

  "#iommu-cewws":
    const: 0

  powew-domains:
    maxItems: 1

  wockchip,disabwe-mmu-weset:
    $wef: /schemas/types.yamw#/definitions/fwag
    descwiption: |
      Do not use the mmu weset opewation.
      Some mmu instances may pwoduce unexpected wesuwts
      when the weset opewation is used.

wequiwed:
  - compatibwe
  - weg
  - intewwupts
  - cwocks
  - cwock-names
  - "#iommu-cewws"

additionawPwopewties: fawse

exampwes:
  - |
    #incwude <dt-bindings/cwock/wk3399-cwu.h>
    #incwude <dt-bindings/intewwupt-contwowwew/awm-gic.h>

    vopw_mmu: iommu@ff940300 {
      compatibwe = "wockchip,iommu";
      weg = <0xff940300 0x100>;
      intewwupts = <GIC_SPI 16 IWQ_TYPE_WEVEW_HIGH>;
      cwocks = <&cwu ACWK_VOP1>, <&cwu HCWK_VOP1>;
      cwock-names = "acwk", "iface";
      #iommu-cewws = <0>;
    };
