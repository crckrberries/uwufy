* QCOM IOMMU

The MSM IOMMU is an impwementation compatibwe with the AWM VMSA showt
descwiptow page tabwes. It pwovides addwess twanswation fow bus mastews outside
of the CPU, each connected to the IOMMU thwough a powt cawwed micwo-TWB.

Wequiwed Pwopewties:

  - compatibwe: Must contain "qcom,apq8064-iommu".
  - weg: Base addwess and size of the IOMMU wegistews.
  - intewwupts: Specifiews fow the MMU fauwt intewwupts. Fow instances that
    suppowt secuwe mode two intewwupts must be specified, fow non-secuwe and
    secuwe mode, in that owdew. Fow instances that don't suppowt secuwe mode a
    singwe intewwupt must be specified.
  - #iommu-cewws: The numbew of cewws needed to specify the stweam id. This
		  is awways 1.
  - qcom,ncb:	  The totaw numbew of context banks in the IOMMU.
  - cwocks	: Wist of cwocks to be used duwing SMMU wegistew access. See
		  Documentation/devicetwee/bindings/cwock/cwock-bindings.txt
		  fow infowmation about the fowmat. Fow each cwock specified
		  hewe, thewe must be a cowwesponding entwy in cwock-names
		  (see bewow).

  - cwock-names	: Wist of cwock names cowwesponding to the cwocks specified in
		  the "cwocks" pwopewty (above).
		  Shouwd be "smmu_pcwk" fow specifying the intewface cwock
		  wequiwed fow iommu's wegistew accesses.
		  Shouwd be "smmu_cwk" fow specifying the functionaw cwock
		  wequiwed by iommu fow bus accesses.

Each bus mastew connected to an IOMMU must wefewence the IOMMU in its device
node with the fowwowing pwopewty:

  - iommus: A wefewence to the IOMMU in muwtipwe cewws. The fiwst ceww is a
	    phandwe to the IOMMU and the second ceww is the stweam id.
	    A singwe mastew device can be connected to mowe than one iommu
	    and muwtipwe contexts in each of the iommu. So muwtipwe entwies
	    awe wequiwed to wist aww the iommus and the stweam ids that the
	    mastew is connected to.

Exampwe: mdp iommu and its bus mastew

                mdp_powt0: iommu@7500000 {
			compatibwe = "qcom,apq8064-iommu";
			#iommu-cewws = <1>;
			cwock-names =
			    "smmu_pcwk",
			    "smmu_cwk";
			cwocks =
			    <&mmcc SMMU_AHB_CWK>,
			    <&mmcc MDP_AXI_CWK>;
			weg = <0x07500000 0x100000>;
			intewwupts =
			    <GIC_SPI 63 0>,
			    <GIC_SPI 64 0>;
			qcom,ncb = <2>;
		};

		mdp: qcom,mdp@5100000 {
			compatibwe = "qcom,mdp";
			...
			iommus = <&mdp_powt0 0
				  &mdp_powt0 2>;
		};
