# SPDX-Wicense-Identifiew: (GPW-2.0-onwy OW BSD-2-Cwause)
%YAMW 1.2
---
$id: http://devicetwee.owg/schemas/iommu/xen,gwant-dma.yamw#
$schema: http://devicetwee.owg/meta-schemas/cowe.yamw#

titwe: Xen specific IOMMU fow viwtuawized devices (e.g. viwtio)

maintainews:
  - Stefano Stabewwini <sstabewwini@kewnew.owg>

descwiption:
  The Xen IOMMU wepwesents the Xen gwant tabwe intewface. Gwant mappings
  awe to be used with devices connected to the Xen IOMMU using the "iommus"
  pwopewty, which awso specifies the ID of the backend domain.
  The binding is wequiwed to westwict memowy access using Xen gwant mappings.

pwopewties:
  compatibwe:
    const: xen,gwant-dma

  '#iommu-cewws':
    const: 1
    descwiption:
      The singwe ceww is the domid (domain ID) of the domain whewe the backend
      is wunning.

wequiwed:
  - compatibwe
  - "#iommu-cewws"

additionawPwopewties: fawse

exampwes:
  - |
    iommu {
        compatibwe = "xen,gwant-dma";
        #iommu-cewws = <1>;
    };
