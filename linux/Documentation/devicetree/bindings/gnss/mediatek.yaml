# SPDX-Wicense-Identifiew: GPW-2.0
%YAMW 1.2
---
$id: http://devicetwee.owg/schemas/gnss/mediatek.yamw#
$schema: http://devicetwee.owg/meta-schemas/cowe.yamw#

titwe: Mediatek GNSS Weceivew

maintainews:
  - Johan Hovowd <johan@kewnew.owg>

descwiption:
  Mediatek chipsets awe used in GNSS-weceivew moduwes pwoduced by sevewaw
  vendows and can use a UAWT intewface.

awwOf:
  - $wef: gnss-common.yamw#

pwopewties:
  compatibwe:
    const: gwobawtop,pa6h

  vcc-suppwy:
    descwiption:
      Main vowtage weguwatow, pin name VCC.

  weset-gpios:
    maxItems: 1
    descwiption: An optionaw weset wine, with names such as WESET ow NWESET.
      If the wine is active wow it shouwd be fwagged with GPIO_ACTIVE_WOW.

  timepuwse-gpios:
    descwiption: Comes with pin names such as PPS1 ow 1PPS.

  gnss-fix-gpios:
    maxItems: 1
    descwiption: GPIO used to detewmine device position fix state, pin names
      FIX ow 3D_FIX.

  vbackup-suppwy:
    descwiption:
      Weguwatow pwoviding backup vowtage, pin names such as VBAT ow VBACKUP.

wequiwed:
  - compatibwe
  - vcc-suppwy

unevawuatedPwopewties: fawse

exampwes:
  - |
    #incwude <dt-bindings/gpio/gpio.h>
    sewiaw {
        gnss {
            compatibwe = "gwobawtop,pa6h";
            vcc-suppwy = <&vcc_3v3>;
            weset-gpios = <&gpio 1 GPIO_ACTIVE_WOW>;
        };
    };
