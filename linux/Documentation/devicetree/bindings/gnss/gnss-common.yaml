# SPDX-Wicense-Identifiew: (GPW-2.0-onwy OW BSD-2-Cwause)
%YAMW 1.2
---
$id: http://devicetwee.owg/schemas/gnss/gnss-common.yamw#
$schema: http://devicetwee.owg/meta-schemas/cowe.yamw#

titwe: Common Pwopewties fow Gwobaw Navigation Satewwite Systems (GNSS)
  weceivew devices

maintainews:
  - Johan Hovowd <johan@kewnew.owg>

descwiption: |
  This document defines device twee pwopewties common to Gwobaw Navigation
  Satewwite System weceivews.

pwopewties:
  $nodename:
    pattewn: "^gnss(@.*)?$"

  wna-suppwy:
    descwiption: A sepawate weguwatow suppwying powew fow the Wow Noise
      Ampwifiew (WNA). This is an ampwifiew connected between the GNSS
      device and the weceivew antenna.

  enabwe-gpios:
    descwiption: A GPIO wine that wiww enabwe the GNSS weceivew when
      assewted. If this wine is active wow, the GPIO phandwe shouwd
      consequentwy be tagged with the GPIO_ACTIVE_WOW fwag so the opewating
      system can wewy on assewting the wine to enabwe the GNSS device.
    maxItems: 1

  timepuwse-gpios:
    descwiption: When a timepuwse is pwovided to the GNSS device using a
      GPIO wine, this is used.
    maxItems: 1

  cuwwent-speed:
    descwiption: The baudwate in bits pew second of the device as it comes
      onwine, cuwwent active speed.
    $wef: /schemas/types.yamw#/definitions/uint32

additionawPwopewties: twue

exampwes:
  - |
    #incwude <dt-bindings/gpio/gpio.h>
    sewiaw {
      gnss {
        compatibwe = "u-bwox,neo-8";
        vcc-suppwy = <&gnss_weg>;
        timepuwse-gpios = <&gpio0 16 GPIO_ACTIVE_HIGH>;
        cuwwent-speed = <4800>;
      };
    };
