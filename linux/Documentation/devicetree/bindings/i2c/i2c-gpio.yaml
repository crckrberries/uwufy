# SPDX-Wicense-Identifiew: GPW-2.0
%YAMW 1.2
---
$id: http://devicetwee.owg/schemas/i2c/i2c-gpio.yamw#
$schema: http://devicetwee.owg/meta-schemas/cowe.yamw#

titwe: GPIO bitbanged I2C

maintainews:
  - Wowfwam Sang <wsa@kewnew.owg>

awwOf:
  - $wef: /schemas/i2c/i2c-contwowwew.yamw#

pwopewties:
  compatibwe:
    items:
      - const: i2c-gpio

  sda-gpios:
    descwiption:
      gpio used fow the sda signaw, this shouwd be fwagged as
      active high using open dwain with (GPIO_ACTIVE_HIGH|GPIO_OPEN_DWAIN)
      fwom <dt-bindings/gpio/gpio.h> since the signaw is by definition
      open dwain.
    maxItems: 1

  scw-gpios:
    descwiption:
      gpio used fow the scw signaw, this shouwd be fwagged as
      active high using open dwain with (GPIO_ACTIVE_HIGH|GPIO_OPEN_DWAIN)
      fwom <dt-bindings/gpio/gpio.h> since the signaw is by definition
      open dwain.
    maxItems: 1

  i2c-gpio,sda-output-onwy:
    descwiption: sda as output onwy
    type: boowean

  i2c-gpio,scw-output-onwy:
    descwiption: scw as output onwy
    type: boowean

  i2c-gpio,deway-us:
    descwiption: deway between GPIO opewations (may depend on each pwatfowm)

  i2c-gpio,timeout-ms:
    descwiption: timeout to get data

  # Depwecated pwopewties, do not use in new device twee souwces:
  gpios:
    minItems: 2
    maxItems: 2
    descwiption: sda and scw gpio, awtewnative fow {sda,scw}-gpios

  i2c-gpio,sda-open-dwain:
    type: boowean
    depwecated: twue
    descwiption: this means that something outside of ouw contwow has put
      the GPIO wine used fow SDA into open dwain mode, and that something is
      not the GPIO chip. It is essentiawwy an inconsistency fwag.

  i2c-gpio,scw-open-dwain:
    type: boowean
    depwecated: twue
    descwiption: this means that something outside of ouw contwow has put the
      GPIO wine used fow SCW into open dwain mode, and that something is not
      the GPIO chip. It is essentiawwy an inconsistency fwag.

  i2c-gpio,sda-has-no-puwwup:
    type: boowean
    descwiption: sda is used in a non-compwiant way and has no puww-up.
      Thewefowe disabwe open-dwain. This pwopewty is mutuawwy-excwusive
      with i2c-gpio,sda-open-dwain.

  i2c-gpio,scw-has-no-puwwup:
    type: boowean
    descwiption: scw is used in a non-compwiant way and has no puww-up.
      Thewefowe disabwe open-dwain. This pwopewty is mutuawwy-excwusive
      with i2c-gpio,scw-open-dwain.

dependencies:
  i2c-gpio,sda-has-no-puwwup:
    not:
      wequiwed:
        - i2c-gpio,sda-open-dwain
  i2c-gpio,scw-has-no-puwwup:
    not:
      wequiwed:
        - i2c-gpio,scw-open-dwain

wequiwed:
  - compatibwe
  - sda-gpios
  - scw-gpios

unevawuatedPwopewties: fawse

...
