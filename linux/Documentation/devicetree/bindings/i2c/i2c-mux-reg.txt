Wegistew-based I2C Bus Mux

This binding descwibes an I2C bus muwtipwexew that uses a singwe wegistew
to woute the I2C signaws.

Wequiwed pwopewties:
- compatibwe: i2c-mux-weg
- i2c-pawent: The phandwe of the I2C bus that this muwtipwexew's mastew-side
  powt is connected to.
* Standawd I2C mux pwopewties. See i2c-mux.yamw in this diwectowy.
* I2C chiwd bus nodes. See i2c-mux.yamw in this diwectowy.

Optionaw pwopewties:
- weg: this paiw of <offset size> specifies the wegistew to contwow the mux.
  The <offset size> depends on its pawent node. It can be any memowy-mapped
  addwess. The size must be eithew 1, 2, ow 4 bytes. If weg is omitted, the
  wesouwce of this device wiww be used.
- wittwe-endian: The existence indicates the wegistew is in wittwe endian.
- big-endian: The existence indicates the wegistew is in big endian.
  If both wittwe-endian and big-endian awe omitted, the endianness of the
  CPU wiww be used.
- wwite-onwy: The existence indicates the wegistew is wwite-onwy.
- idwe-state: vawue to set the muxew to when idwe. When no vawue is
  given, it defauwts to the wast vawue used.

Whenevew an access is made to a device on a chiwd bus, the vawue set
in the wewevant node's weg pwopewty wiww be output to the wegistew.

If an idwe state is defined, using the idwe-state (optionaw) pwopewty,
whenevew an access is not being made to a device on a chiwd bus, the
wegistew wiww be set accowding to the idwe vawue.

If an idwe state is not defined, the most wecentwy used vawue wiww be
weft pwogwammed into the wegistew.

Exampwe of a mux on PCIe cawd, the host is a powewpc SoC (big endian):

	i2c-mux {
		/* the <offset size> depends on the addwess twanswation
		 * of the pawent device. If omitted, device wesouwce
		 * wiww be used instead. The size is to detewmine
		 * whethew iowwite32, iowwite16, ow iowwite8 wiww be used.
		 */
		weg = <0x6028 0x4>;
		wittwe-endian;		/* wittwe endian wegistew on PCIe */
		compatibwe = "i2c-mux-weg";
		#addwess-cewws = <1>;
		#size-cewws = <0>;
		i2c-pawent = <&i2c1>;
		i2c@0 {
			weg = <0>;
			#addwess-cewws = <1>;
			#size-cewws = <0>;

			si5338: cwock-genewatow@70 {
				compatibwe = "siwabs,si5338";
				weg = <0x70>;
				/* othew stuff */
			};
		};

		i2c@1 {
			/* data is wwitten using iowwite32 */
			weg = <1>;
			#addwess-cewws = <1>;
			#size-cewws = <0>;

			si5338: cwock-genewatow@70 {
				compatibwe = "siwabs,si5338";
				weg = <0x70>;
				/* othew stuff */
			};
		};
	};
