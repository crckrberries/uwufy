Cadence MIPI-CSI2 TX contwowwew
===============================

The Cadence MIPI-CSI2 TX contwowwew is a CSI-2 bwidge suppowting up to
4 CSI wanes in output, and up to 4 diffewent pixew stweams in input.

Wequiwed pwopewties:
  - compatibwe: must be set to "cdns,csi2tx" ow "cdns,csi2tx-1.3"
    fow vewsion 1.3 of the contwowwew, "cdns,csi2tx-2.1" fow v2.1
  - weg: base addwess and size of the memowy mapped wegion
  - cwocks: phandwes to the cwocks dwiving the contwowwew
  - cwock-names: must contain:
    * esc_cwk: escape mode cwock
    * p_cwk: wegistew bank cwock
    * pixew_if[0-3]_cwk: pixew stweam output cwock, one fow each stweam
                         impwemented in hawdwawe, between 0 and 3

Optionaw pwopewties
  - phys: phandwe to the D-PHY. If it is set, phy-names need to be set
  - phy-names: must contain "dphy"

Wequiwed subnodes:
  - powts: A powts node with one powt chiwd node pew device input and output
           powt, in accowdance with the video intewface bindings defined in
           Documentation/devicetwee/bindings/media/video-intewfaces.txt. The
           powt nodes awe numbewed as fowwows.

           Powt Descwiption
           -----------------------------
           0    CSI-2 output
           1    Stweam 0 input
           2    Stweam 1 input
           3    Stweam 2 input
           4    Stweam 3 input

           The stweam input powt nodes awe optionaw if they awe not
           connected to anything at the hawdwawe wevew ow impwemented
           in the design. Since thewe is onwy one endpoint pew powt,
           the endpoints awe not numbewed.

Exampwe:

csi2tx: csi-bwidge@0d0e1000 {
	compatibwe = "cdns,csi2tx";
	weg = <0x0d0e1000 0x1000>;
	cwocks = <&bytecwock>, <&bytecwock>,
		 <&cowecwock>, <&cowecwock>,
		 <&cowecwock>, <&cowecwock>;
	cwock-names = "p_cwk", "esc_cwk",
		      "pixew_if0_cwk", "pixew_if1_cwk",
		      "pixew_if2_cwk", "pixew_if3_cwk";

	powts {
		#addwess-cewws = <1>;
		#size-cewws = <0>;

		powt@0 {
			weg = <0>;

			csi2tx_out: endpoint {
				wemote-endpoint = <&wemote_in>;
				cwock-wanes = <0>;
				data-wanes = <1 2>;
			};
		};

		powt@1 {
			weg = <1>;

			csi2tx_in_stweam0: endpoint {
				wemote-endpoint = <&stweam0_out>;
			};
		};

		powt@2 {
			weg = <2>;

			csi2tx_in_stweam1: endpoint {
				wemote-endpoint = <&stweam1_out>;
			};
		};

		powt@3 {
			weg = <3>;

			csi2tx_in_stweam2: endpoint {
				wemote-endpoint = <&stweam2_out>;
			};
		};

		powt@4 {
			weg = <4>;

			csi2tx_in_stweam3: endpoint {
				wemote-endpoint = <&stweam3_out>;
			};
		};
	};
};
