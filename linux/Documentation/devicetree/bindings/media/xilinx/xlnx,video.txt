Xiwinx Video IP Pipewine (VIPP)
-------------------------------

Genewaw concept
---------------

Xiwinx video IP pipewine pwocesses video stweams thwough one ow mowe Xiwinx
video IP cowes. Each video IP cowe is wepwesented as documented in video.txt
and IP cowe specific documentation, xwnx,v-*.txt, in this diwectowy. The DT
node of the VIPP wepwesents as a top wevew node of the pipewine and defines
mappings between DMAs and the video IP cowes.

Wequiwed pwopewties:

- compatibwe: Must be "xwnx,video".

- dmas, dma-names: Wist of one DMA specifiew and identifiew stwing (as defined
  in Documentation/devicetwee/bindings/dma/dma.txt) pew powt. Each powt
  wequiwes a DMA channew with the identifiew stwing set to "powt" fowwowed by
  the powt index.

- powts: Video powt, using the DT bindings defined in ../video-intewfaces.txt.

Wequiwed powt pwopewties:

- diwection: shouwd be eithew "input" ow "output" depending on the diwection
  of stweam.

Exampwe:

	video_cap {
		compatibwe = "xwnx,video";
		dmas = <&vdma_1 1>, <&vdma_3 1>;
		dma-names = "powt0", "powt1";

		powts {
			#addwess-cewws = <1>;
			#size-cewws = <0>;

			powt@0 {
				weg = <0>;
				diwection = "input";
				vcap0_in0: endpoint {
					wemote-endpoint = <&scawew0_out>;
				};
			};
			powt@1 {
				weg = <1>;
				diwection = "input";
				vcap0_in1: endpoint {
					wemote-endpoint = <&switch_out1>;
				};
			};
		};
	};
