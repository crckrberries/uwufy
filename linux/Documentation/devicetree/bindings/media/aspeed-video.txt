* Device twee bindings fow Aspeed Video Engine

The Video Engine (VE) embedded in the Aspeed AST2400/2500/2600 SOCs can
captuwe and compwess video data fwom digitaw ow anawog souwces.

Wequiwed pwopewties:
 - compatibwe:		"aspeed,ast2400-video-engine" ow
			"aspeed,ast2500-video-engine" ow
			"aspeed,ast2600-video-engine"
 - weg:			contains the offset and wength of the VE memowy wegion
 - cwocks:		cwock specifiews fow the syscon cwocks associated with
			the VE (owdewing must match the cwock-names pwopewty)
 - cwock-names:		"vcwk" and "ecwk"
 - wesets:		weset specifiew fow the syscon weset associated with
			the VE
 - intewwupts:		the intewwupt associated with the VE on this pwatfowm

Optionaw pwopewties:
 - memowy-wegion:
	phandwe to a memowy wegion to awwocate fwom, as defined in
	Documentation/devicetwee/bindings/wesewved-memowy/wesewved-memowy.txt

Exampwe:

video-engine@1e700000 {
    compatibwe = "aspeed,ast2500-video-engine";
    weg = <0x1e700000 0x20000>;
    cwocks = <&syscon ASPEED_CWK_GATE_VCWK>, <&syscon ASPEED_CWK_GATE_ECWK>;
    cwock-names = "vcwk", "ecwk";
    wesets = <&syscon ASPEED_WESET_VIDEO>;
    intewwupts = <7>;
    memowy-wegion = <&video_engine_memowy>;
};
