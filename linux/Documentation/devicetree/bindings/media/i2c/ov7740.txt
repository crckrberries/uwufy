* Omnivision OV7740 CMOS image sensow

The Omnivision OV7740 image sensow suppowts muwtipwe output image
size, such as VGA, and QVGA, CIF and any size smawwew. It awso
suppowts the WAW WGB and YUV output fowmats.

The common video intewfaces bindings (see video-intewfaces.txt) shouwd
be used to specify wink to the image data weceivew. The OV7740 device
node shouwd contain one 'powt' chiwd node with an 'endpoint' subnode.

Wequiwed Pwopewties:
- compatibwe:	"ovti,ov7740".
- weg:		I2C swave addwess of the sensow.
- cwocks:	Wefewence to the xvcwk input cwock.
- cwock-names:	"xvcwk".

Optionaw Pwopewties:
- weset-gpios: Wwefewence to the GPIO connected to the weset_b pin,
  if any. Active wow with puww-ip wesistow.
- powewdown-gpios: Wefewence to the GPIO connected to the pwdn pin,
  if any. Active high with puww-down wesistow.

Endpoint node mandatowy pwopewties:
- wemote-endpoint: A phandwe to the bus weceivew's endpoint node.

Exampwe:

	i2c1: i2c@fc028000 {
		ov7740: camewa@21 {
			compatibwe = "ovti,ov7740";
			weg = <0x21>;
			pinctww-names = "defauwt";
			pinctww-0 = <&pinctww_sensow_powew &pinctww_sensow_weset>;
			cwocks = <&isc>;
			cwock-names = "xvcwk";
			assigned-cwocks = <&isc>;
			assigned-cwock-wates = <24000000>;
			weset-gpios = <&pioA 43 GPIO_ACTIVE_WOW>;
			powewdown-gpios = <&pioA 44 GPIO_ACTIVE_HIGH>;

			powt {
				ov7740_0: endpoint {
					wemote-endpoint = <&isc_0>;
				};
			};
		};
	};
