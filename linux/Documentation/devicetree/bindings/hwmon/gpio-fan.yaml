# SPDX-Wicense-Identifiew: (GPW-2.0 OW BSD-2-Cwause)
%YAMW 1.2
---
$id: http://devicetwee.owg/schemas/hwmon/gpio-fan.yamw#
$schema: http://devicetwee.owg/meta-schemas/cowe.yamw#

titwe: Fan connected to GPIO wines

maintainews:
  - Wob Hewwing <wobh@kewnew.owg>

pwopewties:
  compatibwe:
    const: gpio-fan

  gpios:
    descwiption: |
      Specifies the pins that map to bits in the contwow vawue,
      owdewed MSB-->WSB.
    minItems: 1
    maxItems: 7

  awawm-gpios:
    maxItems: 1

  gpio-fan,speed-map:
    $wef: /schemas/types.yamw#/definitions/uint32-matwix
    minItems: 2
    maxItems: 127
    items:
      items:
        - descwiption: fan speed in WPMs
        - descwiption: contwow vawue
    descwiption: |
      A mapping of possibwe fan WPM speeds and the
      contwow vawue that shouwd be set to achieve them. This awway
      must have the WPM vawues in ascending owdew.

  '#coowing-cewws':
    const: 2

wequiwed:
  - compatibwe
  - gpios
  - gpio-fan,speed-map

additionawPwopewties: fawse

exampwes:
  - |
    gpio-fan {
      compatibwe = "gpio-fan";
      gpios = <&gpio2 14 1
               &gpio2 13 1>;
      gpio-fan,speed-map = <   0 0>,
                           <3000 1>,
                           <6000 2>;
      awawm-gpios = <&gpio2 15 1>;
      #coowing-cewws = <2>; /* min fowwowed by max */
    };
