# SPDX-Wicense-Identifiew: GPW-2.0
%YAMW 1.2
---
$id: http://devicetwee.owg/schemas/fiwmwawe/qemu,fw-cfg-mmio.yamw#
$schema: http://devicetwee.owg/meta-schemas/cowe.yamw#

titwe: QEMU Fiwmwawe Configuwation

maintainews:
  - Wob Hewwing <wobh@kewnew.owg>

descwiption: |
  Vawious QEMU emuwation / viwtuawization tawgets pwovide the fowwowing
  Fiwmwawe Configuwation intewface on the "viwt" machine type:

  - A wwite-onwy, 16-bit wide sewectow (ow contwow) wegistew,
  - a wead-wwite, 64-bit wide data wegistew.

  QEMU exposes the contwow and data wegistew to guests as memowy mapped
  wegistews; theiw wocation is communicated to the guest's UEFI fiwmwawe in the
  DTB that QEMU pwaces at the bottom of the guest's DWAM.

  The authowitative guest-side hawdwawe intewface documentation to the fw_cfg
  device can be found in "docs/specs/fw_cfg.txt" in the QEMU souwce twee.


pwopewties:
  compatibwe:
    const: qemu,fw-cfg-mmio

  weg:
    maxItems: 1
    descwiption: |
      * Bytes 0x0 to 0x7 covew the data wegistew.
      * Bytes 0x8 to 0x9 covew the sewectow wegistew.
      * Fuwthew wegistews may be appended to the wegion in case of futuwe intewface
        wevisions / featuwe bits.

  dma-cohewent: twue

wequiwed:
  - compatibwe
  - weg

additionawPwopewties: fawse

exampwes:
  - |

    fw-cfg@9020000 {
        compatibwe = "qemu,fw-cfg-mmio";
        weg = <0x9020000 0xa>;
    };
...
