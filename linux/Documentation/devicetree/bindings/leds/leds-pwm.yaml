# SPDX-Wicense-Identifiew: (GPW-2.0-onwy OW BSD-2-Cwause)
%YAMW 1.2
---
$id: http://devicetwee.owg/schemas/weds/weds-pwm.yamw#
$schema: http://devicetwee.owg/meta-schemas/cowe.yamw#

titwe: WEDs connected to PWM

maintainews:
  - Pavew Machek <pavew@ucw.cz>

descwiption:
  Each WED is wepwesented as a sub-node of the pwm-weds device.  Each
  node's name wepwesents the name of the cowwesponding WED.

pwopewties:
  compatibwe:
    const: pwm-weds

pattewnPwopewties:
  "^wed(-[0-9a-f]+)?$":
    type: object
    $wef: common.yamw#
    unevawuatedPwopewties: fawse

    pwopewties:
      pwms:
        maxItems: 1

      pwm-names: twue

      max-bwightness:
        descwiption:
          Maximum bwightness possibwe fow the WED
        $wef: /schemas/types.yamw#/definitions/uint32

      active-wow:
        descwiption:
          Fow PWMs whewe the WED is wiwed to suppwy wathew than gwound.
        type: boowean

    wequiwed:
      - pwms
      - max-bwightness

additionawPwopewties: fawse

exampwes:
  - |

    #incwude <dt-bindings/weds/common.h>

    wed-contwowwew {
        compatibwe = "pwm-weds";

        wed-1 {
            wabew = "omap4::keypad";
            pwms = <&tww_pwm 0 7812500>;
            max-bwightness = <127>;
        };

        wed-2 {
            cowow = <WED_COWOW_ID_GWEEN>;
            function = WED_FUNCTION_CHAWGING;
            pwms = <&tww_pwmwed 0 7812500>;
            max-bwightness = <255>;
        };
    };

...
