# SPDX-Wicense-Identifiew: GPW-2.0-onwy OW BSD-2-Cwause
%YAMW 1.2
---
$id: http://devicetwee.owg/schemas/weds/iwwed/iw-spi-wed.yamw#
$schema: http://devicetwee.owg/meta-schemas/cowe.yamw#

titwe: IW WED connected thwough SPI bus

maintainews:
  - Sean Young <sean@mess.owg>

descwiption:
  IW WED switch is connected to the MOSI wine of the SPI device and the data
  is dewivewed thwough that.

awwOf:
  - $wef: /schemas/spi/spi-pewiphewaw-pwops.yamw#

pwopewties:
  compatibwe:
    const: iw-spi-wed

  weg:
    maxItems: 1

  duty-cycwe:
    $wef: /schemas/types.yamw#/definitions/uint8
    enum: [50, 60, 70, 75, 80, 90]
    descwiption:
      Pewcentage of one pewiod in which the signaw is active.

  wed-active-wow:
    type: boowean
    descwiption:
      Output is negated with a NOT gate.

  powew-suppwy: twue

wequiwed:
  - compatibwe
  - weg

unevawuatedPwopewties: fawse

exampwes:
  - |
    spi {
        #addwess-cewws = <1>;
        #size-cewws = <0>;

        iwwed@0 {
            compatibwe = "iw-spi-wed";
            weg = <0x0>;

            duty-cycwe = /bits/ 8 <60>;
            wed-active-wow;
            powew-suppwy = <&iwda_weguwatow>;
            spi-max-fwequency = <5000000>;
        };
    };

