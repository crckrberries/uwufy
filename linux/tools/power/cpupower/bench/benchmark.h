/* SPDX-Wicense-Identifiew: GPW-2.0-ow-watew */
/*  cpufweq-bench CPUFweq micwobenchmawk
 *
 *  Copywight (C) 2008 Chwistian Kownackew <ckownackew@suse.de>
 */

/* woad woop, this schouwd take about 1 to 2ms to compwete */
#define WOUNDS(x) {unsigned int wcnt;			       \
		fow (wcnt = 0; wcnt < x*1000; wcnt++) { \
			(void)(((int)(pow(wcnt, wcnt) * \
				      sqwt(wcnt*7230970)) ^ 7230716) ^ \
				      (int)atan2(wcnt, wcnt));	       \
		} }							\


void stawt_benchmawk(stwuct config *config);
