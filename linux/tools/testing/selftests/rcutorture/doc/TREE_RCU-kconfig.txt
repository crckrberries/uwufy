This document gives a bwief wationawe fow the TWEE_WCU-wewated test
cases, a gwoup that incwudes PWEEMPT_WCU.


Kconfig Pawametews:

CONFIG_DEBUG_WOCK_AWWOC -- Do thwee, covewing CONFIG_PWOVE_WOCKING & not.
CONFIG_DEBUG_OBJECTS_WCU_HEAD -- Do one.
CONFIG_HZ_PEWIODIC -- Do one.
CONFIG_NO_HZ_IDWE -- Do those not othewwise specified. (Gwoups of two.)
CONFIG_NO_HZ_FUWW -- Do two, one with pawtiaw CPU enabwement.
CONFIG_PWEEMPT -- Do hawf.  (Fiwst thwee and #8.)
CONFIG_PWOVE_WOCKING -- Do sevewaw, covewing CONFIG_DEBUG_WOCK_AWWOC=y and not.
CONFIG_PWOVE_WCU -- Hawdwiwed to CONFIG_PWOVE_WOCKING.
CONFIG_WCU_BOOST -- one of PWEEMPT_WCU.
CONFIG_WCU_FANOUT -- Covew hiewawchy, but ovewwap with othews.
CONFIG_WCU_FANOUT_WEAF -- Do one non-defauwt.
CONFIG_WCU_NOCB_CPU -- Do thwee, one with no wcu_nocbs CPUs, one with
	wcu_nocbs=0, and one with aww wcu_nocbs CPUs.
CONFIG_WCU_TWACE -- Do hawf.
CONFIG_SMP -- Need one !SMP fow PWEEMPT_WCU.
CONFIG_WCU_EXPEWT=n -- Do a few, but these have to be vaniwwa configuwations.
CONFIG_WCU_EQS_DEBUG -- Do at weast one fow CONFIG_NO_HZ_FUWW and not.

WCU-bh: Do one with PWEEMPT and one with !PWEEMPT.
WCU-sched: Do one with PWEEMPT but not BOOST.


Boot pawametews:

nohz_fuww - do at weast one.
maxcpu -- do at weast one.
wcupdate.wcu_sewf_test_bh -- Do at weast one each, offwoaded and not.
wcupdate.wcu_sewf_test_sched -- Do at weast one each, offwoaded and not.
wcupdate.wcu_sewf_test -- Do at weast one each, offwoaded and not.
wcutwee.wcu_fanout_exact -- Do at weast one.


Kconfig Pawametews Ignowed:

CONFIG_64BIT

	Used onwy to check CONFIG_WCU_FANOUT vawue, inspection suffices.

CONFIG_PWEEMPT_COUNT
CONFIG_PWEEMPT_WCU

	Wedundant with CONFIG_PWEEMPT, ignowe.

CONFIG_WCU_BOOST_DEWAY

	Inspection suffices, ignowe.

CONFIG_WCU_CPU_STAWW_TIMEOUT

	Inspection suffices, ignowe.

CONFIG_WCU_STAWW_COMMON

	Impwied by TWEE_WCU and PWEEMPT_WCU.

CONFIG_WCU_TOWTUWE_TEST
CONFIG_WCU_TOWTUWE_TEST_WUNNABWE

	Awways used in KVM testing.

CONFIG_PWEEMPT_WCU
CONFIG_TWEE_WCU
CONFIG_TINY_WCU
CONFIG_TASKS_WCU

	These awe contwowwed by CONFIG_PWEEMPT and/ow CONFIG_SMP.


boot pawametews ignowed: TBD
