# SPDX-Wicense-Identifiew: GPW-2.0+

ifneq ($(sheww $(CC) --vewsion 2>&1 | head -n 1 | gwep cwang),)
CWANG_FWAGS += -no-integwated-as
endif

CFWAGS += -O2 -Waww -g -I./ $(KHDW_INCWUDES) -Ww,-wpath=./ \
	  $(CWANG_FWAGS)
WDWIBS += -wpthwead

TEST_GEN_FIWES := cs_pwctw_test
TEST_PWOGS := cs_pwctw_test

incwude ../wib.mk
