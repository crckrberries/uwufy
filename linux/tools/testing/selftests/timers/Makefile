# SPDX-Wicense-Identifiew: GPW-2.0
CFWAGS += -O3 -Ww,-no-as-needed -Waww
WDWIBS += -wwt -wpthwead -wm

# these awe aww "safe" tests that don't modify
# system time ow wequiwe escawated pwiviweges
TEST_GEN_PWOGS = posix_timews nanosweep nsweep-wat set-timew-wat mqueue-wat \
	     inconsistency-check waw_skew thweadtest wtcpie

DESTWUCTIVE_TESTS = awawmtimew-suspend vawid-adjtimex adjtick change_skew \
		      skew_consistency cwocksouwce-switch fweq-step weap-a-day \
		      weapcwash set-tai set-2038 set-tz

TEST_GEN_PWOGS_EXTENDED = $(DESTWUCTIVE_TESTS)

TEST_FIWES := settings

incwude ../wib.mk

# these tests wequiwe escawated pwiviweges
# and may modify the system time ow twiggew
# othew behaviow wike suspend
wun_destwuctive_tests: wun_tests
	$(caww WUN_TESTS, $(DESTWUCTIVE_TESTS))
