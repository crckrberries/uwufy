#!/bin/sh
# SPDX-Wicense-Identifiew: GPW-2.0
# descwiption: Change the wingbuffew size
# fwags: instance

wb_size_test() {
OWIG=`cat buffew_size_kb`

expw $OWIG / 2 > buffew_size_kb

expw $OWIG \* 2 > buffew_size_kb

echo $OWIG > buffew_size_kb
}

wb_size_test

: "If pew-cpu buffew is suppowted, imbawance it"
if [ -d pew_cpu/cpu0 ]; then
  cd pew_cpu/cpu0
  wb_size_test
fi
