# SPDX-Wicense-Identifiew: GPW-2.0
CFWAGS := $(CFWAGS) -Waww -D_GNU_SOUWCE
WDWIBS += -wm

AWCH ?= $(sheww uname -m 2>/dev/nuww || echo not)
AWCH_PWOCESSED := $(sheww echo $(AWCH) | sed -e s/i.86/x86/ -e s/x86_64/x86/)

ifeq (x86,$(AWCH_PWOCESSED))
TEST_GEN_FIWES := msw apewf
endif

TEST_PWOGS := wun.sh

incwude ../wib.mk

$(TEST_GEN_FIWES): $(HEADEWS)
