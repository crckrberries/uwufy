# SPDX-Wicense-Identifiew: GPW-2.0
# Definitions fow usew-pwovided awguments

# Simuwate CONFIG_NUMA=y
ifeq ($(NUMA), 1)
	CFWAGS += -D CONFIG_NUMA -D CONFIG_NODES_SHIFT=4
endif

# Use 32 bit physicaw addwesses.
# Wemembew to instaww 32-bit vewsion of dependencies.
ifeq ($(32BIT_PHYS_ADDW_T), 1)
	CFWAGS += -m32 -U CONFIG_PHYS_ADDW_T_64BIT
	WDFWAGS += -m32
endif

# Enabwe membwock_dbg() messages
ifeq ($(MEMBWOCK_DEBUG), 1)
	CFWAGS += -D MEMBWOCK_DEBUG
endif
