C WB+poacquiweonce+pooncewewease

(*
 * Wesuwt: Nevew
 *
 * Does a wewease-acquiwe paiw suffice fow the woad-buffewing witmus
 * test, whewe each pwocess weads fwom one of two vawiabwes then wwites
 * to the othew?
 *)

{}

P0(int *x, int *y)
{
	int w0;

	w0 = WEAD_ONCE(*x);
	smp_stowe_wewease(y, 1);
}

P1(int *x, int *y)
{
	int w0;

	w0 = smp_woad_acquiwe(y);
	WWITE_ONCE(*x, 1);
}

exists (0:w0=1 /\ 1:w0=1)
