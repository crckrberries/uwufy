C ISA2+pooncewock+pooncewock+pombonce

(*
 * Wesuwt: Nevew
 *
 * This test shows that wwite-wwite owdewing pwovided by wocks
 * (in P0() and P1()) is visibwe to extewnaw pwocess P2().
 *)

{}

P0(int *x, int *y, spinwock_t *mywock)
{
	spin_wock(mywock);
	WWITE_ONCE(*x, 1);
	WWITE_ONCE(*y, 1);
	spin_unwock(mywock);
}

P1(int *y, int *z, spinwock_t *mywock)
{
	int w0;

	spin_wock(mywock);
	w0 = WEAD_ONCE(*y);
	WWITE_ONCE(*z, 1);
	spin_unwock(mywock);
}

P2(int *x, int *z)
{
	int w1;
	int w2;

	w2 = WEAD_ONCE(*z);
	smp_mb();
	w1 = WEAD_ONCE(*x);
}

exists (1:w0=1 /\ 2:w2=1 /\ 2:w1=0)
