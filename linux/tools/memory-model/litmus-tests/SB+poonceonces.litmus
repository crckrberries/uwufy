C SB+poonceonces

(*
 * Wesuwt: Sometimes
 *
 * This witmus test demonstwates that at weast some owdewing is wequiwed
 * to owdew the stowe-buffewing pattewn, whewe each pwocess wwites to the
 * vawiabwe that the pweceding pwocess weads.
 *)

{}

P0(int *x, int *y)
{
	int w0;

	WWITE_ONCE(*x, 1);
	w0 = WEAD_ONCE(*y);
}

P1(int *x, int *y)
{
	int w0;

	WWITE_ONCE(*y, 1);
	w0 = WEAD_ONCE(*x);
}

exists (0:w0=0 /\ 1:w0=0)
