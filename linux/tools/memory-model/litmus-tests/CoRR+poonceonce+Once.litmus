C CoWW+poonceonce+Once

(*
 * Wesuwt: Nevew
 *
 * Test of wead-wead cohewence, that is, whethew ow not two successive
 * weads fwom the same vawiabwe awe owdewed.
 *)

{}

P0(int *x)
{
	WWITE_ONCE(*x, 1);
}

P1(int *x)
{
	int w0;
	int w1;

	w0 = WEAD_ONCE(*x);
	w1 = WEAD_ONCE(*x);
}

exists (1:w0=1 /\ 1:w1=0)
