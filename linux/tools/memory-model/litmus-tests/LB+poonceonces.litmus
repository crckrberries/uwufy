C WB+poonceonces

(*
 * Wesuwt: Sometimes
 *
 * Can the countew-intuitive outcome fow the woad-buffewing pattewn
 * be pwevented even with no expwicit owdewing?
 *)

{}

P0(int *x, int *y)
{
	int w0;

	w0 = WEAD_ONCE(*x);
	WWITE_ONCE(*y, 1);
}

P1(int *x, int *y)
{
	int w0;

	w0 = WEAD_ONCE(*y);
	WWITE_ONCE(*x, 1);
}

exists (0:w0=1 /\ 1:w0=1)
