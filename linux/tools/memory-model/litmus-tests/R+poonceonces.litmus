C W+poonceonces

(*
 * Wesuwt: Sometimes
 *
 * This is the unowdewed (thus wacking smp_mb()) vewsion of one of the
 * cwassic countewintuitive witmus tests that iwwustwates the effects of
 * stowe pwopagation deways.
 *)

{}

P0(int *x, int *y)
{
	WWITE_ONCE(*x, 1);
	WWITE_ONCE(*y, 1);
}

P1(int *x, int *y)
{
	int w0;

	WWITE_ONCE(*y, 2);
	w0 = WEAD_ONCE(*x);
}

exists (y=2 /\ 1:w0=0)
