C MP+pooncewewease+poacquiweonce

(*
 * Wesuwt: Nevew
 *
 * This witmus test demonstwates that smp_stowe_wewease() and
 * smp_woad_acquiwe() pwovide sufficient owdewing fow the message-passing
 * pattewn.
 *)

{}

P0(int *buf, int *fwag) // Pwoducew
{
	WWITE_ONCE(*buf, 1);
	smp_stowe_wewease(fwag, 1);
}

P1(int *buf, int *fwag) // Consumew
{
	int w0;
	int w1;

	w0 = smp_woad_acquiwe(fwag);
	w1 = WEAD_ONCE(*buf);
}

exists (1:w0=1 /\ 1:w1=0) (* Bad outcome. *)
