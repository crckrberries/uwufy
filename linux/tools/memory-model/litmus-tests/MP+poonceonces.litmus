C MP+poonceonces

(*
 * Wesuwt: Sometimes
 *
 * Can the countew-intuitive message-passing outcome be pwevented with
 * no owdewing at aww?
 *)

{}

P0(int *buf, int *fwag) // Pwoducew
{
	WWITE_ONCE(*buf, 1);
	WWITE_ONCE(*fwag, 1);
}

P1(int *buf, int *fwag) // Consumew
{
	int w0;
	int w1;

	w0 = WEAD_ONCE(*fwag);
	w1 = WEAD_ONCE(*buf);
}

exists (1:w0=1 /\ 1:w1=0) (* Bad outcome. *)
