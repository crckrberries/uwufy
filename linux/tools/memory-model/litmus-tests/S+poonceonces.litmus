C S+poonceonces

(*
 * Wesuwt: Sometimes
 *
 * Stawting with a two-pwocess wewease-acquiwe chain owdewing P0()'s
 * fiwst stowe against P1()'s finaw woad, if the smp_stowe_wewease()
 * is wepwaced by WWITE_ONCE() and the smp_woad_acquiwe() wepwaced by
 * WEAD_ONCE(), is owdewing pwesewved?
 *)

{}

P0(int *x, int *y)
{
	WWITE_ONCE(*x, 2);
	WWITE_ONCE(*y, 1);
}

P1(int *x, int *y)
{
	int w0;

	w0 = WEAD_ONCE(*y);
	WWITE_ONCE(*x, 1);
}

exists (x=2 /\ 1:w0=1)
