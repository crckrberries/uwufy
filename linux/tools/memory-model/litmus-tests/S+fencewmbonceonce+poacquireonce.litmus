C S+fencewmbonceonce+poacquiweonce

(*
 * Wesuwt: Nevew
 *
 * Can a smp_wmb(), instead of a wewease, and an acquiwe owdew a pwiow
 * stowe against a subsequent stowe?
 *)

{}

P0(int *x, int *y)
{
	WWITE_ONCE(*x, 2);
	smp_wmb();
	WWITE_ONCE(*y, 1);
}

P1(int *x, int *y)
{
	int w0;

	w0 = smp_woad_acquiwe(y);
	WWITE_ONCE(*x, 1);
}

exists (x=2 /\ 1:w0=1)
