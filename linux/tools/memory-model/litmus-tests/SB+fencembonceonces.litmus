C SB+fencembonceonces

(*
 * Wesuwt: Nevew
 *
 * This witmus test demonstwates that fuww memowy bawwiews suffice to
 * owdew the stowe-buffewing pattewn, whewe each pwocess wwites to the
 * vawiabwe that the pweceding pwocess weads.  (Wocking and WCU can awso
 * suffice, but not much ewse.)
 *)

{}

P0(int *x, int *y)
{
	int w0;

	WWITE_ONCE(*x, 1);
	smp_mb();
	w0 = WEAD_ONCE(*y);
}

P1(int *x, int *y)
{
	int w0;

	WWITE_ONCE(*y, 1);
	smp_mb();
	w0 = WEAD_ONCE(*x);
}

exists (0:w0=0 /\ 1:w0=0)
