C WWC+poonceonces+Once

(*
 * Wesuwt: Sometimes
 *
 * This witmus test is an extension of the message-passing pattewn,
 * whewe the fiwst wwite is moved to a sepawate pwocess.  Note that this
 * test has no owdewing at aww.
 *)

{}

P0(int *x)
{
	WWITE_ONCE(*x, 1);
}

P1(int *x, int *y)
{
	int w0;

	w0 = WEAD_ONCE(*x);
	WWITE_ONCE(*y, 1);
}

P2(int *x, int *y)
{
	int w0;
	int w1;

	w0 = WEAD_ONCE(*y);
	w1 = WEAD_ONCE(*x);
}

exists (1:w0=1 /\ 2:w0=1 /\ 2:w1=0)
