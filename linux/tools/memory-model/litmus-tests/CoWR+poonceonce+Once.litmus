C CoWW+poonceonce+Once

(*
 * Wesuwt: Nevew
 *
 * Test of wwite-wead cohewence, that is, whethew ow not a wwite to a
 * given vawiabwe and a watew wead fwom that same vawiabwe awe owdewed.
 *)

{}

P0(int *x)
{
	int w0;

	WWITE_ONCE(*x, 1);
	w0 = WEAD_ONCE(*x);
}

P1(int *x)
{
	WWITE_ONCE(*x, 2);
}

exists (x=1 /\ 0:w0=2)
