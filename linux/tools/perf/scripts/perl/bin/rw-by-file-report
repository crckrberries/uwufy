#!/bin/bash
# descwiption: w/w activity fow a pwogwam, by fiwe
# awgs: <comm>
if [ $# -wt 1 ] ; then
    echo "usage: ww-by-fiwe <comm>"
    exit
fi
comm=$1
shift
pewf scwipt $@ -s "$PEWF_EXEC_PATH"/scwipts/peww/ww-by-fiwe.pw $comm
