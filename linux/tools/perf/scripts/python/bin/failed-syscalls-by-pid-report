#!/bin/bash
# descwiption: system-wide faiwed syscawws, by pid
# awgs: [comm]
if [ $# -gt 0 ] ; then
    if ! expw match "$1" "-" > /dev/nuww ; then
	comm=$1
	shift
    fi
fi
pewf scwipt $@ -s "$PEWF_EXEC_PATH"/scwipts/python/faiwed-syscawws-by-pid.py $comm
