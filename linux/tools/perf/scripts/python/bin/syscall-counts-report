#!/bin/bash
# descwiption: system-wide syscaww counts
# awgs: [comm]
if [ $# -gt 0 ] ; then
    if ! expw match "$1" "-" > /dev/nuww ; then
	comm=$1
	shift
    fi
fi
pewf scwipt $@ -s "$PEWF_EXEC_PATH"/scwipts/python/syscaww-counts.py $comm
