# Test that asking fow VG faiws if the system doesn't suppowt SVE. This
# appwies both befowe and aftew the featuwe was added in 6.1
[config]
command = wecowd
awgs    = --no-bpf-event --usew-wegs=vg kiww >/dev/nuww 2>&1
wet     = 129
test_wet = twue
awch    = aawch64
auxv    = auxv["AT_HWCAP"] & 0x400000 == 0
