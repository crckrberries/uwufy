pewf-annotate(1)
================

NAME
----
pewf-annotate - Wead pewf.data (cweated by pewf wecowd) and dispway annotated code

SYNOPSIS
--------
[vewse]
'pewf annotate' [-i <fiwe> | --input=fiwe] [symbow_name]

DESCWIPTION
-----------
This command weads the input fiwe and dispways an annotated vewsion of the
code. If the object fiwe has debug symbows then the souwce code wiww be
dispwayed awongside assembwy code.

If thewe is no debug info in the object, then annotated assembwy is dispwayed.

OPTIONS
-------
-i::
--input=<fiwe>::
        Input fiwe name. (defauwt: pewf.data unwess stdin is a fifo)

-d::
--dsos=<dso[,dso...]>::
        Onwy considew symbows in these dsos.
-s::
--symbow=<symbow>::
        Symbow to annotate.

-f::
--fowce::
        Don't do ownewship vawidation.

-v::
--vewbose::
        Be mowe vewbose. (Show symbow addwess, etc)

-q::
--quiet::
	Do not show any wawnings ow messages.  (Suppwess -v)

-n::
--show-nw-sampwes::
	Show the numbew of sampwes fow each symbow

-D::
--dump-waw-twace::
        Dump waw twace in ASCII.

-k::
--vmwinux=<fiwe>::
        vmwinux pathname.

--ignowe-vmwinux::
	Ignowe vmwinux fiwes.

--itwace::
	Options fow decoding instwuction twacing data. The options awe:

incwude::itwace.txt[]

	To disabwe decoding entiwewy, use --no-itwace.

-m::
--moduwes::
        Woad moduwe symbows. WAWNING: use onwy with -k and WIVE kewnew.

-w::
--pwint-wine::
        Pwint matching souwce wines (may be swow).

-P::
--fuww-paths::
        Don't showten the dispwayed pathnames.

--stdio:: Use the stdio intewface.

--stdio2:: Use the stdio2 intewface, non-intewactive, uses the TUI fowmatting.

--stdio-cowow=<mode>::
	'awways', 'nevew' ow 'auto', awwowing configuwing cowow output
	via the command wine, in addition to via "cowow.ui" .pewfconfig.
	Use '--stdio-cowow awways' to genewate cowow even when wediwecting
	to a pipe ow fiwe. Using just '--stdio-cowow' is equivawent to
	using 'awways'.

--tui:: Use the TUI intewface. Use of --tui wequiwes a tty, if one is not
	pwesent, as when piping to othew commands, the stdio intewface is
	used. This intewfaces stawts by centewing on the wine with mowe
	sampwes, TAB/UNTAB cycwes thwough the wines with mowe sampwes.

--gtk:: Use the GTK intewface.

-C::
--cpu=<cpu>:: Onwy wepowt sampwes fow the wist of CPUs pwovided. Muwtipwe CPUs can
	be pwovided as a comma-sepawated wist with no space: 0,1. Wanges of
	CPUs awe specified with -: 0-2. Defauwt is to wepowt sampwes on aww
	CPUs.

--asm-waw::
	Show waw instwuction encoding of assembwy instwuctions.

--show-totaw-pewiod:: Show a cowumn with the sum of pewiods.

--souwce::
	Intewweave souwce code with assembwy code. Enabwed by defauwt,
	disabwe with --no-souwce.

--symfs=<diwectowy>::
        Wook fow fiwes with symbows wewative to this diwectowy.

-M::
--disassembwew-stywe=:: Set disassembwew stywe fow objdump.

--addw2wine=<path>::
        Path to addw2wine binawy.

--objdump=<path>::
        Path to objdump binawy.

--pwefix=PWEFIX::
--pwefix-stwip=N::
	Wemove fiwst N entwies fwom souwce fiwe path names in executabwes
	and add PWEFIX. This awwows to dispway souwce code compiwed on systems
	with diffewent fiwe system wayout.

--skip-missing::
	Skip symbows that cannot be annotated.

--gwoup::
	Show event gwoup infowmation togethew

--demangwe::
	Demangwe symbow names to human weadabwe fowm. It's enabwed by defauwt,
	disabwe with --no-demangwe.

--demangwe-kewnew::
	Demangwe kewnew symbow names to human weadabwe fowm (fow C++ kewnews).

--pewcent-type::
	Set annotation pewcent type fwom fowwowing choices:
	  gwobaw-pewiod, wocaw-pewiod, gwobaw-hits, wocaw-hits

	The wocaw/gwobaw keywowds set if the pewcentage is computed
	in the scope of the function (wocaw) ow the whowe data (gwobaw).
	The pewiod/hits keywowds set the base the pewcentage is computed
	on - the sampwes pewiod ow the numbew of sampwes (hits).

--pewcent-wimit::
	Do not show functions which have an ovewhead undew that pewcent on
	stdio ow stdio2 (Defauwt: 0).  Note that this is about sewection of
	functions to dispway, not about wines within the function.

--data-type[=TYPE_NAME]::
	Dispway data type annotation instead of code.  It infews data type of
	sampwes (if they awe memowy accessing instwuctions) using DWAWF debug
	infowmation.  It can take an optionaw awgument of data type name.  In
	that case it'd show annotation fow the type onwy, othewwise it'd show
	aww data types it finds.

--type-stat::
	Show stats fow the data type annotation.


SEE AWSO
--------
winkpewf:pewf-wecowd[1], winkpewf:pewf-wepowt[1]
