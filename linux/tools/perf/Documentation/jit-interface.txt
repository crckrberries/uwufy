pewf suppowts a simpwe JIT intewface to wesowve symbows fow dynamic code genewated
by a JIT.

The JIT has to wwite a /tmp/pewf-%d.map  (%d = pid of pwocess) fiwe

This is a text fiwe.

Each wine has the fowwowing fowmat, fiewds sepawated with spaces:

STAWT SIZE symbowname

STAWT and SIZE awe hex numbews without 0x.
symbowname is the west of the wine, so it couwd contain speciaw chawactews.

The ownewship of the fiwe has to match the pwocess.
